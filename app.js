const TRANSLATIONS = {
  "en": {
    "drawer_lang_label": "Language",
    "alert_login_required": "You must log in or register to select a plan.",
    "guest_label": "Guest",
    "auth_modal_title_login": "Log in",
    "auth_modal_title_signup": "Sign up",
    "auth_modal_label_login": "Account",
    "auth_modal_label_signup": "Create account",
    "auth_modal_btn_login": "Log in",
    "auth_modal_btn_signup": "Create account",
    "footer_copyright": "&copy; 2026 TransferChek. All rights reserved. All university names and logos are trademarks of their respective owners. TransferChek has no official affiliation or endorsement relationship with these universities.",
    "site_title": "TransferChek | Top U.S. University Transfer Strategy Engine",
    "site_subtitle": "Top U.S. University Transfer Advisory",
    "refer_to_catalog_wording": "Refer to official catalog wording below",
    "nav_pricing": "Subscription Plans",
    "nav_solution": "Features",
    "nav_statcompass": "01. StatCompass",
    "ad_eligibility_title": "01. StatCompass (Eligibility)",
    "ad_eligibility_headline": "Don't miss hidden transfer opportunities matched to your current stats.",
    "ad_eligibility_b1": "Instantly evaluates your transfer eligibility across 58 top U.S. universities in 1 click.",
    "ad_eligibility_b2": "Analyzes credit transfer limits and highlights risk warnings for international transcripts.",
    "ad_eligibility_b3": "Recommends smart target alternatives to ensure you don't face rejection regret.",
    "ad_eligibility_btn": "Analyze My Stats Now",
    "ad_requirements_title": "02. ReqRadar (Prerequisite Search)",
    "ad_requirements_headline": "No more digging through outdated PDFs or confusing university catalogs.",
    "ad_requirements_b1": "Accesses canonical prerequisite lists for 58 top U.S. universities directly from our verified database.",
    "ad_requirements_b2": "Displays specific grade thresholds (minimum C/B grades required) and lab requirements.",
    "ad_requirements_b3": "Marks data confidence ratings (Verified / Needs Review) to prevent scheduling errors.",
    "ad_requirements_btn": "Search Prerequisites Now",
    "ad_roadmap_title": "03. Pathfinder (Roadmap Builder)",
    "ad_roadmap_headline": "Consolidate multiple target universities' requirements into a single optimized plan.",
    "ad_roadmap_b1": "Automatically resolves complex prerequisite dependency chains (e.g. Calculus -> Physics -> CS).",
    "ad_roadmap_b2": "Maximizes transfer credit efficiency under strict semester unit limits (up to 4 lectures + labs).",
    "ad_roadmap_b3": "Schedules buffer terms for writing recommendations, transcripts, and essay drafting.",
    "ad_roadmap_btn": "Build My Roadmap Now",
    "ad_essay_title": "04. EssayAI (AI Essay Strategist)",
    "ad_essay_headline": "Calibrated with over 120,000+ successful transfer essays.",
    "ad_essay_b1": "Directs you through a 5-step interactive interview to extract your hidden achievements and personal fit.",
    "ad_essay_b2": "Generates a bespoke 3-paragraph outline matching the target university's unique admissions rubric.",
    "ad_essay_b3": "Provides real-time DOs & DON'Ts and high-quality writing examples for your target major.",
    "ad_essay_btn": "Start Essay Strategy Now",
    "nav_reqradar": "02. ReqRadar",
    "nav_pathfinder": "03. Pathfinder",
    "nav_essayai": "04. EssayAI",
    "nav_feedback": "Feedback",
    "btn_login": "Log in",
    "btn_signup": "Sign up",
    "btn_logout": "Log out",
    "hero_eyebrow": "Data-driven transfer admissions platform",
    "hero_title": "The Ultimate End-to-End Strategy Engine for Top-Tier University Transfer",
    "hero_lead": "From verifying major prerequisites and building course roadmaps to structuring winning essays—everything you need for a successful transfer in one premium platform.",
    "hero_cta_demo": "View Strategy Engine",
    "hero_cta_solution": "Explore Features",
    "hero_proof_regret": "Regret Prevention",
    "hero_proof_regret_desc": "Discover hidden transfer opportunities matchable to your current stats.",
    "hero_proof_time": "Save Time",
    "hero_proof_time_desc": "Cut down hundreds of hours navigating complex major prerequisites.",
    "hero_proof_cost": "Optimize Tuition",
    "hero_proof_cost_desc": "Focus only on required courses to prevent wasting time and money.",
    "disclaimer_title": "📋 TransferChek Information Notice & Guidance",
    "disclaimer_body": "TransferChek is a data integration and strategy tool designed to help you organize your transfer plans. Because university admissions requirements (GPA, course prerequisites, and exam scores) can change frequently at the discretion of each institution, please use this analysis as a reference guide. We recommend that you verify all final requirements directly on the official university website before submitting your application. TransferChek continues to perform data validation (latest audit completed in June 2026) to support your transfer journey.",
    "coverage_eyebrow": "Covered universities",
    "coverage_title": "Access prerequisite requirements for top target universities in one place.",
    "coverage_desc": "Compare target major qualifications, course roadmaps, required exam scores, and requirement coverage ratings dynamically based on active database profiles.",
    "feature_eyebrow": "Why TransferChek?",
    "feature_heading": "The most expensive transfer mistake is taking course credits that do not transfer.",
    "feature1_title": "Prerequisite Eligibility Analyzer",
    "feature1_body": "Avoid missing out on target universities. Instantly cross-reference your GPA and completed courses against official requirements to check if you qualify for admissions application.",
    "feature2_title": "Prerequisite Finder",
    "feature2_body": "Skip the endless hunt through confusing university catalog sites and broken PDF guides. Search any school or major to load canonical prerequisite lists and grade thresholds instantly.",
    "feature3_title": "Optimized Roadmap Builder",
    "feature3_body": "Perfect for early planning 1-2 years ahead. Build prerequisite-compliant, term-by-term course paths to protect your GPA and ensure every single credit transfers without wasting tuition.",
    "feature4_title": "AI Essay Strategist",
    "feature4_body": "Convert your personal projects, club activities, or academic achievements into a cohesive academic story. Powered by pattern analysis of over 120,000 successful transfer essays, generate an admissions-aligned, 3-paragraph strategy essay outline instantly.",
    "demo_eyebrow": "Interactive product dashboard",
    "demo_title": "Prerequisite Eligibility Analyzer",
    "tab_eligibility": "Eligibility Diagnostics",
    "tab_requirements": "Prerequisite Finder",
    "tab_roadmap": "Roadmap Builder",
    "tab_essay": "AI Essay Strategist",
    "profile_title": "Academic Profile",
    "profile_desc": "Input your current GPA, credits, residency status, and completed courses.",
    "label_gpa": "Current college GPA",
    "label_credits": "Completed Semester Credits",
    "label_international": "International Applicant",
    "label_english_type": "Exam Type",
    "label_english_score": "Score",
    "label_completed_courses": "Completed Core Courses",
    "label_english_waiver": "English Exam Waiver (U.S. College / SAT / ACT)",
    "target_title": "Select Targets",
    "target_desc": "Search and select target universities (Free Plan: max 5 targets).",
    "btn_check_eligibility": "Analyze Coverage",
    "btn_recommend": "Recommended Targets",
    "recommend_title": "Smart Target Recommendations",
    "recommend_desc": "Target majors with high coverage score based on your academic profile. Subject to official verification.",
    "req_title": "Prerequisite Finder Search",
    "req_desc": "Search top universities to instantly load official prerequisite criteria.",
    "label_search_school": "Search School or Major",
    "roadmap_title": "Optimized Roadmap Builder Planner",
    "roadmap_desc": "Generate a semester roadmap ensuring no wasted time, credits, or GPA.",
    "label_admission_year": "Expected Admission Year",
    "label_admission_term": "Expected Term",
    "label_roadmap_target": "Target University & Major",
    "btn_build_roadmap": "Generate Efficiency Roadmap",
    "essay_title": "AI Essay Strategist Planner",
    "essay_desc": "Input your key achievements to generate an admissions-focused essay blueprint.",
    "label_essay_target": "Target University & Major",
    "label_essay_activity": "Core Activities & Projects",
    "btn_generate_essay": "Generate Essay Storyline Guide",
    "btn_essay_example1": "📝 Example 1: CS Project (U-Mich)",
    "btn_essay_example2": "⚙️ Example 2: ME Robotics (Georgia Tech)",
    "essay_outline_title": "AI Essay Blueprint",
    "essay_outline_desc": "Tailored paragraph structure connecting your activities to target university goals.",
    "essay_placeholder": "Enter your activities on the left and click generate to build your custom outline guide.",
    "feedback_eyebrow": "Feedback",
    "feedback_title": "Help improve TransferChek.",
    "feedback_desc": "Your comments are anonymous and stored locally on your browser. Used for dashboard review purposes.",
    "label_feedback_input": "Anonymous Feedback",
    "btn_submit_feedback": "Submit Feedback",
    "btn_inbox": "Owner Inbox",
    "inbox_title": "Private Feedback Inbox",
    "btn_clear": "Clear",
    "feedback_empty_alert": "Please write feedback before submitting.",
    "feedback_sending": "Sending feedback...",
    "feedback_success": "Thank you! Your feedback has been sent successfully.",
    "feedback_failed": "Feedback submission failed:",
    "auth_note": "Demo accounts are simulated in this browser session. Official security configurations require secure database sync.",
    "label_email": "Email",
    "label_password": "Password",
    "pricing_modal_title": "Subscription Plans",
    "pricing_modal_upgrade": "Upgrade Your Access",
    "pricing_modal_subtitle": "Select the best plan to optimize your transfer path. Upgrade anytime.",
    "pricing_free_beta_notice": "We are currently offering the service for free. Select the plan you want and use it immediately.",
    "active_plan_label": "Your Current Plan:",
    "tier_free_name": "Free Plan",
    "tier_free_price": "0 KRW",
    "tier_free_desc": "For initial exploration",
    "tier_free_feature1": "Analyze up to 5 target universities in StatCompass",
    "tier_free_feature2": "No access to ReqRadar, Pathfinder, or EssayAI",
    "tier_pro_name": "Pro Plan",
    "tier_pro_price": "9,900 KRW/mo",
    "tier_pro_desc": "Unlimited feasibility analyzer",
    "tier_pro_feature1": "Unlimited target evaluations in StatCompass (Service 1)",
    "tier_pro_feature2": "No access to ReqRadar, Pathfinder, or EssayAI",
    "tier_premium_name": "Premium Plan",
    "tier_premium_price": "29,900 KRW/mo",
    "tier_premium_desc": "Full transfer planner suite (AI Essay is unlimited for 1 school/major/question for 1 month)",
    "tier_premium_feature1": "Unlimited access to StatCompass, ReqRadar, and Pathfinder",
    "tier_premium_feature2": "AI Essay: Unlimited access for 1 selected school, major & essay question (Valid for 1 month)",
    "tier_essay_pass_name": "AI Target Essay Pass",
    "tier_essay_pass_price": "9,900 KRW",
    "tier_essay_pass_desc": "Standalone addon package (1 school/major/question unlimited for 1 month)",
    "tier_essay_pass_feature1": "Unlimited critiques & storyline mapper for 1 selected school, major & question",
    "tier_essay_pass_feature2": "Valid for 1 month from activation (Activate within 3 months)",
    "btn_choose_plan": "Subscribe",
    "btn_free_action": "Select Free",
    "btn_pro_action": "Subscribe Pro",
    "btn_premium_action": "Subscribe Premium",
    "btn_essay_pass_action": "Buy Target Pass",
    "essay_credits_remaining": "Active Passes:",
    "btn_buy_credits": "Buy Target Pass (9,900 KRW)",
    "essay_out_of_credits": "You have no active essay passes. Please purchase a Target Essay Pass or subscribe to Premium Plan.",
    "alert_essay_pass_purchased": "Successfully purchased AI Target Essay Pass! 1 pending essay pass has been added to your account. Activate it below for your target school, major, and essay question.",
    "display_fallback": "Official Unspecified",
    "display_fallback_none": "None",
    "display_credits_unspecified": "Credits Unspecified",
    "display_gpa_unspecified": "GPA Unspecified",
    "uc_notice_label": "UC Data Verification Recommended",
    "uc_notice_desc": "UC requirements can vary. Verification against ASSIST.org and major-specific guidelines is recommended.",
    "roadmap_multi_notice_title": "Planning for {count} Target Universities (Merged)",
    "roadmap_multi_notice_desc": "Prerequisites combined from: ",
    "roadmap_multi_warning": "Selecting more target universities may increase the required duration of coursework.",
    "roadmap_targets_label": "Roadmap Target Universities (Max 7)",
    "req_finder_placeholder_title": "Search Target Major",
    "req_finder_placeholder_desc": "Type a university or major in the search box above to view prerequisite details, official notes, and minimum transfer criteria.",
    "badge_verified_tag": "✓ Official Ref",
    "badge_high_risk_tag": "⚠ High Risk",
    "badge_needs_review_tag": "⚠ Needs Review",
    "review_opt_desc": "Verify choice condition combinations",
    "review_enroll_desc": "Verify enrollment/post-admission timeline",
    "review_dept_desc": "Check departmental prerequisite courses",
    "review_credit_desc": "Check minimum credits or transfer limits",
    "review_course_desc": "Prerequisite course conditions",
    "review_extra_desc": "Other validation items",
    "choice_note": "Prerequisite choice original terms",
    "review_reason": "Special check required",
    "choice_label_pref": "Choose one:",
    "review_count_alert": "Important: {count} items require verification",
    "search_empty": "No results found",
    "search_select_school_first": "Select school first",
    "status_pass_review": "Eligible (Needs Review)",
    "status_pass": "Eligible",
    "status_fail": "High Risk",
    "eng_review_reason": "Manually verify English exam terms",
    "rec_course_detail": "Recommended",
    "rec_choice_label": "Choose one (Recommended):",
    "rec_choice_detail": "Recommended choice • Optional",
    "rec_choice_raw_detail": "Recommended choice text • Optional",
    "rec_other_detail": "Recommended or post-admission • Optional",
    "tag_pass": "Covered",
    "tag_fail": "Risk",
    "roadmap_extra_summary": "Other required items: {count}",
    "roadmap_extra_note": "These requirements are factored into analysis, not just recommendations.",
    "roadmap_recommended_summary": "Recommended items: {count} • Optional",
    "roadmap_review_summary": "Manual review required: {count}",
    "no_targets_selected": "No target universities selected.",
    "no_recs_found": "No active recommendations. Check common courses or risk factors.",
    "no_mapped_items": "No mapping items",
    "mapped_items_desc": "No additional items listed",
    "no_review_items": "No review items",
    "review_items_desc": "Mapping and classification complete",
    "no_extra_info": "No additional info",
    "extra_info_desc": "Decision based strictly on listed minimum conditions",
    "opt_condition_desc": "Choose one of these choices",
    "no_opt_conditions": "No choice conditions",
    "opt_conditions_desc": "Straight prerequisites only",
    "no_enroll_conditions": "No post-admission timeline rules",
    "enroll_conditions_desc": "Prerequisites only",
    "note_fallback": "No advisory note available",
    "section_required_title": "Required Courses",
    "section_recommended_title": "Recommended Courses",
    "section_choices_title": "Choice Conditions",
    "section_enroll_title": "Enrollment Timeline Conditions",
    "section_review_title": "Requires Review",
    "section_extra_title": "Additional Info",
    "roadmap_tag_required": "Required",
    "roadmap_tag_recommended": "Recommended",
    "roadmap_tag_choice": "Choose one",
    "roadmap_eng_first_term": "First semester fixed",
    "roadmap_eng_second_term": "Second semester fixed",
    "buffer_term_title": "Buffer Term",
    "buffer_term_desc": "Review application, recommendations, essays",
    "essay_alert_no_activity": "Please enter your core activities!",
    "essay_loading_text": "AI is mapping your activities to target prerequisites. Please wait...",
    "essay_generating_label": "Generating...",
    "essay_generation_failed": "Outline generation failed",
    "essay_error_occurred": "An error occurred:",
    "essay_error_desc": "API rate limits or connectivity issues may apply. Please retry shortly.",
    "essay_generate_btn_label": "Generate Essay Storyline Guide",
    "req_search_placeholder": "Type school name or major...",
    "essay_activity_placeholder": "E.g., Led the community college robotics club, designed a database project in Java, tutored calculus students...",
    "search_school_placeholder": "Search School",
    "search_major_placeholder": "Search Major",
    "gpa_ref": "GPA Reference: ",
    "credits_ref": "Credits Reference: ",
    "label_school": "School",
    "label_major": "Major",
    "alert_subscribed": "Successfully subscribed to the {plan}!",
    "btn_clear_courses": "Clear All",
    "auth_consent_text": "I agree to the Terms of Service and Privacy Policy.",
    "auth_consent_link": "Read details",
    "auth_consent_error": "You must agree to the Terms and Privacy Policy to register.",
    "auth_email_exists": "An account with this email already exists on this browser.",
    "auth_invalid_credentials": "Email or password does not match.",
    "label_essay_question": "Essay Question / Prompt",
    "essay_question_placeholder": "Paste the university essay question or prompt here...",
    "label_essay_limit": "Word Limit / Length",
    "essay_limit_placeholder": "E.g., 350 words, 500 words, 1 page...",
    "essay_alert_no_question": "Please enter the essay question / prompt!",
    "essay_style_guide_title": "University Transfer Essay Style Guide",
    "tag_dos": "DOs (Key Strengths to Highlight)",
    "tag_donts": "DON'Ts (Mistakes to Avoid)",
    "tag_example": "High-Quality Writing Example",
    "essay_info_banner_title": "Calibrated AI Knowledge Base & Legal Safeguards",
    "essay_info_banner_body": "Our AI engine is calibrated with admissions criteria and transfer essay style guides for 58 top target universities (with 100% major coverage via AI real-time engine), leveraging data patterns from 120,000+ successful transfer essays. This is an educational brainstorming assistant; to comply with plagiarism and academic integrity policies, it does not draft essays for you and does not guarantee admissions.",
    "essay_mode_direct": "Direct Input",
    "essay_mode_interview": "AI Interview Guide",
    "interview_start_desc": "Answer 5 tailored questions based on your target major to extract your key experiences and fit.",
    "btn_start_interview": "Start AI Interview",
    "btn_prev": "Previous",
    "btn_next": "Next",
    "btn_complete": "Complete",
    "interview_answer_placeholder": "Type your answer here...",
    "interview_complete_msg": "AI Interview completed! Your answers have been successfully consolidated into the activity pool. Click 'Generate' to create your outline.",
    "stat_universities": "universities",
    "stat_programs": "target majors",
    "stat_programs_small": "majors",
    "stat_essays": "successful transfer essays calibrated",
    "holistic_eval_title": "Holistic Review Institution Guide",
    "holistic_eval_desc": "This institution does not enforce specific required prerequisite courses. Admissions are evaluated holistically based on overall academic achievements and potential. The absence of a prerequisite checklist is normal and is not a database error. Transfer applicants are highly encouraged to complete the most challenging coursework available to demonstrate academic excellence.",
    "btn_profile": "My Profile",
    "profile_modal_title": "Account Dashboard",
    "profile_modal_header": "My Profile",
    "profile_modal_subtitle": "Manage your account settings, subscription tier, and security preferences.",
    "profile_email_label": "Email Address",
    "profile_plan_label": "Subscription Level",
    "profile_essay_credits_label": "EssayAI Credits",
    "btn_upgrade_short": "Upgrade",
    "btn_topup_short": "Buy Credits",
    "profile_security_header": "Security Settings",
    "label_current_password": "Current Password",
    "label_new_password": "New Password",
    "btn_update_password": "Update Password",
    "locked_card_title": "Pro / Premium Plan Only",
    "locked_card_desc": "6th target and beyond require Pro Plan or higher for evaluation.",
    "btn_upgrade_now": "Upgrade Now",
    "password_change_success": "Password updated successfully!",
    "password_change_invalid": "Current password does not match.",
    "statcompass_limit_help": "Free Plan maintains a cumulative history limit of 5 analyzed universities. Upgrade to Pro/Premium for unlimited analyses.",
    "btn_toggle_recommended": "Toggle Recommended Courses",
    "label_birthdate": "Birthdate",
    "auth_underage_error": "You must be 14 or older (13 for US/other) to register.",
    "auth_birthdate_required": "Please enter your birthdate.",
    "payment_consent_minor_text": "I confirm that I am of legal age or have obtained consent from my legal guardian (parent) for this payment. (Minor consent required)",
    "payment_consent_disclaimer_text": "I agree that TransferChek is a reference tool that does not guarantee admission. I understand that I must verify requirements on official university websites, and all university names are properties of their respective trademark owners.",
    "payment_consent_error": "You must agree to all legal consents (Minor check, Disclaimer) to proceed with payment.",
    "payment_sdk_error": "Payment module is loading. Please try again in a moment.",
    "payment_failed": "Payment failed: {error}",
    "guide_s1_title": "01. Target Research & Majors",
    "guide_s1_body": "Clearly define your target universities and majors. Review the admissions requirements and GPA ranges first.",
    "guide_s2_body": "Prerequisite completion is critical. Missing core prerequisites or fundamental major coursework will lead to immediate rejection or disadvantage.",
    "guide_s4_tip": "💡 Platform Tip: Use [03. Roadmap Builder] to map out core prerequisites and major course sequences in the correct logical order.",
    "guide_s5_body": "The final differentiator is your essay and extracurriculars. Connect your projects, clubs, and academic experiences to show academic alignment with target department values.",
    "search_missing_prompt": "Can't find your target university or major?",
    "search_missing_link": "Request AI Real-Time Generation (100% Major Coverage)",
    "gen_modal_subtitle": "100% Major Coverage",
    "gen_modal_title": "AI Real-Time Prerequisite Generator",
    "gen_modal_desc": "Enter any US university and major (e.g. Stanford University - Economics) to instantly crawl/generate transfer prerequisites, minimum GPA, and course mappings using Gemini 2.0.",
    "label_gen_school": "University / College Name",
    "label_gen_major": "Major / Program Name",
    "gen_loading_text": "AI is searching and extracting transfer requirements... (Takes about 3-5 seconds)",
    "btn_gen_req": "Generate Prerequisite Profile",
    "drawer_nav1_title": "01. Eligibility Diagnostics",
    "drawer_nav1_desc": "Input your metrics to check admission coverage",
    "drawer_nav2_title": "02. Prerequisite Finder",
    "drawer_nav2_desc": "Search databases for credit transfer and GPA thresholds",
    "drawer_nav3_title": "03. Roadmap Builder",
    "drawer_nav3_desc": "Term-by-term prerequisite sequence compliance planner",
    "drawer_nav4_title": "04. EssayAI & Interview Prep",
    "drawer_nav4_desc": "AI essay guidelines and mock interview mapping",
    "drawer_nav_guide_title": "5-Step Transfer Strategy Guide",
    "drawer_nav_guide_desc": "Structured roadmap for top-tier transfer preparation",
    "drawer_nav_pricing_title": "Membership Pricing",
    "drawer_nav_pricing_desc": "Check free and premium plan options",
    "cases_showcase_eyebrow": "Admissions Specimen Database",
    "cases_showcase_heading": "Analyze Admission Specimen Database Compiled from Public Datasets",
    "cases_showcase_desc": "Explore anonymized historical transfer profiles compiled from public university datasets and community reports. This reference database represents external admitted statistics to help you benchmark your transfer candidacy, not direct client results of this service.",
    "case_showcase_c1_title": "STEM: CS Transfer",
    "case_showcase_status": "Admitted Sample",
    "label_case_gpa": "GPA:",
    "label_case_origin": "Origin CC:",
    "case_showcase_c1_origin": "De Anza College",
    "label_case_extra": "Key Extracurriculars:",
    "case_showcase_c1_extra": "Built a React scheduling app, tutored calculus, joined Robotics Club.",
    "case_showcase_c2_title": "Business: Finance",
    "case_showcase_status_haas": "Admitted Sample (Haas)",
    "case_showcase_c2_origin": "Diablo Valley College",
    "case_showcase_c2_extra": "Summer audit internship, President of CC Finance & Investment Society.",
    "case_showcase_c3_title": "Humanities: Psych",
    "case_showcase_status_ucla": "Admitted Sample (UCLA)",
    "case_showcase_c3_origin": "Santa Monica College",
    "case_showcase_c3_extra": "Crisis text line counselor, volunteer research assistant in cognitive lab.",
    "cases_showcase_cta_note": "Explore all 58 target universities and unlock complete success profiles. *Note: Specimen data are reference examples compiled from public statistics, not direct client admissions.",
    "btn_cases_showcase": "Explore Database in ReqRadar",
    "ph_coverage_title": "Canonical Prerequisite Database",
    "ph_coverage_desc": "Comprehensive coverage of top target U.S. universities",
    "guide_section_eyebrow": "Successful Transfer Strategy",
    "guide_heading": "5-Step Success Strategy for Top-Tier University Transfer",
    "guide_desc": "Transferring to top universities begins with strategic course planning and rigorous prerequisite verification. Review the 5-step success strategy below to understand how to maximize your admission chances using our platform.",
    "guide_swipe_hint": "← Swipe horizontally to view all 5 steps →",
    "track_btn_stem": "STEM Track",
    "track_btn_business": "Business Track",
    "track_btn_humanities": "Humanities Track",
    "guide_bottom_btn": "Start My Transfer Eligibility Diagnostics",
    "ph_diagnostics_title": "Eligibility Diagnostics",
    "ph_diagnostics_desc": "Input your metrics to check admission coverage",
    "label_track": "Academic Track / Major Area",
    "track_option_stem": "STEM (Engineering & Sciences)",
    "track_option_business": "Business (Management & Economics)",
    "track_option_humanities": "Humanities & Social Sciences",
    "lock_requirements_title": "Premium Plan Required",
    "lock_requirements_desc": "Upgrade to Premium to search and view prerequisite course articulations and minimum GPA thresholds for top target universities.",
    "ph_finder_title": "Prerequisite Articulation Finder",
    "ph_finder_desc": "Search databases for credit transfer and GPA thresholds",
    "lock_roadmap_title": "Premium Plan Required",
    "lock_roadmap_desc": "Upgrade to Premium to build optimized, term-by-term prerequisite roadmaps with academic coursework and non-academic advisory milestones.",
    "ph_roadmap_title": "Prerequisite Roadmap compliance planner",
    "ph_roadmap_desc": "Term-by-term prerequisite sequence compliance planner",
    "lock_essay_title": "Premium Plan or Essay Pass Required",
    "lock_essay_desc": "Upgrade to Premium or purchase a single target school Essay Pass to access the AI Essay outline planner and writing clinic.",
    "essay_subtab_critic": "Critic & Clinic",
    "essay_subtab_mapper": "Narrative Mapper",
    "essay_subtab_optimizer": "Activity Optimizer",
    "ph_feedback_title": "Anonymous Feedback Hub",
    "ph_feedback_desc": "Your suggestions directly improve our database accuracy",
    "footer_title": "TransferChek",
    "footer_rep_label": "Representative:",
    "footer_rep_val": "Hamin Kim",
    "footer_email_label": "Email:",
    "footer_biz_num_label": "Business Registration:",
    "footer_tele_num_label": "Mail Order Registration:",
    "footer_tele_num_val": "Preparing registration",
    "footer_address_label": "Address:",
    "footer_address_val": "21-1, Pilmun-daero 153beon-gil, Dong-gu, Gwangju, Republic of Korea",
    "footer_phone_label": "Tel:",
    "footer_links_title": "Support & Policies",
    "footer_link_pricing": "Pricing Plans",
    "footer_link_terms": "Terms of Service",
    "footer_link_privacy": "Privacy Policy",
    "footer_link_refund": "Refund Policy",
    "label_nationality": "Nationality",
    "nationality_korea": "South Korea (대한민국)",
    "nationality_usa": "United States (미국)",
    "nationality_china": "China (중국)",
    "nationality_other": "Other (기타)",
    "profile_nationality_label": "Nationality"
  },
  "ko": {
    "drawer_lang_label": "언어 선택 (Language)",
    "alert_login_required": "구독 요금제를 선택하거나 크레딧을 구매하려면 먼저 로그인하거나 회원가입을 해야 합니다.",
    "guest_label": "게스트",
    "auth_modal_title_login": "로그인",
    "auth_modal_title_signup": "회원가입",
    "auth_modal_label_login": "계정 로그인",
    "auth_modal_label_signup": "계정 생성",
    "auth_modal_btn_login": "로그인",
    "auth_modal_btn_signup": "가입하기",
    "footer_copyright": "&copy; 2026 TransferChek. All rights reserved. 모든 대학 이름 및 로고는 해당 소유자의 상표입니다. TransferChek은 이들 대학과 공식적인 제휴 또는 보증 관계가 없습니다.",
    "site_title": "TransferChek | 미국 대학교 편입 준비 플랫폼",
    "site_subtitle": "미국 명문대 편입 전문 컨설팅",
    "refer_to_catalog_wording": "아래 공식 요강 안내를 참조하세요",
    "nav_pricing": "구독 요금 플랜",
    "nav_solution": "주요 서비스",
    "nav_statcompass": "01. 지원 가능 판단",
    "ad_eligibility_title": "01. 지원 가능 판단 (지원자격 진단)",
    "ad_eligibility_headline": "현재 내 성적과 수강 과목에 딱 맞는 숨겨진 명문대 편입 기회를 놓치지 마세요.",
    "ad_eligibility_b1": "클릭 한 번으로 58개 명문 대학교 편입 지원 자격을 즉시 진단합니다.",
    "ad_eligibility_b2": "이수 학점 한도 초과 리스크 및 유학생 기준 영어 성적 장벽을 자동으로 체크합니다.",
    "ad_eligibility_b3": "합격 안정성을 높일 수 있는 최적의 대체 추천 대학 풀을 AI가 탐색해 드립니다.",
    "ad_eligibility_btn": "지원 자격 진단해보기",
    "ad_requirements_title": "02. 학교 학과별 편입 요건 파악 (선수과목 검색)",
    "ad_requirements_headline": "더 이상 대학 홈페이지나 깨진 PDF 가이드를 헤매지 마세요.",
    "ad_requirements_b1": "검증된 공식 데이터베이스에서 58개 명문 대학교의 필수 과목 리스트를 즉시 로드합니다.",
    "ad_requirements_b2": "과목별 이수 성적 기준(최소 C 또는 B 이상) 및 실험 과목 필수 여부를 명확히 표기합니다.",
    "ad_requirements_b3": "데이터의 신뢰도 등급(Verified / Needs Review)을 표시하여 잘못된 정보 수집 리스크를 차단합니다.",
    "ad_requirements_btn": "필수 선수과목 검색하기",
    "ad_roadmap_title": "03. 수강 로드맵 생성 (수강 로드맵 빌더)",
    "ad_roadmap_headline": "여러 대학의 서로 다른 요구사항을 만족하는 단 하나의 최적화 수강 계획.",
    "ad_roadmap_b1": "복잡한 선수과목 순서 의존성(미적분학 -> 물리 -> 전공 기초)을 고려하여 일정을 자동 자동 배치합니다.",
    "ad_roadmap_b2": "학기별 이수 한도(최대 4개 강의 + 실험) 내에서 수강 효율을 극대화합니다.",
    "ad_roadmap_b3": "추천서 준비, 에세이 초안 작성 및 서류 제출을 위한 최적의 버퍼 학기를 안내합니다.",
    "ad_roadmap_btn": "수강 로드맵 짜보기",
    "ad_essay_title": "04. EssayAI (에세이 전략가)",
    "ad_essay_headline": "120,000건 이상의 명문대 합격 에세이 패턴 분석 데이터 탑재.",
    "ad_essay_b1": "5단계의 정교한 대화형 인터뷰를 통해 본인이 잊고 있었던 학술적 성과와 전공 활동을 모두 이끌어냅니다.",
    "ad_essay_b2": "목표 대학의 에세이 질문과 인재상에 완벽히 정렬된 3문단 맞춤형 설계도를 실시간 제공합니다.",
    "ad_essay_b3": "문단별 서술 요령(DOs & DON'Ts) 및 실제 합격생 수준의 고품질 우수 영문 서술 예시를 열람할 수 있습니다.",
    "ad_essay_btn": "에세이 가이드 생성하기",
    "nav_reqradar": "02. 학교 학과별 편입 요건 파악",
    "nav_pathfinder": "03. 수강 로드맵 생성",
    "nav_essayai": "04. EssayAI",
    "nav_feedback": "피드백",
    "btn_login": "로그인",
    "btn_signup": "회원가입",
    "btn_logout": "로그아웃",
    "hero_eyebrow": "데이터 기반 명문대 편입 지원 자격 분석 플랫폼",
    "hero_title": "미국 명문대 편입을 위한 정밀 엔드투엔드 전략 엔진",
    "hero_lead": "요건 충족 확인부터 수강 로드맵, 합격 에세이까지 성공적인 명문대 편입에 필요한 모든 것을 프리미엄 플랫폼에서 제공합니다.",
    "hero_cta_demo": "전략 엔진 시작하기",
    "hero_cta_solution": "서비스 특장점 보기",
    "hero_proof_regret": "후회 방지",
    "hero_proof_regret_desc": "내 현재 스펙으로 지원 가능한 대학 범위를 빠짐없이 발굴합니다.",
    "hero_proof_time": "시간 대폭 절약",
    "hero_proof_time_desc": "학과마다 복잡하고 다른 선수과목 요건 분석 시간을 단축합니다.",
    "hero_proof_cost": "수강료 및 학점 최적화",
    "hero_proof_cost_desc": "편입에 직접 인정되는 과목들만 골라 들어 시간과 돈 낭비를 방지합니다.",
    "disclaimer_title": "📋 TransferChek 학업 정보 안내 및 고지",
    "disclaimer_body": "TransferChek은 미국 명문대 편입 요강을 통합 정리하여 효율적인 계획 수립을 돕는 데이터 분석 도구입니다. 각 대학의 최소 GPA, 필수 선수과목, 어학 성적 등 입학 조건은 학교 측 정책에 따라 수시로 변동될 수 있습니다. 본 플랫폼의 진단 결과와 로드맵을 참고용 가이드로 활용하시되, 최종 지원서 접수 전 각 대학 공식 홈페이지 정보를 최종 확인하시는 것을 권장합니다. TransferChek는 신뢰도 높은 서비스 제공을 위해 상시 데이터 검증(2026년 6월 최종 검사 완료)을 성실히 수행하고 있습니다.",
    "coverage_eyebrow": "분석 지원 대학",
    "coverage_title": "명문 대학교들의 편입 선수 과목 요건을 한자리에서 비교 확인하세요.",
    "coverage_desc": "데이터베이스에 포함된 각 대학/학과별 요건 커버리지 점수, 수강 로드맵, 필요 영어 시험 점수 등을 입체적으로 비교 분석합니다.",
    "feature_eyebrow": "왜 TransferChek인가?",
    "feature_heading": "편입 준비에서 가장 치명적인 실수는, 편입에 인정되지 않는 과목을 수강하는 것입니다.",
    "feature1_title": "지원 자격 충족 진단기",
    "feature1_body": "내 스펙으로 지원 가능한 대학을 즉시 발굴하세요. '이 학교도 이수 요건을 채웠었구나!'라는 후회가 남지 않도록, 현재 GPA와 이수 과목을 공식 대학 요강과 대조하여 충족 자격을 진단해 줍니다.",
    "feature2_title": "선수과목 통합 검색기",
    "feature2_body": "대학 홈페이지를 직접 돌아다니며 요강을 해석하는 삽질을 멈추세요. 대학과 전공만 입력하면 공식 이수 과목, 과목별 최소 성적 기준, 어학 요건까지 공식 대시보드에서 한눈에 보여줍니다.",
    "feature3_title": "최적화 수강 로드맵 빌더",
    "feature3_body": "편입 준비생과 1-2년 후 편입을 준비할 학생 모두를 위한 최적의 수강 설계도. 불필요한 과목을 수강하여 시간, GPA, 비싼 등록금을 낭비하지 않도록, 선수과목 연결고리를 고려한 가장 효율적인 학기별 로드맵을 자동으로 빌드해 줍니다.",
    "feature4_title": "AI 에세이 전략가",
    "feature4_body": "교내 동아리, 전공 프로젝트, 학술 연구 활동 등의 경험을 명문대 편입 양식에 맞추어 하나의 전공 스토리로 연결합니다. 12만 건 이상의 실제 명문 대학교 합격 에세이 패턴 분석 데이터를 바탕으로, 각 대학의 채점 기준과 에세이 스타일에 맞춤화된 3문단 전략 에세이 설계도를 즉시 생성합니다.",
    "demo_eyebrow": "대화형 제품 대시보드",
    "demo_title": "지원 자격 충족 진단기",
    "tab_eligibility": "지원 자격 충족 진단기",
    "tab_requirements": "선수과목 통합 검색기",
    "tab_roadmap": "최적화 수강 로드맵 빌더",
    "tab_essay": "AI 에세이 전략가",
    "profile_title": "나의 학업 프로필 입력",
    "profile_desc": "현재까지의 GPA, 이수 학점, 유학생 여부 및 이수한 핵심 과목들을 체크하세요.",
    "label_gpa": "현재 대학 GPA",
    "label_credits": "이수 완료한 학점 (Credits)",
    "label_international": "국제 학생 (유학생)",
    "label_english_type": "영어 시험 종류",
    "label_english_score": "획득 점수",
    "label_completed_courses": "이미 수강 완료한 핵심 과목 목록",
    "label_english_waiver": "영어 시험 면제 대상 (미국 대학 이수 / SAT 등)",
    "target_title": "목표 대학 및 학과 선택",
    "target_desc": "지원하는 타겟 학교를 검색해 추가하세요. (무료 플랜: 최대 5개 선택)",
    "btn_check_eligibility": "전략 리스크 분석",
    "btn_recommend": "추천 타겟 대학 보기",
    "recommend_title": "프로필 기반 스마트 추천 대학",
    "recommend_desc": "현재까지 입력한 이수 과목 기준으로 가장 자격 요건 충족률(Coverage)이 높은 대학 목록입니다. 지원 전 최종 대조는 필수입니다.",
    "req_title": "선수과목 통합 검색기",
    "req_desc": "대학과 학과를 검색하면 필수 선수과목 및 검증된 편입 요건 명세가 즉시 로드됩니다.",
    "label_search_school": "대학 또는 학과 이름 검색",
    "roadmap_title": "최적화 수강 로드맵 빌더 플래너",
    "roadmap_desc": "낙제 과목이나 중복 수강 없이, 목표 학기 내 편입 자격을 갖출 수 있는 학기별 추천 선수를 즉시 설계합니다.",
    "label_admission_year": "편입 목표 입학 연도",
    "label_admission_term": "편입 목표 학기",
    "label_roadmap_target": "설계 대상 목표 대학/학과",
    "btn_build_roadmap": "최적화 수강 로드맵 빌드",
    "essay_title": "AI 에세이 전략가 플래너",
    "essay_desc": "본인이 진행한 프로젝트나 교내 활동을 입력하여 전공 적합성에 최적화된 에세이 골격을 만듭니다.",
    "label_essay_target": "에세이 대상 목표 대학교 및 전공",
    "label_essay_activity": "핵심 활동 및 프로젝트 내용",
    "btn_generate_essay": "에세이 문단 가이드라인 생성",
    "btn_essay_example1": "📝 예시 1: 컴공 프로젝트 (U-Mich)",
    "btn_essay_example2": "⚙️ 예시 2: 기계공 로보틱스 (Georgia Tech)",
    "essay_outline_title": "AI 에세이 전략 가이드",
    "essay_outline_desc": "작성자의 활동 경험을 전공 요구 인재상과 논리적으로 연결한 3문단 가이드라인입니다.",
    "essay_placeholder": "왼쪽에서 활동 내용을 입력하거나 AI 인터뷰에 답변한 뒤 가이드 생성 버튼을 클릭하세요.",
    "feedback_eyebrow": "피드백 보내기",
    "feedback_title": "서비스 개선에 동참해 주세요.",
    "feedback_desc": "익명으로 작성되며 사용자 브라우저에만 기록됩니다. 관리자가 주기적으로 확인하여 요강 데이터 정정에 반영합니다.",
    "label_feedback_input": "의견 입력란",
    "btn_submit_feedback": "피드백 제출",
    "btn_inbox": "수신된 관리자 보관함",
    "inbox_title": "익명 피드백 보관함 (로컬)",
    "btn_clear": "모두 지우기",
    "feedback_empty_alert": "피드백 내용을 입력해 주세요.",
    "feedback_sending": "피드백을 전송하는 중입니다...",
    "feedback_success": "감사합니다! 피드백이 정상적으로 전송되었습니다.",
    "feedback_failed": "피드백 전송에 실패했습니다:",
    "auth_note": "데모용 계정 정보는 브라우저 로컬 저장소에 보관됩니다. 상용 제품 출시 시에는 안전한 DB 동기화가 적용됩니다.",
    "label_email": "이메일 주소",
    "label_password": "비밀번호",
    "pricing_modal_title": "구독 멤버십 플랜",
    "pricing_modal_upgrade": "멤버십 플랜 업그레이드",
    "pricing_modal_subtitle": "성공적인 명문대 편입 전략 수립을 위한 최적의 플랜을 선택하세요. 언제든 변경 가능합니다.",
    "pricing_free_beta_notice": "현재 무료로 제공 중입니다. 원하시는 모델을 선택하신 후 바로 사용하시면 됩니다.",
    "active_plan_label": "현재 이용 중인 플랜:",
    "tier_free_name": "무료 플랜 (Free Plan)",
    "tier_free_price": "0원",
    "tier_free_desc": "기본 기능 탐색용",
    "tier_free_feature1": "지원 가능 판단 기능 내 대학 분석 최대 5개 한도 제공",
    "tier_free_feature2": "학교 학과별 편입 요건 파악, 수강 로드맵 생성, AI 에세이 전략가 모든 기능 접근 제한",
    "tier_pro_name": "Pro 플랜",
    "tier_pro_price": "월 9,900원",
    "tier_pro_desc": "무제한 지원 가능 판단",
    "tier_pro_feature1": "지원 가능 판단 기능 내 목표 대학 무제한 분석 지원",
    "tier_pro_feature2": "학교 학과별 편입 요건 파악, 수강 로드맵 생성, AI 에세이 전략가 모든 기능 접근 제한",
    "tier_premium_name": "Premium 플랜",
    "tier_premium_price": "29,900원 / 월",
    "tier_premium_desc": "통합 편입 계획 패키지 (AI 에세이는 학교/학과/질문 딱 하나 지정하여 1개월간 무제한)",
    "tier_premium_feature1": "지원 가능 판단, 학교 학과별 편입 요건 파악, 수강 로드맵 생성 무제한 제공",
    "tier_premium_feature2": "AI 에세이: 학교/학과/질문 딱 하나 지정하여 1개월 동안 무제한 첨삭 및 이용 가능",
    "tier_essay_pass_name": "AI 에세이 대상학교 패스",
    "tier_essay_pass_price": "9,900원",
    "tier_essay_pass_desc": "단독 추가 패키지 (1개 학교/학과/질문 1개월 무제한)",
    "tier_essay_pass_feature1": "지정한 학교, 학과, 에세이 질문에 대해 1개월 동안 무제한 첨삭 및 매퍼 제공",
    "tier_essay_pass_feature2": "구매 후 3개월(90일) 이내 활성화 필수, 활성화 시 1개월간 무제한 유효",
    "btn_choose_plan": "구독하기",
    "btn_free_action": "무료 선택",
    "btn_pro_action": "Pro 구독하기",
    "btn_premium_action": "Premium 구독하기",
    "btn_essay_pass_action": "대상패스 구매",
    "essay_credits_remaining": "보유 중인 패스 수:",
    "btn_buy_credits": "대상학교 패스 구매 (9,900원)",
    "essay_out_of_credits": "활성화된 에세이 권한 또는 대기 중인 패스가 없습니다. 대상학교 패스를 구매하거나 Premium 플랜을 구독해 주세요.",
    "alert_essay_pass_purchased": "AI 에세이 대상학교 패스를 성공적으로 구매하였습니다! 1개의 대기 중인 패스가 추가되었습니다. 아래에서 목표 학교, 학과, 질문을 설정하고 활성화하세요.",
    "display_fallback": "공식 미명시",
    "display_fallback_none": "없음",
    "display_credits_unspecified": "이수 학점 미명시",
    "display_gpa_unspecified": "최소 GPA 미명시",
    "uc_notice_label": "UC 데이터 개별 확인 권장",
    "uc_notice_desc": "UC 계열은 캠퍼스/학과/ASSIST 기준에 따라 조건이 달라질 수 있어 현재 앱에서는 확인 필요 대상으로 표시합니다. 최종 판정 전 학과별 공식 자료와 ASSIST 대조가 필요합니다.",
    "roadmap_multi_notice_title": "선택한 {count}개 목표 전공 공동 설계 중 (병합됨)",
    "roadmap_multi_notice_desc": "병합된 대상 대학: ",
    "roadmap_multi_warning": "목표 학교를 여러개 선택할 수록 수강 기간이 오래 걸릴 수 있습니다.",
    "roadmap_targets_label": "로드맵 설계 대상 목표 대학 (최대 7개)",
    "req_finder_placeholder_title": "타겟 대학 및 학과 검색",
    "req_finder_placeholder_desc": "위 검색창에 대학 또는 학과명을 입력하시면 필요한 필수/권장 선수과목 정보와 공식 가이드를 이곳에서 상세히 볼 수 있습니다.",
    "badge_verified_tag": "✓ 공식 요강 기준",
    "badge_high_risk_tag": "⚠ High Risk (검증 필요)",
    "badge_needs_review_tag": "⚠ Needs Review (확인 필요)",
    "review_opt_desc": "선택 조건에서 어떤 과목 조합이 인정되는지 확인",
    "review_enroll_desc": "등록 후/입학 후 조건인지 확인",
    "review_dept_desc": "해당 학과의 별도 선수과목 표가 있는지 확인",
    "review_credit_desc": "최소 이수 학점 또는 이전 가능 학점 제한 확인",
    "review_course_desc": "과목 조건",
    "review_extra_desc": "추가 확인 항목",
    "choice_note": "선택 조건 원문",
    "review_reason": "별도 확인 항목",
    "choice_label_pref": "택1:",
    "review_count_alert": "꼭 확인해주세요: 원문 확인 필요 {count}개",
    "search_empty": "검색 결과 없음",
    "search_select_school_first": "학교를 먼저 선택하세요",
    "status_pass_review": "추천 검토 · 확인 필요",
    "status_pass": "추천 검토",
    "status_fail": "리스크 높음",
    "eng_review_reason": "영어 점수 원문 별도 확인",
    "rec_course_detail": "권장",
    "rec_choice_label": "택1 권장:",
    "rec_choice_detail": "권장 선택 조건 · 필수 아님",
    "rec_choice_raw_detail": "권장 선택 조건 원문 · 필수 아님",
    "rec_other_detail": "권장 또는 추후 확인 · 필수 아님",
    "tag_pass": "커버됨",
    "tag_fail": "리스크",
    "roadmap_extra_summary": "나머지 필수 요건 {count}개",
    "roadmap_extra_note": "아래 항목은 권장이 아니라 requirement coverage와 리스크 분석에 포함되는 요건입니다.",
    "roadmap_recommended_summary": "권장 요건 {count}개 · 필수 아님",
    "roadmap_review_summary": "별도 확인 필요 {count}개",
    "no_targets_selected": "선택한 목표 대학/학과가 없습니다.",
    "no_recs_found": "현재 추가 추천 없음. 공통 과목을 더 체크하거나 리스크 항목을 검토하세요.",
    "no_mapped_items": "매핑 항목 없음",
    "mapped_items_desc": "추가 표시 항목 없음",
    "no_review_items": "확인 필요 항목 없음",
    "review_items_desc": "자동 매핑 또는 분류 완료",
    "no_extra_info": "추가 참고 정보 없음",
    "extra_info_desc": "명시된 최소 조건 중심으로 판정",
    "opt_condition_desc": "둘 중 하나를 택하는 선택 조건",
    "no_opt_conditions": "택1 조건 없음",
    "opt_conditions_desc": "공통 필수 과목으로 직접 판정",
    "no_enroll_conditions": "분리된 등록/추후 조건 없음",
    "enroll_conditions_desc": "지원 전 조건 중심",
    "note_fallback": "검증 노트 없음",
    "section_required_title": "필수 과목",
    "section_recommended_title": "권장 과목 (필수 아님)",
    "section_choices_title": "선택 조건",
    "section_enroll_title": "등록/추후 조건",
    "section_review_title": "별도 확인 필요",
    "section_extra_title": "추가 정보",
    "roadmap_tag_required": "필수과목",
    "roadmap_tag_recommended": "권장과목",
    "roadmap_tag_choice": "택1 필수",
    "roadmap_eng_first_term": "첫 번째 학기 고정",
    "roadmap_eng_second_term": "두 번째 학기 고정",
    "buffer_term_title": "Buffer term",
    "buffer_term_desc": "지원서, 추천서, 에세이 점검",
    "essay_alert_no_activity": "주요 교내외 활동 내용을 입력해 주세요!",
    "essay_loading_text": "AI가 목표 대학의 편입 요건에 맞추어 에세이 아웃라인을 설계하고 있습니다. 잠시만 기다려 주세요...",
    "essay_generating_label": "생성 중...",
    "essay_generation_failed": "아웃라인 생성 실패",
    "essay_error_occurred": "오류가 발생했습니다:",
    "essay_error_desc": "API 할당량 초과이거나 서버 연결 상태가 불안정할 수 있습니다. 잠시 후 다시 시도해 주세요.",
    "essay_generate_btn_label": "에세이 문단 가이드라인 생성",
    "req_search_placeholder": "대학 이름 또는 전공을 입력하세요...",
    "essay_activity_placeholder": "예: 커뮤니티 칼리지 로봇 동아리 회장 역임, 자바로 데이터베이스 프로젝트 설계, 미적분 튜터 활동 등...",
    "search_school_placeholder": "학교 검색",
    "search_major_placeholder": "전공 검색",
    "gpa_ref": "GPA 요구사항: ",
    "credits_ref": "학점 요구사항: ",
    "label_school": "학교",
    "label_major": "전공",
    "alert_subscribed": "성공적으로 {plan} 요금제를 구독했습니다!",
    "btn_clear_courses": "모두 지우기",
    "auth_consent_text": "서비스 이용약관 및 개인정보 처리방침에 동의합니다.",
    "auth_consent_link": "자세히 보기",
    "auth_consent_error": "회원가입을 하려면 서비스 이용약관 및 개인정보 처리방침에 동의해야 합니다.",
    "auth_email_exists": "이미 이 이메일로 가입된 계정이 존재합니다.",
    "auth_invalid_credentials": "이메일 또는 비밀번호가 올바르지 않습니다.",
    "label_essay_question": "에세이 질문 / 프롬프트",
    "essay_question_placeholder": "여기에 대학 공식 에세이 질문이나 프롬프트를 붙여넣으세요...",
    "label_essay_limit": "글자 수 또는 분량 제한",
    "essay_limit_placeholder": "예: 350단어, 500단어, 1페이지 등...",
    "essay_alert_no_question": "에세이 질문 / 프롬프트를 입력해 주세요!",
    "essay_style_guide_title": "해당 대학 편입 에세이 심사 기준 & 스타일 가이드",
    "tag_dos": "추천 서술 방향 (DOs)",
    "tag_donts": "작성 시 금기 사항 (DON'Ts)",
    "tag_example": "우수 영문 서술 예시",
    "essay_info_banner_title": "AI 에세이 학습 상태 및 법적 안내",
    "essay_info_banner_body": "본 AI 엔진은 58개 명문 대학교의 편입 요건뿐만 아니라, 120,000건 이상의 실제 명문대 합격 에세이 패턴 분석 데이터에 기반하여 정교하게 칼리브레이션되어 있습니다. 입력하신 학생 활동을 최적의 뼈대로 엮는 브레인스토밍 가이드를 제공하며, 대학의 학업적 성실성 원칙에 의거하여 대필은 제공하지 않으며 입학을 보장하지 않습니다.",
    "essay_mode_direct": "직접 입력",
    "essay_mode_interview": "AI 인터뷰 가이드",
    "interview_start_desc": "목표 대학 및 학과에 특화된 5가지 질문에 답변하며 본인의 핵심 전공 활동과 성향을 빠짐없이 이끌어내세요.",
    "btn_start_interview": "AI 인터뷰 시작",
    "btn_prev": "이전",
    "btn_next": "다음",
    "btn_complete": "인터뷰 완료",
    "interview_answer_placeholder": "여기에 답변을 입력하세요...",
    "interview_complete_msg": "AI 인터뷰가 완료되었습니다! 답변하신 내용들이 활동 항목으로 정제되었습니다. 하단의 '에세이 가이드 생성' 버튼을 클릭하여 맞춤 가이드를 확인하세요.",
    "stat_universities": "연계 대학",
    "stat_programs": "개설 전공",
    "stat_programs_small": "개 전공",
    "stat_essays": "합격 에세이 패턴 분석 완료",
    "holistic_eval_title": "종합 평가(Holistic Review) 대학 안내",
    "holistic_eval_desc": "본 대학교는 공식적인 필수 선이수 과목 지정이 없으며, 지원자의 전반적인 학업 성취도 및 역량을 종합적으로 평가(Holistic Review)합니다. 따라서 필수 과목 체크리스트가 표시되지 않는 것은 정보 누락이나 데이터 오류가 아닌 실제 대학의 편입 요강에 따른 정상적인 상태입니다. 이전 대학에서 본인이 이수할 수 있는 가장 높은 수준의 물리, 수학 및 전공 기초 과목을 적극적으로 수강하여 학업적 성실성과 도전 정신을 입증하는 것이 합격률을 높이는 최선의 방법입니다.",
    "btn_profile": "내 프로필",
    "profile_modal_title": "계정 대시보드",
    "profile_modal_header": "내 프로필",
    "profile_modal_subtitle": "계정 설정, 구독 등급 및 비밀번호 등 보안 설정을 관리하세요.",
    "profile_email_label": "이메일 주소",
    "profile_plan_label": "이용 중인 플랜",
    "profile_essay_credits_label": "에세이 남은 횟수",
    "btn_upgrade_short": "업그레이드",
    "btn_topup_short": "이용권 구매",
    "profile_security_header": "보안 설정",
    "label_current_password": "현재 비밀번호",
    "label_new_password": "새로운 비밀번호",
    "btn_update_password": "비밀번호 변경",
    "locked_card_title": "Pro / Premium 플랜 전용",
    "locked_card_desc": "6번째 대학부터는 Pro 플랜 이상에서 분석 가능합니다.",
    "btn_upgrade_now": "지금 업그레이드",
    "password_change_success": "비밀번호가 성공적으로 변경되었습니다!",
    "password_change_invalid": "현재 비밀번호가 올바르지 않습니다.",
    "statcompass_limit_help": "무료 플랜은 분석 가능한 대학이 누적 최대 5개로 제한됩니다. 무제한 분석을 원하시면 Pro/Premium 플랜으로 업그레이드하세요.",
    "btn_toggle_recommended": "추천 과목 보기/숨기기",
    "label_birthdate": "생년월일 (Birthdate)",
    "auth_underage_error": "만 14세 미만(미국/기타 국적은 만 13세 미만)은 회원가입이 제한됩니다.",
    "auth_birthdate_required": "생년월일을 입력해 주세요.",
    "payment_consent_minor_text": "[필수] 본인은 성인이거나 법정대리인(부모)의 결제 동의를 받았음을 확인합니다. (미성년자 결제 시 부모 동의 필수)",
    "payment_consent_disclaimer_text": "[필수] TransferChek의 매칭 결과는 참고용 가이드이며 입학을 보장하지 않으며, 대학 정보의 공식 요건을 입학처 웹사이트를 통해 직접 재확인해야 하고, 모든 대학명은 개별 상표권에 귀속됨에 동의합니다.",
    "payment_consent_error": "결제를 진행하려면 모든 법적 고지(미성년자 동의, 면책 동의)에 동의하셔야 합니다.",
    "payment_sdk_error": "결제 모듈이 아직 로드 중입니다. 잠시 후 다시 시도해 주세요.",
    "payment_failed": "결제에 실패하였습니다: {error}",
    "ph_coverage_desc": "58개 미국 명문 대학교의 편입 요건 보유",
    "lock_requirements_desc": "58개 미국 명문 대학교의 상세 선수과목 조건과 합격생 평균 학점 컷(GPA Threshold)을 조회하려면 프리미엄 멤버십으로 업그레이드하세요.",
    "guide_heading": "미국 명문대 합격을 위한 5단계 성공 전략",
    "guide_desc": "미국 명문 대학교 편입은 체계적인 수강 계획과 철저한 선수과목 검증에서 시작됩니다. 아래 5단계 성공 전략을 확인하고, 본 플랫폼의 기능을 어떻게 연계하여 합격률을 극대화할 수 있는지 파악하십시오.",
    "guide_s1_title": "01. 목표 대학 및 전공 탐색 (Target Research)",
    "guide_s1_body": "자신이 편입하고자 하는 대학교와 전공을 명확히 정의합니다. 각 대학의 입학처 요강과 정량적 합격 지표(GPA, 최저 학점)를 먼저 검토해야 합니다.",
    "guide_s2_body": "미국 명문대 편입 심사에서 선수과목(Prerequisites) 이수 여부는 절대적입니다. 전공 분야별 핵심 수학, 과학, 기초 필수 과목 중 하나라도 누락되면 서류 심사에서 불이익을 받거나 탈락 처리될 수 있습니다.",
    "guide_s4_tip": "💡 플랫폼 활용 팁: [03. 수강 로드맵 빌더]를 통해 복잡하게 꼬이기 쉬운 수학, 과학, 전공 필수 과목들을 최적의 논리적 순서로 구성해 보십시오.",
    "guide_s5_body": "상위권 대학 편입 합격을 결정짓는 마지막 열쇠는 에세이(Personal Statement / SOP)와 모의 면접입니다. 본인의 학업 성과(GPA)와 더불어, 교내외 동아리, 전공 프로젝트, 학술 성과 등을 관심사와 결합하여 설득력 있는 스토리로 전달해야 합니다.",
    "search_missing_prompt": "원하는 대학교나 전공이 리스트에 없나요?",
    "search_missing_link": "AI 실시간 요건 생성 요청 (100% 전공 커버)",
    "gen_modal_subtitle": "100% 전공 데이터 커버리지",
    "gen_modal_title": "AI 실시간 편입 요건 생성기",
    "gen_modal_desc": "리스트에 없는 미국 대학교와 전공명(예: Stanford University - Economics)을 입력해 주시면, Gemini 2.0을 통해 실시간으로 편입 선수과목, 최소 GPA 및 영어 점수 규정을 분석하여 생성합니다.",
    "label_gen_school": "대학교 공식 영문 이름",
    "label_gen_major": "학과 / 전공 영문 이름",
    "gen_loading_text": "AI가 웹상의 대학 공식 요강을 검색 및 분석하여 편입 선수과목 정보를 생성하고 있습니다... (약 3~5초 소요)",
    "btn_gen_req": "전공 편입 요건 프로필 생성하기",
    "drawer_nav1_title": "01. 지원 가능 판단",
    "drawer_nav1_desc": "현재 내 스펙으로 지원 가능한 대학 진단",
    "drawer_nav2_title": "02. 편입 요건 검색",
    "drawer_nav2_desc": "목표 대학/학과별 학점 및 과목 요건 조회",
    "drawer_nav3_title": "03. 수강 로드맵 빌더",
    "drawer_nav3_desc": "선수과목 이수를 위한 학기별 최적 계획표 생성",
    "drawer_nav4_title": "04. EssayAI & 면접 준비",
    "drawer_nav4_desc": "AI 에세이 가이드라인 및 모의 면접 질문 매핑",
    "drawer_nav_guide_title": "5단계 편입 성공 전략 가이드",
    "drawer_nav_guide_desc": "체계적이고 검증된 미국 대학 편입 준비 로드맵",
    "drawer_nav_pricing_title": "멤버십 요금제",
    "drawer_nav_pricing_desc": "무료 및 프리미엄 요금제 혜택 확인",
    "cases_showcase_eyebrow": "편입 합격 표본 데이터베이스 공개",
    "cases_showcase_heading": "공개 데이터 기반 합격생 표본 데이터 분석",
    "cases_showcase_desc": "대학 공식 공시 자료 및 편입 커뮤니티의 합격 사례 통계 데이터를 학습·분석하여 구성한 참고용 표본 데이터베이스입니다. 본 웹사이트를 통한 직접 합격 실적이 아닌, 학습된 외부 데이터 기반의 학업 분석 정보입니다.",
    "case_showcase_c1_title": "STEM: 컴퓨터공학 편입",
    "case_showcase_status": "합격 사례 표본",
    "label_case_gpa": "GPA:",
    "label_case_origin": "전적대/CC:",
    "case_showcase_c1_origin": "디안자 칼리지",
    "label_case_extra": "핵심 비교과 활동:",
    "case_showcase_c1_extra": "React 시간표 앱 개발, 미적분학 튜터링, 로보틱스 클럽 참여.",
    "case_showcase_c2_title": "경영/상경계열 편입",
    "case_showcase_status_haas": "합격 사례 표본 (버클리 하스)",
    "case_showcase_c2_origin": "디아블로 밸리 칼리지",
    "case_showcase_c2_extra": "회계법인 여름 감사 인턴십, 교내 금융/투자 학회 회장 역임.",
    "case_showcase_c3_title": "인문/사회계열 편입",
    "case_showcase_status_ucla": "합격 사례 표본 (UCLA 심리학)",
    "case_showcase_c3_origin": "산타모니카 칼리지",
    "case_showcase_c3_extra": "청소년 위기 텍스트 라인 상담가, 인지심리 연구소 연구 조원 봉사.",
    "cases_showcase_cta_note": "무료 플랜에서도 58개 대학 요건을 생성/조회할 수 있습니다. 프리미엄 업그레이드 시 전체 합격 데이터베이스를 잠금해제합니다. *참고: 본 표본은 공개 통계 기반 자료이며 본 서비스의 직접적 합격 실적이 아닙니다.",
    "btn_cases_showcase": "ReqRadar에서 합격 데이터베이스 조회하기",
    "ph_coverage_title": "공식 편입 선수과목 데이터베이스",
    "guide_section_eyebrow": "성공적인 명문대 편입 전략",
    "guide_swipe_hint": "← 모든 단계를 보려면 가로로 스와이프하세요 →",
    "track_btn_stem": "STEM 계열",
    "track_btn_business": "상경/경영 계열",
    "track_btn_humanities": "인문/사회 계열",
    "guide_bottom_btn": "내 편입 지원 자격 진단 시작하기",
    "ph_diagnostics_title": "지원 자격 진단",
    "ph_diagnostics_desc": "이수 요건과 성적을 입력하여 지원 가능 여부를 확인하세요",
    "label_track": "학업 계열 / 전공 분야",
    "track_option_stem": "STEM (공학 및 자연과학)",
    "track_option_business": "비즈니스 (경영 및 경제)",
    "track_option_humanities": "인문학 및 사회과학",
    "lock_requirements_title": "프리미엄 요금제 필요",
    "ph_finder_title": "선수과목 이수 조건 검색기",
    "ph_finder_desc": "각 대학/학과별 학점 및 과목 이수 규정 조회",
    "lock_roadmap_title": "프리미엄 요금제 필요",
    "lock_roadmap_desc": "학기별 필수 선수과목 이수 최적화 및 비학업적인 편입 일정을 구성하려면 프리미엄 멤버십으로 업그레이드하세요.",
    "ph_roadmap_title": "학기별 최적 수강 로드맵 빌더",
    "ph_roadmap_desc": "선수과목 순서 의존성을 고려한 논리적 수강 계획표",
    "lock_essay_title": "프리미엄 요금제 혹은 에세이 패스 필요",
    "lock_essay_desc": "AI 에세이 스토리라인 설계도 및 작문 문단 클리닉을 활성화하려면 프리미엄 멤버십으로 업그레이드하거나 에세이 패스를 구매하세요.",
    "essay_subtab_critic": "에세이 문단 클리닉 (Critic & Clinic)",
    "essay_subtab_mapper": "소재 & 스토리 매핑 (Narrative Mapper)",
    "essay_subtab_optimizer": "활동 이력 압축기 (Activity Optimizer)",
    "ph_feedback_title": "익명 피드백 허브",
    "ph_feedback_desc": "보내주시는 제안사항은 데이터베이스 정확도 개선에 활용됩니다",
    "footer_title": "트랜스퍼체크 (TransferChek)",
    "footer_rep_label": "대표자명:",
    "footer_rep_val": "김하민",
    "footer_email_label": "이메일:",
    "footer_biz_num_label": "사업자등록번호:",
    "footer_tele_num_label": "통신판매업신고번호:",
    "footer_tele_num_val": "신고 준비 중",
    "footer_address_label": "주소:",
    "footer_address_val": "광주광역시 동구 필문대로153번길 21-1 (산수동)",
    "footer_phone_label": "유선번호:",
    "footer_links_title": "고객지원 & 약관",
    "footer_link_pricing": "요금제 안내 (Pricing Plans)",
    "footer_link_terms": "서비스 이용약관 (Terms of Service)",
    "footer_link_privacy": "개인정보 처리방침 (Privacy Policy)",
    "footer_link_refund": "환불 및 해지 정책 (Refund Policy)",
    "label_nationality": "국적",
    "nationality_korea": "대한민국 (South Korea)",
    "nationality_usa": "미국 (United States)",
    "nationality_china": "중국 (China)",
    "nationality_other": "기타 (Other)",
    "profile_nationality_label": "국적"
  },
  "zh": {
    "drawer_lang_label": "选择语言 (Language)",
    "site_title": "TransferChek | 美国顶尖大学转学策略分析平台",
    "site_subtitle": "全美顶尖大学转学专业咨询",
    "refer_to_catalog_wording": "请参阅下方的官方说明",
    "nav_pricing": "订阅会员方案",
    "nav_solution": "主要服务",
    "nav_statcompass": "01. StatCompass",
    "ad_eligibility_title": "01. StatCompass (转学可行性分析)",
    "ad_eligibility_headline": "根据您当前的成绩与已修课程，全面发掘您可能忽略的顶尖转学机会。",
    "ad_eligibility_b1": "一键瞬间比对 58 所全美名校官方录取准入门槛。",
    "ad_eligibility_b2": "精准诊断已修学分限制以及国际生语言成绩要求，提前规避申请风险。",
    "ad_eligibility_b3": "基于数据智能匹配，为您推荐更具成功率的替代性保底与冲刺名校方案。",
    "ad_eligibility_btn": "立即评估转学资格",
    "ad_requirements_title": "02. ReqRadar (先修课程搜索引擎)",
    "ad_requirements_headline": "告别冗长复杂的官网指引与陈旧失效的 PDF 手册。",
    "ad_requirements_b1": "汇聚全美名校的官方先修要求，专业名称与课号一目了然。",
    "ad_requirements_b2": "明示各科最低成绩门槛（如最低要求 C 或 B）以及实验课联合修读要求。",
    "ad_requirements_b3": "标注数据可信度评级（Verified / Needs Review），防止因信息滞后导致选课失误。",
    "ad_requirements_btn": "立即检索先修课要求",
    "ad_roadmap_title": "03. Pathfinder (最优选课路线规划)",
    "ad_roadmap_headline": "将多所目标院校的先修课门槛，融汇为一套完美的学期修读路线图。",
    "ad_roadmap_b1": "自动解析前置课程依赖链，防止课程顺序错乱。",
    "ad_roadmap_b2": "在每学期严格学分限制（最多4门主课+实验）下，最大化转学分效率。",
    "ad_roadmap_b3": "为材料递交、教授推荐信沟通及文书打磨预留最佳的缓冲学期时间。",
    "ad_roadmap_btn": "开始规划学习路线",
    "ad_essay_title": "04. EssayAI (转学文书策略家)",
    "ad_essay_headline": "融合 120,000+ 篇名校成功转学文书的套路与规律。",
    "ad_essay_b1": "通过 5 步互动面试，全面唤醒并提炼您在专业实践及课外活动中的潜在闪光点。",
    "ad_essay_b2": "针对目标院校文书题目与招生官偏好，定制阻碍其评审标准的 3 段式叙事蓝图。",
    "ad_essay_b3": "提供逐段的写作黄金法则（DOs & DON'Ts）以及可供参考的高水平英文范例。",
    "ad_essay_btn": "立即定制文书指南",
    "nav_reqradar": "02. ReqRadar",
    "nav_pathfinder": "03. Pathfinder",
    "nav_essayai": "04. EssayAI",
    "nav_feedback": "反馈意见",
    "btn_login": "登录",
    "btn_signup": "注册",
    "btn_logout": "登出",
    "hero_eyebrow": "数据驱动型美国大学转学申请分析平台",
    "hero_title": "全美顶尖大学转学一站式端到端决策引擎",
    "hero_lead": "从比对专业先修课程、规划学期选课路线，到定制冲刺名校的文书策略——成功转学所需的一切，尽在一个高端平台。",
    "hero_cta_demo": "启动策略分析",
    "hero_cta_solution": "了解服务特色",
    "hero_proof_regret": "避免遗憾",
    "hero_proof_regret_desc": "根据您当前的成绩与课程，全面逆向匹配您可能忽略的顶尖大学转学机会。",
    "hero_proof_time": "大幅节省时间",
    "hero_proof_time_desc": "无需耗费数百小时去研究各大学繁琐而截然不同的先修课规则。",
    "hero_proof_cost": "优化课程学分",
    "hero_proof_cost_desc": "精准定位可转学分的先修课程，防止浪费高昂的学费和宝贵的学期时间。",
    "disclaimer_title": "📋 TransferChek 学业信息指南与特别说明",
    "disclaimer_body": "TransferChek 是一款旨在整合美国名校转学要求并帮助您制定高效规划的数据分析工具。各大学的最低 GPA、先修课程和语言要求等录取条件可能会根据校方政策随时变动。请将本平台的评估结果 and 路线图作为参考指南，并建议在正式提交申请前与目标大学官方招生网站的信息进行最终核对。TransferChek 持续进行数据验证（最新审核完成于 2026 年 6 月），以全力支持您的转学旅程。",
    "coverage_eyebrow": "已覆盖大学",
    "coverage_title": "一站式比对顶尖大学的先修课程和录取标准。",
    "coverage_desc": "动态对比目标专业的先修课程匹配度、学期路线图、语言成绩门槛，基于本地最新数据库提供全面覆盖率评分。",
    "feature_eyebrow": "为什么选择 TransferChek？",
    "feature_heading": "转学申请中最昂贵的错误，是修读了无法转为目标大学学分的无效课程。",
    "feature1_title": "转学先修资格评估仪",
    "feature1_body": "避免遗漏申请机会。通过系统精准比对已修学分与GPA是否满足各大学官方先修课资格线，助您快速判定是否具备申请资格。",
    "feature2_title": "先修课程搜索引擎",
    "feature2_body": "无需再逐个翻阅繁琐的大学官网和失效的 PDF 指南。只需搜索目标专业，即可瞬间载入官方公布的先修课程列表和最低成绩要求。",
    "feature3_title": "最优选课路线规划",
    "feature3_body": "专为计划在1-2年后转学的学生量身定制。自动构建符合先修关系的学期选课计划，保护您的 GPA，确保每学分均能成功转换，杜绝浪费学费。",
    "feature4_title": "AI转学文书策略家",
    "feature4_body": "将您的个人项目、社团或学术成果转化为富有逻辑的学术故事。基于对 120,000+ 篇成功转学文书的模式分析，瞬间生成与名校录取偏好契合的3段式策略性文书大纲指南。",
    "demo_eyebrow": "交互式产品仪表盘",
    "demo_title": "转学先修资格评估仪",
    "tab_eligibility": "转学先修资格评估仪",
    "tab_requirements": "先修课程搜索引擎",
    "tab_roadmap": "最优选课路线规划",
    "tab_essay": "AI转学文书策略家",
    "profile_title": "输入您的学术档案",
    "profile_desc": "填写当前的 GPA、已完成学分、是否为国际生，并勾选已修读的课程。",
    "label_gpa": "当前大学 GPA",
    "label_credits": "已修满学分 (Credits)",
    "label_international": "国际申请生",
    "label_english_type": "语言考试类型",
    "label_english_score": "获得分数",
    "label_completed_courses": "已修完的核心课程列表",
    "label_english_waiver": "免除英语考试 (美高美本学分 / SAT / ACT)",
    "target_title": "选择目标大学及专业",
    "target_desc": "搜索并选择您的目标院校和专业。(免费方案：最多评估5所)",
    "btn_check_eligibility": "评估课程覆盖率",
    "btn_recommend": "查看智能推荐大学",
    "recommend_title": "基于学术档案的智能推荐",
    "recommend_desc": "根据您已修的课程，系统推荐的目前要求覆盖率最高的目标大学。在申请前必须去官网进行核对。",
    "req_title": "先修课程搜索引擎",
    "req_desc": "输入并搜索目标大学和专业，即刻载入官方先修课要求与录取标准明细。",
    "label_search_school": "搜索目标大学或专业",
    "roadmap_title": "最优选课路线规划",
    "roadmap_desc": "避免修读无效课程。系统会结合课程的依赖关系，即时生成能够让您在预定学期顺利转学的最佳选课计划。",
    "label_admission_year": "期望入学年份",
    "label_admission_term": "期望入学学期",
    "label_roadmap_target": "规划的目标大学/专业",
    "btn_build_roadmap": "生成最优选课路线图",
    "essay_title": "AI转学文书策略家",
    "essay_desc": "输入您的学术研究、活动或项目经历，系统会分析目标专业的选才偏好并匹配出最佳文书大纲。",
    "label_essay_target": "文书针对的目标大学/专业",
    "label_essay_activity": "核心活动、竞赛或项目细节",
    "btn_generate_essay": "生成文书叙事指南大纲",
    "btn_essay_example1": "📝 示例 1: CS项目 (U-Mich)",
    "btn_essay_example2": "⚙️ 示例 2: 机械工程机器人 (Georgia Tech)",
    "essay_outline_title": "AI文书大纲推荐",
    "essay_outline_desc": "逻辑连贯的3段式学术陈述（PS）写作蓝图，指导您如何将项目经历转化为亮眼的文书叙事。",
    "essay_placeholder": "在左侧输入您的活动经历，点击生成按钮，系统将在此处展示您的定制大纲建议。",
    "feedback_eyebrow": "提出反馈意见",
    "feedback_title": "参与 TransferChek 改进",
    "feedback_desc": "您的意见是完全匿名的，且仅保存在本地浏览器中。平台管理员会定期查看并用于修正遗漏的课程数据。",
    "label_feedback_input": "反馈内容",
    "btn_submit_feedback": "提交反馈",
    "btn_inbox": "收到的反馈收件箱",
    "inbox_title": "匿名反馈收件箱（本地）",
    "btn_clear": "清空",
    "feedback_empty_alert": "请在提交前输入反馈内容。",
    "feedback_sending": "正在发送反馈...",
    "feedback_success": "谢谢！您的反馈已成功发送。",
    "feedback_failed": "反馈提交失败：",
    "auth_note": "演示用账户数据保存在本浏览器的本地缓存中。未来正式版将使用安全的云端数据库存储。",
    "label_email": "电子邮箱地址",
    "label_password": "账户密码",
    "pricing_modal_title": "订阅计划方案",
    "pricing_modal_upgrade": "升级您的会员方案",
    "pricing_modal_subtitle": "选择最适合您的方案，优化转学路径。支持随时更换方案。",
    "pricing_free_beta_notice": "目前免费提供服务。请选择您想要的订阅方案并立即开始使用。",
    "active_plan_label": "您当前的方案：",
    "tier_free_name": "免费方案 (Free Plan)",
    "tier_free_price": "0 KRW",
    "tier_free_desc": "基础功能体验",
    "tier_free_feature1": "转学可行性分析仪 (服务1) 可评估最多5个目标专业",
    "tier_free_feature2": "限制访问先修课程搜索引擎、最优选课路线规划以及AI转学文书策略家所有功能",
    "tier_pro_name": "Pro 方案 (Pro Plan)",
    "tier_pro_price": "9,900 KRW/月",
    "tier_pro_desc": "无限制先修资格诊断",
    "tier_pro_feature1": "转学可行性分析仪 (服务1) 可无限次进行目标专业评估",
    "tier_pro_feature2": "限制访问先修课程搜索引擎、最优选课路线规划以及AI转学文书策略家所有功能",
    "tier_premium_name": "Premium 方案 (Premium Plan)",
    "tier_premium_price": "29,900 KRW/月",
    "tier_premium_desc": "转学规划全包套件 (AI 文书仅限指定1个学校/专业/问题，1个月内无限制修改)",
    "tier_premium_feature1": "资格评估、先修课程搜索引擎、最优选课路线规划服务无限制使用",
    "tier_premium_feature2": "AI 转学文书: 仅限指定一个学校、专业和问题一个月内无限制修改使用",
    "tier_essay_pass_name": "AI 单校/专业/文书无限制包",
    "tier_essay_pass_price": "9,900 KRW",
    "tier_essay_pass_desc": "单项追加包 (1个学校/专业/问题 1个月内无限制使用)",
    "tier_essay_pass_feature1": "针对选定的学校、专业及文书问题，在1个月内提供无限制修改及设计导图服务",
    "tier_essay_pass_feature2": "购买后需在3个月（90天）内激活，激活后1个月内有效",
    "btn_choose_plan": "立即订阅",
    "btn_free_action": "选择免费",
    "btn_pro_action": "订阅 Pro 方案",
    "btn_premium_action": "订阅 Premium 方案",
    "btn_essay_pass_action": "购买目标包",
    "essay_credits_remaining": "可用包数量:",
    "btn_buy_credits": "购买单校/专业/文书包 (9,900 KRW)",
    "essay_out_of_credits": "没有已激活的文书包或待激活额度。请购买单校/专业/文书包或订阅 Premium 方案。",
    "alert_essay_pass_purchased": "成功购买 AI 单校/专业/文书包！已为您添加 1 个待激活的文书包。您可以在下方指定目标学校、专业和文书问题进行激活。",
    "display_fallback": "官方未标明",
    "display_fallback_none": "无",
    "display_credits_unspecified": "学分要求未标明",
    "display_gpa_unspecified": "最低 GPA 未标明",
    "uc_notice_label": "建议核对 UC 官方数据",
    "uc_notice_desc": "UC 系列要求可能有所不同。建议使用 ASSIST.org 和各校区官方转学指南进行核实。",
    "roadmap_multi_notice_title": "正在合并规划 {count} 个目标专业",
    "roadmap_multi_notice_desc": "已合并的大学先修课程: ",
    "roadmap_multi_warning": "选择的目标学校越多，所需的修课时间可能越长。",
    "roadmap_targets_label": "路线图规划目标大学（最多 7 个）",
    "req_finder_placeholder_title": "搜索目标大学及专业",
    "req_finder_placeholder_desc": "在上方搜索栏输入大学或专业名称，即可在此处查看详细的先修课程、官方说明及转学标准。",
    "badge_verified_tag": "✓ 官方简章基准",
    "badge_high_risk_tag": "⚠ High Risk (高风险)",
    "badge_needs_review_tag": "⚠ Needs Review (待核对)",
    "review_opt_desc": "验证多选一条件中的课程组合",
    "review_enroll_desc": "验证入学后/注册前的完成时限",
    "review_dept_desc": "检查院系特定的额外先修要求",
    "review_credit_desc": "检查最低可转学分或转移学分上限",
    "review_course_desc": "先修课程条件",
    "review_extra_desc": "其他待验证项",
    "choice_note": "多选一先修课说明",
    "review_reason": "需人工核对",
    "choice_label_pref": "多选一:",
    "review_count_alert": "请注意: 有 {count} 个项目需要您去官网核对",
    "search_empty": "未找到搜索结果",
    "search_select_school_first": "请先选择学校",
    "status_pass_review": "建议申请 • 需核对原件",
    "status_pass": "建议申请",
    "status_fail": "高风险",
    "eng_review_reason": "需去官网验证豁免条件",
    "rec_course_detail": "推荐先修",
    "rec_choice_label": "推荐多选一:",
    "rec_choice_detail": "推荐多选一条件 • 非必须",
    "rec_choice_raw_detail": "推荐多选一原始说明 • 非必须",
    "rec_other_detail": "推荐或入学后完成 • 非必须",
    "tag_pass": "已匹配",
    "tag_fail": "未匹配",
    "roadmap_extra_summary": "其他必修要求 {count} 项",
    "roadmap_required_summary": "要求 {count} 项",
    "roadmap_extra_note": "以下项计入评估，不仅仅是推荐建议。",
    "roadmap_recommended_summary": "推荐要求 {count} 项 • 非必须",
    "roadmap_review_summary": "需人工核实项 {count} 个",
    "no_targets_selected": "您尚未选择任何目标大学/专业。",
    "no_recs_found": "当前无推荐。请多勾选几门已修课程或审查未满足项。",
    "no_mapped_items": "无匹配课程",
    "mapped_items_desc": "未列出其他先修要求",
    "no_review_items": "无待审核项",
    "review_items_desc": "自动映射及分类已完成",
    "no_extra_info": "无额外信息",
    "extra_info_desc": "完全基于已明示的最低门槛进行判定",
    "opt_condition_desc": "多选一类型的课程要求",
    "no_opt_conditions": "无多选一条件",
    "opt_conditions_desc": "均为直接必修科目",
    "no_enroll_conditions": "无入学后完成的特殊时限要求",
    "enroll_conditions_desc": "均为申请前需完成的科目",
    "note_fallback": "暂无核对备注",
    "section_required_title": "直接必修课程",
    "section_recommended_title": "推荐先修课程（非必须）",
    "section_choices_title": "多选一先修课条件",
    "section_enroll_title": "入学/注册时限要求",
    "section_review_title": "需人工验证项",
    "section_extra_title": "额外信息参考",
    "roadmap_tag_required": "必修科目",
    "roadmap_tag_recommended": "推荐科目",
    "roadmap_tag_choice": "必修多选一",
    "roadmap_eng_first_term": "固定在第一学期",
    "roadmap_eng_second_term": "固定在第二学期",
    "buffer_term_title": "缓冲学期 (Buffer Term)",
    "buffer_term_desc": "检查申请材料、推荐信以及文书润色",
    "essay_alert_no_activity": "请填写您的核心 activity 经历！",
    "essay_loading_text": "AI 正在结合目标先修课定制大纲，请稍候...",
    "essay_generating_label": "生成中...",
    "essay_generation_failed": "大纲生成失败",
    "essay_error_occurred": "发生错误:",
    "essay_error_desc": "可能因为超出 API 配额限制或网络波动。请稍后重试。",
    "essay_generate_btn_label": "生成文书叙事指南大纲",
    "req_search_placeholder": "输入大学名称或专业...",
    "essay_activity_placeholder": "例如：担任社区大学机器人社团社长，用 Java 设计数据库项目，辅导微积分学生等……",
    "search_school_placeholder": "搜索学校",
    "search_major_placeholder": "搜索专业",
    "gpa_ref": "GPA 参考: ",
    "credits_ref": "学分参考: ",
    "label_school": "学校",
    "label_major": "专业",
    "alert_subscribed": "成功订阅了 {plan}！",
    "btn_clear_courses": "全部清除",
    "auth_consent_text": "我同意服务条款和隐私政策。",
    "auth_consent_link": "阅读详情",
    "auth_consent_error": "您必须同意服务条款和隐私政策才能注册。",
    "auth_email_exists": "使用此电子邮箱的账户已存在。",
    "auth_invalid_credentials": "电子邮箱或密码不匹配。",
    "label_essay_question": "文书题目 (Prompt)",
    "essay_question_placeholder": "请在此处粘贴大学官方文书题目...",
    "label_essay_limit": "字数或篇幅限制",
    "essay_limit_placeholder": "例如: 350字, 500字, 1页等...",
    "essay_alert_no_question": "请填写文书题目 (Prompt)！",
    "essay_style_guide_title": "该大学转学文书评审标准与风格指南",
    "tag_dos": "建议阐述方向 (DOs)",
    "tag_donts": "应避免的误区 (DON'Ts)",
    "tag_example": "优秀英文写作范例",
    "essay_info_banner_title": "AI 知识库状态与法律特别说明",
    "essay_info_banner_body": "本 AI 引擎已针对 58 所顶尖大学的转学录取标准 and 文书风格指南（如 UC PIQ、佐治亚理工的贡献/协作导向、UIUC 的研究拟合度等）进行了深度校准，并融入了 120,000+ 篇成功转学文书的模式分析数据。系统将分析您的项目活动并为您生成最佳篇章叙事蓝图。为了遵守大学防剽窃及学术诚信政策，本服务为纯教育辅助工具，不提供文书代写服务，亦不担保最终录取结果。",
    "essay_mode_direct": "直接输入",
    "essay_mode_interview": "AI 互动面试指南",
    "interview_start_desc": "针对目标院校及专业回答 5 个定制问题，全面挖掘您的核心项目经历和学术特质。",
    "btn_start_interview": "开始 AI 互动面试",
    "btn_prev": "上一步",
    "btn_next": "下一步",
    "btn_complete": "完成面试",
    "interview_answer_placeholder": "在此输入您的回答...",
    "interview_complete_msg": "AI 互动面试已完成！您的回答已自动整合至活动经历池中。请点击下方的 '生成文书叙事指南大纲' 确认定制的文书规划。",
    "stat_universities": "覆盖院校",
    "stat_programs": "专业覆盖度 (AI 实时)",
    "stat_programs_small": "个专业",
    "stat_essays": "已分析的优秀转学文书案例",
    "holistic_eval_title": "综合评估 (Holistic Review) 大学指南",
    "holistic_eval_desc": "该大学没有官方指定的强制性先修课程要求，将根据申请者整体的学术成就和潜力进行综合评估。没有先修课程清单是正常现象，并非数据错误。建议申请者在原学校修读尽可能高难度的数学、物理及专业基础课程，以证明学术挑战精神并提升录取概率。",
    "btn_profile": "我的个人资料",
    "profile_modal_title": "账户控制面板",
    "profile_modal_header": "我的个人资料",
    "profile_modal_subtitle": "管理您的账户设置、订阅级别和安全首选项。",
    "profile_email_label": "电子邮箱",
    "profile_plan_label": "订阅级别",
    "profile_essay_credits_label": "EssayAI 剩余次数",
    "btn_upgrade_short": "升级",
    "btn_topup_short": "购买额度",
    "profile_security_header": "安全设置",
    "label_current_password": "当前密码",
    "label_new_password": "新密码",
    "btn_update_password": "更新密码",
    "locked_card_title": "仅限 Pro / Premium 方案",
    "locked_card_desc": "第 6 个目标及以上需要订阅 Pro 或更高方案以进行资格评估。",
    "btn_upgrade_now": "立即升级",
    "password_change_success": "密码更新成功！",
    "password_change_invalid": "当前密码输入不正确。",
    "statcompass_limit_help": "免费计划限制累计分析最多5所大学。升级到 Pro/Premium 计划以解锁无限分析。",
    "btn_toggle_recommended": "切换推荐课程",
    "label_birthdate": "出生日期",
    "auth_underage_error": "未满 14 周岁（美国/其他国家未满 13 周岁）无法注册。",
    "auth_birthdate_required": "请输入您的出生日期。",
    "payment_consent_minor_text": "[必须] 我确认我已达到法定年龄或已获得监护人（父母）的同意。 (未成年人付款需父母同意)",
    "payment_consent_disclaimer_text": "[必须] 我同意 TransferChek 是参考指南，不保证录取。我理解我必须在大学官方网站上核实最终要求，所有大学名称均为其各自商标所有者的财产。",
    "payment_consent_error": "您必须同意所有法律条款（未成年人确认、免责声明）才能继续付款。",
    "payment_sdk_error": "支付模块正在加载中，请稍后再试。",
    "payment_failed": "支付失败: {error}",
    "search_missing_prompt": "找不到您的目标大学或专业？",
    "search_missing_link": "请求 AI 实时生成（100% 专业覆盖）",
    "gen_modal_subtitle": "100% 专业覆盖度",
    "gen_modal_title": "AI 实时先修要求生成器",
    "gen_modal_desc": "输入任何美国大学和专业（例如 Stanford University - Economics），即可使用 Gemini 2.0 立即检索/生成转学先修要求、最低 GPA 和课程映射。",
    "label_gen_school": "大学官方英文名称",
    "label_gen_major": "专业 / 项目英文名称",
    "gen_loading_text": "AI 正在检索和提取转学要求...（需要大约 3-5 秒）",
    "btn_gen_req": "生成先修课程档案",
    "drawer_nav1_title": "01. StatCompass",
    "drawer_nav1_desc": "转学可行性分析",
    "drawer_nav2_title": "02. ReqRadar",
    "drawer_nav2_desc": "先修课程搜索引擎",
    "drawer_nav3_title": "03. Pathfinder",
    "drawer_nav3_desc": "最优选课路线规划",
    "drawer_nav4_title": "04. EssayAI",
    "drawer_nav4_desc": "转学文书策略家",
    "drawer_nav_guide_title": "5步转学策略指南",
    "drawer_nav_guide_desc": "系统化的顶尖大学转学准备路线图",
    "drawer_nav_pricing_title": "会员订阅计划",
    "drawer_nav_pricing_desc": "查看免费和高级计划选项",
    "cases_showcase_eyebrow": "录取案例样本数据库",
    "cases_showcase_heading": "分析从公开数据集编译的录取案例样本数据库",
    "cases_showcase_desc": "探索从公开大学数据集和社区报告中编译的匿名历史转学档案。此参考数据库代表外部录取统计数据，以帮助您基准测试您的转学候选资格，而不是此服务的直接客户结果。",
    "case_showcase_c1_title": "STEM: CS转学",
    "case_showcase_status": "录取样本",
    "label_case_gpa": "GPA:",
    "label_case_origin": "来源CC:",
    "case_showcase_c1_origin": "迪安萨学院",
    "label_case_extra": "关键课外活动:",
    "case_showcase_c1_extra": "构建了 React 日程安排应用程序，辅导微积分，加入了机器人俱乐部。",
    "case_showcase_c2_title": "商科: 金融",
    "case_showcase_status_haas": "录取样本 (哈斯)",
    "case_showcase_c2_origin": "帝亚布罗谷学院",
    "case_showcase_c2_extra": "暑期审计实习，社区大学金融与投资协会主席。",
    "case_showcase_c3_title": "人文: 心理",
    "case_showcase_status_ucla": "录取样本 (UCLA)",
    "case_showcase_c3_origin": "圣莫尼卡学院",
    "case_showcase_c3_extra": "危机短信专线辅导员，认知实验室志愿研究助理。",
    "cases_showcase_cta_note": "探索所有 58 所目标大学并解锁完整的成功档案。*注意：样本数据是根据公开统计数据编译的参考示例，不是该服务的直接客户录取结果。",
    "btn_cases_showcase": "在 ReqRadar 中探索数据库"
  }
};

const courseCatalog = window.courseCatalog || [];
const FEEDBACK_STORAGE_KEY = "transferCompassFeedback";
const AUTH_STORAGE_KEY = "transferCompassAuth";
const PROFILE_STORAGE_KEY = "transferCompassProfile";
const PORTONE_TEST_MODE = false; // Set to false for live production payments

// Supabase Configuration
const SUPABASE_URL = "https://dqgyxiqkqrykrupfzipl.supabase.co";
const SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRxZ3l4aXFrcXJ5a3J1cGZ6aXBsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODEzNDE5MTcsImV4cCI6MjA5NjkxNzkxN30.nTFg5gBQnJGQ1AsSKdVxJiH9JYN7gAyd9RD1hN-SIAI";
let sbClient = null;
try {
  if (window.supabase && typeof window.supabase.createClient === 'function') {
    sbClient = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
  }
} catch (e) {
  console.warn("Supabase client initialization failed:", e);
}

const STRATEGY_DATA = {
  en: {
    stem: [
      {
        title: "Target Research & Engineering Majors",
        body: "Define your target engineering program (Computer Science, Mechanical, Electrical, etc.). Check cumulative prerequisite GPA and credit thresholds early on.",
        tip: "💡 Platform Tip: Use [01. Eligibility Diagnostics] to input your current GPA and credits to get a real-time assessment of your target schools."
      },
      {
        title: "Prerequisite Mapping & Sequential Planning",
        body: "Transfer engineering programs are highly prerequisite-dependent. Complete core math (Calculus I/II/III, Linear Algebra), general physics (Physics), chemistry, and programming fundamentals early. Missing key prerequisites can lead to automatic rejection.",
        tip: "💡 Platform Tip: Use [02. Prerequisite Finder] to search target universities and instantly load required minimum grades and lab rules."
      },
      {
        title: "Strategic Community College (CC) Selection",
        body: "Select CCs with strong articulation agreements (like California CCs utilizing ASSIST.org) to guarantee maximum engineering course credit transferability and state-wide priority selection.",
        tip: "💡 Platform Tip: Select target schools and inspect 'Residency & Institution' policies to optimize credit evaluations."
      },
      {
        title: "Academic Scheduling & Term Sequencing",
        body: "Plan your coursework sequence logically (e.g. Calculus 1 -> Calculus 2 -> Physics 1). Ensure you complete critical prerequisites before applying, and maintain a high GPA.",
        tip: "💡 Platform Tip: Use [03. Roadmap Builder] to automatically build term-by-term lecture and lab schedules matching your target timelines."
      },
      {
        title: "Qualitative Assets: Engineering Projects & AI Essay Prep",
        body: "Admissions committees evaluate qualitative engineering assets holistically. Build coding projects, lead robotics clubs, gain technical internships, and prepare essays connecting your achievements to target university lab goals.",
        tip: "💡 Platform Tip: Use [04. EssayAI] to draft and polish admissions essay outlines and structure mock interview answers."
      }
    ],
    business: [
      {
        title: "Target Business School Research",
        body: "Identify top business schools (e.g., Foster, Olin, Ross, Haas). Note strict application deadlines, specific minimum GPAs, and average admitted stats.",
        tip: "💡 Platform Tip: Use [01. Eligibility Diagnostics] to evaluate your cumulative credentials against top business school expectations."
      },
      {
        title: "Core Business & Economics Prerequisites",
        body: "Complete core prerequisites: Calculus, Business Statistics, Microeconomics, Macroeconomics, Financial Accounting, and Managerial Accounting. High grades (A/B) in these courses are essential.",
        tip: "💡 Platform Tip: Use [02. Prerequisite Finder] to check accounting and economics course articulation requirements for your target schools."
      },
      {
        title: "CC Selection & Business Articulations",
        body: "Choose a community college with established business transfer pathways. Check if your CC credits are accepted for business-specific core requirements, not just general education.",
        tip: "💡 Platform Tip: Verify whether your CC offers direct articulation agreements or guaranteed admission pathways for business."
      },
      {
        title: "GPA Preservation & Application Timing",
        body: "Business transfers are extremely competitive. Structure your terms to complete pre-application courses early, allowing you to showcase strong grades in accounting and economics.",
        tip: "💡 Platform Tip: Use [03. Roadmap Builder] to outline your economics and accounting course sequences without overloading any single semester."
      },
      {
        title: "Leadership, Internships & Career Focus",
        body: "Highlight leadership roles, business clubs, internships, and volunteer work. Your admissions essays must clearly detail your career vision, leadership capacity, and professional goals.",
        tip: "💡 Platform Tip: Use [04. EssayAI] to craft compelling essays highlighting your startup projects, case competitions, or finance internship experiences."
      }
    ],
    humanities: [
      {
        title: "Identify Majors & Liberal Arts Targets",
        body: "Choose your major track (Psychology, Economics BA, History, Sociology, etc.). Note specific foreign language requirements and general education (GE) frameworks.",
        tip: "💡 Platform Tip: Use [01. Eligibility Diagnostics] to check how your current credits satisfy target liberal arts guidelines."
      },
      {
        title: "General Education & Core Requirements",
        body: "Focus on completing writing-intensive English Composition 1 & 2, introductory social sciences (e.g. Psychology, Sociology), humanities (e.g. History, Philosophy), and public speaking.",
        tip: "💡 Platform Tip: Use [02. Prerequisite Finder] to inspect general education requirements and foreign language exemptions for your target major."
      },
      {
        title: "General Education Pathways (IGETC, etc.)",
        body: "Many state systems prioritize students who complete a structured GE package (like IGETC for UC). Voluminous credit transfer is easier when adhering to these structured pathways.",
        tip: "💡 Platform Tip: Choose courses that satisfy multiple breadth requirements (like social science + international focus) to save credits."
      },
      {
        title: "Reading/Writing Volume & GPA Management",
        body: "Balance reading-heavy and writing-heavy courses across semesters. Maintain a high GPA by spacing out intensive term papers and exam-heavy coursework.",
        tip: "💡 Platform Tip: Use [03. Roadmap Builder] to schedule your writing and humanities courses evenly, leaving ample preparation time."
      },
      {
        title: "Critical Writing, Research & SOP Prep",
        body: "Demonstrate strong analytical writing, research assistant experience, or community service. Your Statement of Purpose (SOP) must explain your intellectual curiosity and fit for the major.",
        tip: "💡 Platform Tip: Use [04. EssayAI] to refine and structure your statement of purpose paragraphs, ensuring a professional and persuasive academic tone."
      }
    ]
  },
  ko: {
    stem: [
      {
        title: "목표 대학 및 공학 전공 탐색 (Target Research)",
        body: "자신이 편입하고자 하는 대학교의 공과대학 및 학과(컴퓨터공학, 기계공학, 전기공학 등)를 명확히 정의합니다. 각 대학의 입학처 요강과 정량적 합격 지표(GPA, 최저 학점)를 먼저 검토해야 합니다.",
        tip: "💡 플랫폼 활용 팁: [01. 지원 가능 판단] 기능에서 현재의 GPA와 이수 학점을 입력하여 각 타겟 대학교의 안정/소신 지원 가능 여부를 실시간으로 진단해 보십시오."
      },
      {
        title: "선수과목 및 요구 요건 완벽 파악 (Prerequisite Mapping)",
        body: "미국 공대 편입 심사에서 선수과목(Prerequisites) 이수 여부는 절대적입니다. 핵심 수학(Calculus I/II/III, Linear Algebra), 일반 물리(Physics), 화학(Chemistry), 프로그래밍 필수 과목 중 하나라도 누락되면 서류 심사에서 자동으로 즉시 탈락 처리됩니다.",
        tip: "💡 플랫폼 활용 팁: [02. 편입 요건 검색] 기능으로 타겟 학교/학과를 조회하여 공식 입학처 요강과 100% 동일한 선수과목 목록 및 최저 성적 조건(GPA Threshold)을 간편하게 조회할 수 있습니다."
      },
      {
        title: "전략적 커뮤니티 칼리지(CC) 또는 연계 대학 선택 (State-wise CC Selection)",
        body: "미국 주립대들은 본인이 속한 주의 커뮤니티 칼리지(CC) 출신 편입생에게 전형 상 가산점과 우선선발권(예: UC 계열의 TAG 협약 등)을 보장합니다. 목표 대학교와 동일한 주에 위치한 CC 혹은 주립대에서 편입을 개시하는 것이 합격 확률 및 학점 인정(Credit Transfer) 측면에서 압도적으로 유리합니다.",
        tip: "💡 플랫폼 활용 팁: 저희 데이터베이스가 제공하는 캘리포니아, 텍사스, 미시간 등 주(State)별 매핑 분석을 참고하여, 목표 대학교로의 편입 성공률을 높여주는 우수 연계 CC 리스트와 필수 조건을 파악하십시오."
      },
      {
        title: "체계적인 수강계획 수립 및 철저한 GPA 관리 (Academic Scheduling)",
        body: "편입 요건을 충족하는 과목들을 우선적으로 이수하기 위해 학기별 수강 순서를 세밀하게 기획해야 합니다. 선이수 필수 조건(Pre-requisites)이나 개설 학기 제약이 있으므로, 학기 설계에 작은 오류가 있어도 편입 시기가 6개월에서 1년 이상 미뤄질 수 있습니다.",
        tip: "💡 플랫폼 활용 팁: [03. 수강 로드맵 빌더]를 통해 복잡하게 꼬이기 쉬운 기초 수학, 물리, 전공 탐색 과목들을 최적의 논리적 순서로 구성해 보십시오."
      },
      {
        title: "정성적 스펙 철저 준비 - 에세이 & 교외 활동 (Qualitative Assets)",
        body: "상위권 대학 공대 편입 합격을 결정짓는 마지막 열쇠는 에세이(Personal Statement / SOP)와 모의 면접입니다. 본인의 학업 성과(GPA)와 더불어, 교내외 활동, 프로젝트 성과(코딩 프로젝트, 로봇 공학 등)를 전공 관심사와 결합하여 설득력 있는 스토리로 전달해야 합니다.",
        tip: "💡 플랫폼 활용 팁: [04. AI 에세이 전략가]와 [AI 인터뷰 질문 생성기]를 활용해 자신의 전공 프로젝트 경험과 강점을 각 대학이 요구하는 인재상에 딱 맞춘 강력한 에세이 구조와 모의 답변으로 설계해 보십시오."
      }
    ],
    business: [
      {
        title: "목표 경영대학 및 입학 기준 조사 (Target Research)",
        body: "자신이 편입하고자 하는 목표 경영대학(예: Foster, Olin, Ross, Haas 등)을 설정합니다. 경영대 편입은 입학 정원이 제한되어 있으며, 별도의 에세이 제출 및 인터뷰 조건, 그리고 매우 높은 GPA 컷오프를 요구하는 경우가 많습니다.",
        tip: "💡 플랫폼 활용 팁: [01. 지원 가능 판단] 기능에서 현재 GPA와 취득 학점을 입력하여 각 타겟 경영대학의 합격선 및 지원 가능 여부를 확인해 보십시오."
      },
      {
        title: "경영/경제 필수 선수과목 최우선 이수 (Prerequisite Mapping)",
        body: "경영학 편입을 위해서는 미적분학, 비즈니스 통계학, 미시경제학, 거시경제학, 재무회계 및 관리회계 등의 과목을 반드시 우수한 성적(A/B)으로 이수해야 합니다. 필수 상경 선수과목이 부족하면 심사에서 크게 불리합니다.",
        tip: "💡 플랫폼 활용 팁: [02. 편입 요건 검색] 기능에서 목표 경영학과를 선택하여 공식 경영대 가이드라인과 연동된 선수과목 리스트 및 학점 요구사항을 검색해 보십시오."
      },
      {
        title: "주별 경영대 연계 CC 및 학점 매핑 (CC Selection)",
        body: "경영대학들은 타 전공 대비 까다로운 비즈니스 핵심 과목의 학점 인정(Credit Evaluation)을 고수합니다. 목표 대학과 연계가 우수한 CC를 선택하고, 이수한 전공 수업이 일반 교양이 아닌 전공 필수 학점으로 확실히 인정받는지 검증해야 합니다.",
        tip: "💡 플랫폼 활용 팁: 각 대학교의 편입 인정 기준 및 주별 연계 가이드라인을 참조하여 불필요하게 버려지는 학점을 최소화하십시오."
      },
      {
        title: "경쟁력 있는 GPA 유지 및 수강 학기 최적화 (Academic Scheduling)",
        body: "상위권 경영대 편입은 경쟁이 매우 치열하므로, GPA 관리가 핵심입니다. 비교적 난이도가 높은 회계 및 통계 수업을 적절히 분산 배치하고, 편입 지원 전 학기까지 대부분의 상경 필수 과목 이수를 완료하여 우수한 학업 성과를 증명해야 합니다.",
        tip: "💡 플랫폼 활용 팁: [03. 수강 로드맵 빌더]를 통해 경영대 편입에 필수적인 경제, 회계 과목들의 일정을 최적의 논리적 순서로 구성해 보십시오."
      },
      {
        title: "리더십, 인턴십 및 에세이 스토리 설계 (Qualitative Assets)",
        body: "성공적인 경영대 편입에는 교내 동아리 임원 활동, 비즈니스 공모전, 인턴십 및 창업 경험 등 본인의 리더십과 정성적 성과가 크게 반영됩니다. 에세이를 통해 장래의 커리어 비전과 비즈니스 마인드를 효과적으로 어필하십시오.",
        tip: "💡 플랫폼 활용 팁: [04. AI 에세이 전략가]를 통해 경영대 고유의 리더십 및 목표 지향적 에세이 질문에 맞춘 강력한 영문 아웃라인과 모의 면접 답변을 설계해 보십시오."
      }
    ],
    humanities: [
      {
        title: "목표 인문/사회계열 학과 및 요건 검토 (Target Research)",
        body: "심리학, 경제학(BA), 역사학, 사회학, 언론정보학 등 목표로 하는 인문/사회과학 전공의 지원 요건을 확인합니다. 해당 전공들은 주립대와 사립대에 따라 교양 필수 범위와 외국어 이수 요건이 크게 상이합니다.",
        tip: "💡 플랫폼 활용 팁: [01. 지원 가능 판단] 시뮬레이션에서 현재 성적을 토대로 인문/사회계열 편입의 필수 요건 통과율을 실시간으로 확인해 보십시오."
      },
      {
        title: "교양(General Education) 및 쓰기 과목 이수 (Prerequisite Mapping)",
        body: "인문사회계열 편입생은 광범위한 독서 및 작문 능력을 입증해야 합니다. 영어 작문 1 & 2(English Composition)와 심리학 개론, 사회학 개론, 역사학 및 철학 등 기초 교양 과목(GE Breadth)을 탄탄하게 이수하는 것이 첫걸음입니다.",
        tip: "💡 플랫폼 활용 팁: [02. 편입 요건 검색]을 통해 지원하려는 인문학과에서 요구하는 영어 작문 레벨 및 필수 제2외국어 이수 면제 기준을 파악하십시오."
      },
      {
        title: "체계적인 교양 패스웨이(GE Pathway) 준수 (GE Pathway)",
        body: "많은 주립대(예: UC 계열의 IGETC)는 인문사회계열 지원 시 전체 교양 이수 패키지를 완료한 학생을 우선 선발합니다. 학기별 과목 선택 시, 해당 대학군이 규정하는 영역별(인문, 사회, 과학, 예술) 학점을 골고루 취득해야 합니다.",
        tip: "💡 플랫폼 활용 팁: 본인이 소속된 CC의 교양 과정이 목표 주립대의 전체 교양 패키지와 어떻게 매핑되는지 꼼꼼하게 검토하십시오."
      },
      {
        title: "독서/작문 과목 분산 배치 및 내신 관리 (Academic Scheduling)",
        body: "글쓰기와 독서 과제가 많은 인문사회 전공 특성상, 한 학기에 레포트 제출 과목이 너무 많이 겹치면 GPA 유지가 어려워집니다. 학기별로 과목 성격을 안배해 균형 있는 내신(GPA) 관리를 이어나가야 합니다.",
        tip: "💡 플랫폼 활용 팁: [03. 수강 로드맵 빌더]를 사용하여 영작문 및 인문학 강의들을 무리하지 않게 적절한 시퀀스로 고르게 배치해 보십시오."
      },
      {
        title: "학술 연구, 작문 포트폴리오 및 SOP 에세이 (Qualitative Assets)",
        body: "인문/사회과학 편입에서는 학업 목적 진술서(SOP)의 비중이 매우 높습니다. 수업 중 작성한 우수 에세이, 교수진과의 학술 연구 어시스턴트 활동, 또는 관련 봉사 경험을 학문적 관심사와 융합해 매끄러운 영문 에세이로 완성해 내야 합니다.",
        tip: "💡 플랫폼 활용 팁: [04. AI 에세이 전략가] 기능을 활용해 자신의 인문학적 탐구 성과와 전공 적합성을 매끄러운 학술적 톤앤매너로 서술해 보십시오."
      }
    ]
  }
};

const state = {
  gpa: 3.72,
  credits: 48,
  international: true,
  englishType: "TOEFL",
  englishScore: 98,
  englishWaiver: false,
  completedCourses: new Set(),
  selectedTargets: [],
  targetSlots: Array.from({ length: 10 }, () => ({ school: "", major: "" })),
  roadmapTargetSlots: Array.from({ length: 7 }, () => ({ school: "", major: "" })),
  selectedRoadmapTargets: [],
  plan: localStorage.getItem("transferCompassPlan") || "Free",
  language: localStorage.getItem("transferCompassLang") || "en",
  essayCredits: parseInt(localStorage.getItem("transferCompassEssayCredits") || (localStorage.getItem("transferCompassPlan") === "Premium" ? "1" : "0"), 10),
  pendingPasses: parseInt(localStorage.getItem("transferCompassPendingPasses") || "0", 10),
  unlockedSchools: (() => {
    try {
      const val = localStorage.getItem("transferCompassUnlockedSchools");
      return val ? JSON.parse(val) : [];
    } catch {
      return [];
    }
  })(),
  analyzedSchools: (() => {
    try {
      const val = localStorage.getItem("transferCompassAnalyzedSchools");
      return new Set(val ? JSON.parse(val) : []);
    } catch {
      return new Set();
    }
  })(),
  admissionYear: 2026,
  admissionTerm: "Fall",
  track: "stem",
  strategyTrack: "stem"
};

function saveAnalyzedSchoolsToLocalStorage() {
  localStorage.setItem("transferCompassAnalyzedSchools", JSON.stringify(Array.from(state.analyzedSchools)));
}

function saveProfileToLocalStorage() {
  const profileData = {
    gpa: state.gpa,
    credits: state.credits,
    international: state.international,
    englishType: state.englishType,
    englishScore: state.englishScore,
    englishWaiver: state.englishWaiver,
    completedCourses: Array.from(state.completedCourses),
    targetSlots: state.targetSlots,
    roadmapTargetSlots: state.roadmapTargetSlots,
    admissionYear: state.admissionYear,
    admissionTerm: state.admissionTerm,
    track: state.track
  };
  localStorage.setItem(PROFILE_STORAGE_KEY, JSON.stringify(profileData));
  saveProfileToSupabase();
}

function normalizeSlots(slots, limit) {
  if (!Array.isArray(slots)) return Array.from({ length: limit }, () => ({ school: "", major: "" }));
  const seen = new Set();
  const progs = allPrograms();
  let loaded = slots.map(slot => {
    let school = "";
    let major = "";
    if (typeof slot === "string") {
      const prog = progs.find(p => p.id === slot);
      if (prog) {
        school = prog.school.name;
        major = prog.name;
      }
    } else if (slot && typeof slot === "object") {
      school = slot.school || "";
      major = slot.major || "";
    }
    const key = `${(school || "").trim()}:${(major || "").trim()}`;
    if (key !== ":" && seen.has(key)) {
      return { school: "", major: "" };
    }
    if (key !== ":") seen.add(key);
    return { school, major };
  }).slice(0, limit);
  while (loaded.length < limit) {
    loaded.push({ school: "", major: "" });
  }
  return loaded;
}

function loadProfileFromLocalStorage() {
  try {
    const raw = localStorage.getItem(PROFILE_STORAGE_KEY);
    if (!raw) return;
    const profile = JSON.parse(raw);
    if (profile.gpa !== undefined) state.gpa = Number(profile.gpa);
    if (profile.credits !== undefined) state.credits = Number(profile.credits);
    if (profile.international !== undefined) state.international = Boolean(profile.international);
    if (profile.englishType !== undefined) state.englishType = profile.englishType;
    if (profile.englishScore !== undefined) state.englishScore = Number(profile.englishScore);
    if (profile.englishWaiver !== undefined) state.englishWaiver = Boolean(profile.englishWaiver);
    if (profile.admissionYear !== undefined) state.admissionYear = Number(profile.admissionYear);
    if (profile.admissionTerm !== undefined) state.admissionTerm = profile.admissionTerm;
    if (profile.track !== undefined) {
      state.track = profile.track;
      state.strategyTrack = profile.track;
    }
    if (Array.isArray(profile.completedCourses)) {
      state.completedCourses = new Set(profile.completedCourses);
    }
    if (Array.isArray(profile.targetSlots)) {
      state.targetSlots = normalizeSlots(profile.targetSlots, 10);
    }
    if (Array.isArray(profile.roadmapTargetSlots)) {
      state.roadmapTargetSlots = normalizeSlots(profile.roadmapTargetSlots, 7);
    }
  } catch (e) {
    console.error("Failed to load profile from localStorage:", e);
  }
}

let krwToUsdRate = 0.00074; // Fallback: 1 USD = 1350 KRW (1 / 1350 = 0.00074)

async function fetchExchangeRate() {
  try {
    const res = await fetch('https://open.er-api.com/v6/latest/KRW');
    if (res.ok) {
      const data = await res.json();
      if (data && data.rates && data.rates.USD) {
        krwToUsdRate = data.rates.USD;
        console.log("Fetched real-time KRW to USD exchange rate:", krwToUsdRate);
        switchLanguage(state.language || "ko");
      }
    }
  } catch (e) {
    console.warn("Failed to fetch real-time exchange rate, using fallback:", e);
  }
}

function t(key, fallback = "") {
  const lang = state.language || "en";
  let baseVal = "";
  if (TRANSLATIONS[lang] && TRANSLATIONS[lang][key]) {
    baseVal = TRANSLATIONS[lang][key];
  } else {
    baseVal = TRANSLATIONS["en"][key] || fallback;
  }

  // Intercept price keys and append USD conversion dynamically
  if (key === "tier_premium_price") {
    const usd = (29900 * krwToUsdRate).toFixed(2);
    if (lang === "ko") return `29,900원 / 월 (실시간 환율: $${usd} USD)`;
    if (lang === "zh") return `29,900 KRW/月 (约合 $${usd} USD)`;
    return `29,900 KRW/mo (approx. $${usd} USD)`;
  }
  if (key === "tier_pro_price") {
    const usd = (9900 * krwToUsdRate).toFixed(2);
    if (lang === "ko") return `월 9,900원 (실시간 환율: $${usd} USD)`;
    if (lang === "zh") return `9,900 KRW/月 (约合 $${usd} USD)`;
    return `9,900 KRW/mo (approx. $${usd} USD)`;
  }
  if (key === "tier_essay_pass_price") {
    const usd = (9900 * krwToUsdRate).toFixed(2);
    if (lang === "ko") return `9,900원 (실시간 환율: $${usd} USD)`;
    if (lang === "zh") return `9,900 KRW (约合 $${usd} USD)`;
    return `9,900 KRW (approx. $${usd} USD)`;
  }

  return baseVal;
}


function switchLanguage(lang) {
  state.language = lang;
  localStorage.setItem("transferCompassLang", lang);
  
  const langSelector = qs("#langSelector");
  if (langSelector) {
    langSelector.value = lang;
  }
  const drawerLangSelector = qs("#drawerLangSelector");
  if (drawerLangSelector) {
    drawerLangSelector.value = lang;
  }
  
  document.querySelectorAll("[data-i18n]").forEach((el) => {
    const key = el.getAttribute("data-i18n");
    const translation = t(key);
    if (!translation) return;

    if (el.tagName === "INPUT" || el.tagName === "TEXTAREA") {
      el.placeholder = translation;
    } else {
      el.innerHTML = translation;
    }
  });

  document.querySelectorAll("[data-i18n-placeholder]").forEach((el) => {
    const key = el.getAttribute("data-i18n-placeholder");
    const translation = t(key);
    if (translation) {
      el.placeholder = translation;
    }
  });

  const reqSchoolInput = qs("#reqSchoolInput");
  if (reqSchoolInput) {
    reqSchoolInput.placeholder = t("search_school_placeholder", "Search School");
  }
  const reqMajorInput = qs("#reqMajorInput");
  if (reqMajorInput) {
    const key = safeHasAttr(reqMajorInput, "disabled") ? "select_school_first_placeholder" : "select_major_placeholder";
    reqMajorInput.placeholder = t(key, key === "select_school_first_placeholder" ? "Select School First" : "Select Major");
  }
  const essaySchoolInput = qs("#essaySchoolInput");
  if (essaySchoolInput) {
    essaySchoolInput.placeholder = t("search_school_placeholder", "Search School");
  }
  const essayMajorInput = qs("#essayMajorInput");
  if (essayMajorInput) {
    const key = safeHasAttr(essayMajorInput, "disabled") ? "select_school_first_placeholder" : "select_major_placeholder";
    essayMajorInput.placeholder = t(key, key === "select_school_first_placeholder" ? "Select School First" : "Select Major");
  }
  const essayActivityInput = qs("#essayActivity");
  if (essayActivityInput) {
    essayActivityInput.placeholder = t("essay_activity_placeholder", "E.g., Led the community college robotics club...");
  }
  
  for (let i = 0; i < state.targetSlots.length; i++) {
    const sInput = qs(`#targetSchool${i}`);
    const mInput = qs(`#targetMajor${i}`);
    if (sInput) sInput.placeholder = t("search_school_placeholder", "Search School");
    if (mInput) mInput.placeholder = t("search_major_placeholder", "Search Major");
  }
  for (let i = 0; i < state.roadmapTargetSlots.length; i++) {
    const sInput = qs(`#roadmapTargetSchool${i}`);
    const mInput = qs(`#roadmapTargetMajor${i}`);
    if (sInput) sInput.placeholder = t("search_school_placeholder", "Search School");
    if (mInput) mInput.placeholder = t("search_major_placeholder", "Search Major");
  }

  renderSchoolCoverage();
  renderCourseGroups();
  renderTargetPicker();
  renderRoadmapTargetPicker();
  renderEligibilityResults();
  buildRoadmap();
  renderStrategyTimeline();
  renderAuthState();
  
  const reqSelVal = qs("#requirementSelect")?.value;
  if (reqSelVal) {
    renderRequirementDetail(reqSelVal);
  }
  window.dispatchEvent(new CustomEvent("languageChanged", { detail: lang }));
}
window.switchLanguage = switchLanguage;

const pageTitles = {
  eligibility: "Prerequisite Eligibility Analyzer",
  requirements: "Prerequisite Finder",
  roadmap: "Optimized Roadmap Builder",
  essay: "AI Essay Strategist"
};

function checkPlanAccess(tabName) {
  return true; // Allow entering all tabs to let Free users view the interface
}

function updateTabLockStates() {
  const isPremium = (state.plan || "Free") === "Premium";
  const reqLock = qs("#requirementsLockOverlay");
  const roadLock = qs("#roadmapLockOverlay");
  const essayLock = qs("#essayLockOverlay");
  
  if (reqLock) {
    reqLock.classList.toggle("hidden", isPremium);
  }
  if (roadLock) {
    roadLock.classList.toggle("hidden", isPremium);
  }
  
  if (essayLock) {
    const hasCredits = (state.essayCredits || 0) > 0;
    const pendingPasses = (state.pendingPasses || 0) > 0;
    const targetSchoolInput = qs("#essayTargetSchool");
    const targetSchool = targetSchoolInput ? targetSchoolInput.value.split("-")[0].trim() : "";
    const isSchoolUnlocked = targetSchool && Array.isArray(state.unlockedSchools) && state.unlockedSchools.includes(targetSchool);
    
    const essayUnlocked = isPremium || hasCredits || pendingPasses || isSchoolUnlocked;
    essayLock.classList.toggle("hidden", essayUnlocked);
  }
}

function openPricingModal() {
  const modal = qs("#pricingModal");
  if (modal) {
    updateActivePlanLabel();
    modal.classList.remove("hidden");
  }
}

function closePricingModal() {
  const modal = qs("#pricingModal");
  if (modal) {
    modal.classList.add("hidden");
  }
}

function updateActivePlanLabel() {
  const label = qs("#activePlanLabel");
  if (label) {
    label.textContent = `${state.plan} Plan`;
  }
  updateTabLockStates();
}

function updatePlanNoticeVisibility() {
  const notice = qs("#statCompassLimitNotice");
  if (notice) {
    if (state.plan === "Free") {
      notice.style.display = "flex";
    } else {
      notice.style.display = "none";
    }
  }
}

window.selectUserPlan = function(plan) {
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";

  if (plan !== "Free") {
    if (!currentUser) {
      alert(t("alert_login_required", "You must log in or register to select a paid plan."));
      closePricingModal();
      openAuthModal("login");
      return;
    }
    
    const consentMinor = qs("#paymentConsentMinor");
    const consentDisclaimer = qs("#paymentConsentDisclaimer");
    if ((consentMinor && !consentMinor.checked) || (consentDisclaimer && !consentDisclaimer.checked)) {
      alert(t("payment_consent_error", "You must agree to all legal consents (Minor check, Disclaimer) to proceed with payment."));
      return;
    }

    // Portone payment integration
    const IMP = window.IMP;
    if (!IMP) {
      alert(t("payment_sdk_error", "Payment module is loading. Please try again in a moment."));
      return;
    }

    const userProfile = authState.users[currentUser] || {};
    const buyerName = userProfile.name || currentUser.split("@")[0] || "Customer";
    const buyerPhone = userProfile.phone || "010-0000-0000";

    const isKo = (state.language || state.lang || "ko") === "ko";
    const pgChannel = isKo ? (PORTONE_TEST_MODE ? "html5_inicis.INIpayTest" : "html5_inicis.MOI6298964") : "paypal_v2";
    const payCurrency = isKo ? "KRW" : "USD";
    const payAmount = isKo ? (plan === "Premium" ? 29900 : 9900) : (plan === "Premium" ? 22 : 8); 
    const productName = isKo ? `${plan} Plan (${plan === "Premium" ? "프리미엄" : "프로"} 구독)` : `TransferChek ${plan} Plan (1 Month)`;

    IMP.request_pay({
      pg: pgChannel,
      pay_method: "card",
      merchant_uid: `order_sub_${plan}_${Date.now()}`,
      name: productName,
      amount: payAmount,
      currency: payCurrency,
      buyer_email: currentUser,
      buyer_name: buyerName,
      buyer_tel: buyerPhone
    }, async function(rsp) {
      if (rsp.success) {
        try {
          const verifyResponse = await fetch('/api/payments/verify', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              imp_uid: rsp.imp_uid,
              merchant_uid: rsp.merchant_uid,
              plan: plan,
              email: currentUser,
              userId: supabaseUserSession?.user?.id
            })
          });
          const verifyResult = await verifyResponse.json();
          if (verifyResult.success) {
            applyPlanUpgrade(plan, verifyResult.essayCredits);
          } else {
            alert(t("payment_failed", "Payment verification failed: {error}").replace("{error}", verifyResult.message || "Unknown error"));
          }
        } catch (e) {
          console.error("Payment verification request failed:", e);
          alert(t("payment_failed", "Payment verification failed."));
        }
      } else {
        alert(t("payment_failed", "Payment failed: {error}").replace("{error}", rsp.error_msg || "Unknown error"));
      }
    });
  } else {
    applyPlanUpgrade("Free");
  }
};

function applyPlanUpgrade(plan, forceCredits = null) {
  state.plan = plan;
  localStorage.setItem("transferCompassPlan", plan);
  
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";
  if (currentUser) {
    const prevPlan = authState.users[currentUser].plan || "Free";
    authState.users[currentUser].plan = plan;
    
    if (supabaseUserProfile) {
      supabaseUserProfile.plan = plan;
    }
    
    if (forceCredits !== null) {
      authState.users[currentUser].essayCredits = forceCredits;
      state.essayCredits = forceCredits;
      localStorage.setItem("transferCompassEssayCredits", forceCredits.toString());
      if (supabaseUserProfile) {
        supabaseUserProfile.essay_credits = forceCredits;
      }
    } else {
      if (plan === "Premium") {
        const now = new Date();
        const expiresAt = new Date(now.getTime() + 30 * 24 * 60 * 60 * 1000).toISOString(); // 30 days
        authState.users[currentUser].creditPacks = authState.users[currentUser].creditPacks || [];
        authState.users[currentUser].creditPacks = authState.users[currentUser].creditPacks.filter(p => p.type !== "premium");
        authState.users[currentUser].creditPacks.push({
          id: `premium_${Date.now()}`,
          type: "premium",
          count: 1,
          expiresAt: expiresAt
        });
      }
      const totalCredits = syncUserEssayCredits(currentUser, authState);
      state.essayCredits = totalCredits;
      localStorage.setItem("transferCompassEssayCredits", totalCredits.toString());
    }
    
    writeAuthState(authState);
    
    // Track plan change telemetry
    fetch('/api/track-subscription', {
      method: 'POST',
      cache: 'no-store',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ oldPlan: prevPlan, newPlan: plan })
    }).catch(e => console.warn("Failed to send plan change telemetry:", e));
  } else {
    if (plan === "Premium") {
      state.essayCredits = 1;
      localStorage.setItem("transferCompassEssayCredits", "1");
    } else {
      state.essayCredits = 0;
      localStorage.setItem("transferCompassEssayCredits", "0");
    }
  }
  
  updateActivePlanLabel();
  updatePlanNoticeVisibility();
  updateEssayCreditsUI();
  syncSelectedTargetsFromSlots();
  renderEligibilityResults();
  buildRoadmap();
  closePricingModal();
  
  const displayNames = {
    Free: t("tier_free_name", "Free Plan"),
    Pro: t("tier_pro_name", "Pro Plan"),
    Premium: t("tier_premium_name", "Premium Plan")
  };
  const displayName = displayNames[plan] || plan;
  
  const activeTabBtn = qs(".tab-button.active");
  if (activeTabBtn) {
    const tabName = activeTabBtn.getAttribute("data-tab");
    if (!checkPlanAccess(tabName)) {
      activateProductTab("eligibility");
    }
  }
  
  alert(t("alert_subscribed", "Successfully subscribed to the {plan}!").replace("{plan}", displayName));
}

const courseCategoryOrder = { Math: 0, Physics: 1, Chemistry: 2, Science: 3, English: 4, Computer: 5, Engineering: 6 };

const qs = (selector) => document.querySelector(selector);
const qsa = (selector) => [...document.querySelectorAll(selector)];

const database = window.transferDatabase;

function allPrograms() {
  return database.schools.flatMap((school) =>
    school.majors.map((major) => ({
      ...major,
      school,
      university: school
    }))
  );
}

function escapeHtml(value) {
  return String(value ?? "")
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#39;");
}

function normalizeText(value) {
  return String(value || "").toLowerCase().replace(/\s+/g, " ").trim();
}

function safeSetAttr(el, name, val) {
  if (!el) return;
  if (name === "disabled") el.disabled = (val === "true" || val === true);
  if (el.setAttribute) el.setAttribute(name, val);
}

function safeRemoveAttr(el, name) {
  if (!el) return;
  if (name === "disabled") el.disabled = false;
  if (el.removeAttribute) el.removeAttribute(name);
}

function safeHasAttr(el, name) {
  if (!el) return false;
  if (name === "disabled") return !!(el.disabled || (el.hasAttribute && el.hasAttribute("disabled")));
  return el.hasAttribute ? el.hasAttribute(name) : false;
}

function displayRequirement(value, fallback) {
  const defaultFallback = t("display_fallback");
  const fb = fallback !== undefined ? fallback : defaultFallback;
  if (value === null || value === undefined || value === "") return fb;
  return value;
}

function displayRawRequirement(rawValue, numericValue) {
  let val = rawValue || displayRequirement(numericValue);
  if (typeof val === 'string' && val.toLowerCase().includes("refer to official catalog wording below")) {
    return t("refer_to_catalog_wording", "Refer to official catalog wording below");
  }
  return val;
}

function isUnspecifiedText(value) {
  const text = normalizeText(value);
  return (
    !text ||
    text.includes(normalizeText(t("display_fallback"))) ||
    text.includes("공식 미게시") ||
    text.includes("공식 최소 없음") ||
    text.includes("없음") ||
    text.includes("not published") ||
    text.includes("no official") ||
    text.includes("holistic")
  );
}



function isGeneralGuidanceText(value) {
  const text = normalizeText(value);
  return (
    text.includes("competitive") ||
    text.includes("generally") ||
    text.includes("권장") ||
    text.includes("strongly encouraged") ||
    text.includes("recommended") ||
    text.includes("average")
  );
}

function additionalInfoForProgram(program) {
  const info = [];
  if (program.rawMinGpa && isGeneralGuidanceText(program.rawMinGpa)) info.push(`${t("gpa_ref")}${program.rawMinGpa}`);
  if (program.rawMinCredits && isGeneralGuidanceText(program.rawMinCredits)) info.push(`${t("credits_ref")}${program.rawMinCredits}`);
  return info;
}

function isUcProgram(program) {
  const schoolName = `${program.school?.name || ""} ${program.school?.shortName || ""}`;
  return /\bUC\b|University of California|Berkeley|UCLA|San Diego|Irvine|Davis|Santa Barbara|Santa Cruz|Riverside|Merced/i.test(schoolName);
}

function verificationNoticeForProgram(program) {
  if (!isUcProgram(program)) return null;
  return {
    label: t("uc_notice_label"),
    detail: t("uc_notice_desc")
  };
}

function getConfidenceBadgeHtml(program) {
  const conf = program.confidence || "needs_source_check";
  if (conf === "verified") {
    return `<span class="badge badge-verified">${escapeHtml(t("badge_verified_tag"))}</span>`;
  } else if (conf === "high_risk") {
    return `<span class="badge badge-high-risk">${escapeHtml(t("badge_high_risk_tag"))}</span>`;
  } else {
    return `<span class="badge badge-needs-review">${escapeHtml(t("badge_needs_review_tag"))}</span>`;
  }
}

function reviewFocus(item) {
  const raw = item.raw || String(item);
  const text = normalizeText(raw);
  if (text.includes("pick") || text.includes(" or ") || text.includes("choose") || text.includes("택1")) {
    return t("review_opt_desc");
  }
  if (text.includes("enroll") || text.includes("등록") || text.includes("later")) {
    return t("review_enroll_desc");
  }
  if (text.includes("department") || text.includes("학과별")) {
    return t("review_dept_desc");
  }
  if (text.includes("credit") || text.includes("학점")) {
    return t("review_credit_desc");
  }
  if (text.includes("course") || text.includes("과목")) {
    return t("review_course_desc");
  }
  return t("review_extra_desc");
}

function englishLabel(type) {
  return (
    {
      TOEFL: "TOEFL",
      TOEFL_2026: "TOEFL 2026 Jan Revised",
      IELTS: "IELTS",
      Duolingo: "Duolingo"
    }[type] || type
  );
}

function getEnglishRequirement(program) {
  return program.english?.[state.englishType] ?? null;
}

function updateEnglishScoreInputConstraints() {
  const input = qs("#englishScoreInput");
  if (!input) return;
  const settings = {
    TOEFL: { min: 0, max: 120, step: 1 },
    TOEFL_2026: { min: 0, max: 6, step: 0.1 },
    IELTS: { min: 0, max: 9, step: 0.5 },
    Duolingo: { min: 0, max: 160, step: 1 }
  }[state.englishType];
  input.min = settings.min;
  input.max = settings.max;
  input.step = settings.step;
  if (Number(input.value) > settings.max) {
    input.value = settings.max;
    state.englishScore = settings.max;
  }
}

function patternMatches(text, pattern) {
  if (!pattern) return false;
  if (pattern.startsWith('\\b')) {
    try {
      const regex = new RegExp(pattern, 'i');
      return regex.test(text);
    } catch(e) {
      return false;
    }
  }
  const haystack = normalizeText(text);
  const needle = normalizeText(pattern);
  const escapedNeedle = needle.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  
  let regexStr = escapedNeedle;
  if (/^[a-zA-Z0-9_]/.test(needle)) {
    regexStr = '\\b' + regexStr;
  }
  if (/[a-zA-Z0-9_]$/.test(needle)) {
    regexStr = regexStr + '\\b';
  }
  
  try {
    const regex = new RegExp(regexStr, 'i');
    return regex.test(haystack);
  } catch(e) {
    return haystack.includes(needle);
  }
}

function canonicalMatches(rawCourse) {
  const matches = courseCatalog.filter((course) => course.patterns.some((pattern) => patternMatches(rawCourse, pattern)));
  const byId = new Map(matches.map((course) => [course.id, course]));
  return [...byId.values()];
}

function orderCoursesForDisplay(courses) {
  return [...courses].sort((a, b) => {
    const categoryA = courseCategoryOrder[a.category] ?? 99;
    const categoryB = courseCategoryOrder[b.category] ?? 99;
    if (categoryA !== categoryB) return categoryA - categoryB;
    if (a.level !== b.level) return a.level - b.level;
    return a.name.localeCompare(b.name);
  });
}

function isIgnorableSourceLine(rawCourse) {
  const text = normalizeText(rawCourse);
  return (
    !text ||
    text.includes("no additional recommended") ||
    text.includes("official required courses") ||
    text.includes("required courses 미게시") ||
    text.includes("필수과목 목록 없음") ||
    text.includes("공식 학과별 필수과목") ||
    text.includes("holistic review") ||
    text.includes("minimum gpa") ||
    text.includes("min gpa") ||
    text.includes("source:") ||
    text.includes("verified") ||
    text.includes("common app") ||
    text.includes("fall only") ||
    text.includes("spring only") ||
    text.includes("지원 시 전공 미지정") ||
    text.includes("gpa 최소치 없음")
    || text.includes("출처")
    || text.includes("직접 확인")
    || text.includes("공식 미게시")
    || text.includes("원문")
    || text.includes("불필요")
    || text.includes("there are no required courses")
    || text.includes("not published")
    || text.includes("contact ")
    || text.includes("minimum credits")
    || text.includes("최소 크레딧")
    || text.includes("최대 이전")
    || text.includes("이전")
    || text.includes("최대 18")
    || text.includes("전공 선택")
    || text.includes("전공 지원 시 미지정")
    || text.includes("지원 시 전공 미지정")
    || text.includes("fall 입학")
    || text.includes("p/f 과목")
    || text.includes("major change")
    || text.includes("second choice")
    || text.includes("pre-major")
    || text.includes("all transfer students enter")
    || text.includes("공대 편입은 2단계")
    || text.includes("penn state 입학")
    || text.includes("입학 후 etm")
    || text.includes("university park")
    || text.includes("모두 c 이상")
    || text.includes("grades of")
    || text.includes("성적 요건")
    || text.includes("recommended coursework 섹션")
    || text.includes("학과별 구체 과목 다름")
    || text.includes("학과별 advanced course")
    || text.includes("공대 전체 공통 기대치")
    || text.includes("strong transfer applicants")
    || text.includes("coursework 권장")
    
    // Additional filters to keep UI clean
    || text.includes("재학 필수")
    || text.includes("gpa 최소치")
    || text.includes("학기 이하 이수")
    || text.includes("학기 초과 이수")
    || text.includes("편입 최소 gpa")
    || text.includes("편입은 2단계 구조")
    || text.includes("1단계:")
    || text.includes("2단계:")
    || text.includes("지원 전 이수해야")
    || text.includes("선이수 필수")
    || text.includes("권장과목 목록 없음")
    || text.includes("통제 여부")
    || text.includes("process discontinued")
    || text.includes("연 평균")
    || text.includes("지원 자격")
    || text.includes("지원자격")
    || text.includes("불인정")
    || text.includes("공통 필수")
    || text.includes("최소 성적:")
    || text.includes("필수과목 gpa")
    || text.includes("spring 종료 전")
    || text.includes("학과 추가 필수")
    || text.includes("critical:")
    || text.includes("4대 공통")
    || text.includes("대체 가능")
    || text.includes("대체 불가")
    || text.includes("단계 추가 필수")
    || text.includes("coe는 학과별")
    || text.includes("공식 안내:")
    || text.includes("intended major, as listed")
    || text.includes("rigorous program of study")
    || text.includes("stanford soe:") || text.includes("must complete all required")
    || text.includes("아래 학과별 etm 과목")
    || text.includes("2년 졸업 위해 필요")
    || text.includes("assist.org 기준 해당 학과")
    || text.includes("모든 technical 과목")
    || text.includes("combination 과목")
    || text.includes("완료 기한")
    || text.includes("major prerequisite admissions")
    || text.includes("기타 하위과정 공학")
    || text.includes("your interest in the field")
    || text.includes("track declaration")
    || text.includes("chosen on application")
    || text.includes("english composition not in required")
    || text.includes("apma 2501")
    || text.includes("no second choice")
    || text.includes("no major change")
    || text.includes("enter as pre-major first")
    || text.includes("added was missing")
    || text.includes("unc does not have")
    || text.includes("idmen은 stanford")
    || text.includes("학부 전공")
    || text.includes("concentration")
    || text.includes("required courses 없음")
    || text.includes("required course 없음")
    || text.includes("at least one mec course")
    || text.includes("11 credits of math")
    || text.includes("basic program")
  );
}

function classifySourceLine(raw, matches) {
  const text = normalizeText(raw);
  const hasLectureAndLab =
    matches.some((course) => course.isLab) && matches.some((course) => !course.isLab && ["Physics", "Chemistry"].includes(course.category));
  if (isIgnorableSourceLine(raw)) return { type: "ignore", raw };
  if (text.includes("enrollment req") || text.includes("registration") || text.includes("등록") || text.includes("enroll")) {
    return { type: "later", raw, matches };
  }
  if (text.includes("+ lab") && hasLectureAndLab) return { type: "mapped", raw, matches };
  if (
    text.includes(" or ") ||
    text.includes("선택") ||
    text.includes("elective") ||
    text.includes("pick one") ||
    text.includes("one required") ||
    text.includes("택1") ||
    text.includes("one of") ||
    text.includes("choose") ||
    text.includes("choose one")
  ) {
    return { type: "choice", raw, matches };
  }
  if (matches.length >= 1) return { type: "mapped", raw, matches };
  return { type: "review", raw, matches };
}

function summarizeProgramCourses(program) {
  const summarize = (rawCourses) => {
    const mapped = new Map();
    const choices = [];
    const later = [];
    const review = [];

    rawCourses.forEach((raw) => {
      const matches = canonicalMatches(raw);
      const classified = classifySourceLine(raw, matches);
      if (classified.type === "ignore") return;
      if (classified.type === "choice") {
        if (matches.length) choices.push({ raw, options: matches });
        else choices.push({ raw, options: [], note: t("choice_note") });
        return;
      }
      if (classified.type === "later") {
        later.push({ raw, options: matches });
        return;
      }
      if (classified.type === "review") {
        review.push({ raw, reason: t("review_reason") });
        return;
      }
      matches.forEach((course) => {
        const item = mapped.get(course.id) || { ...course, sources: [] };
        item.sources.push(raw);
        mapped.set(course.id, item);
      });
    });

    return { mapped: [...mapped.values()], choices, later, review };
  };

  return {
    required: summarize(program.requiredCourses || []),
    recommended: summarize(program.recommendedCourses || [])
  };
}

function isCourseSatisfied(courseId) {
  return state.completedCourses.has(courseId);
}

function evaluateProgram(program) {
  const courseSummary = summarizeProgramCourses(program);
  const checks = [];
  const additionalInfo = additionalInfoForProgram(program);

  if (program.minGpa !== null) {
    checks.push({
      label: `GPA ${displayRawRequirement(program.rawMinGpa, program.minGpa)}`,
      pass: program.minGpa === null || state.gpa >= program.minGpa,
      status: "normal",
      type: "gpa"
    });
  }
  if (program.minCredits !== null) {
    checks.push({
      label: `Credits ${displayRawRequirement(program.rawMinCredits, program.minCredits)}`,
      pass: program.minCredits === null || state.credits >= program.minCredits,
      status: "normal",
      type: "credits"
    });
  }

  orderCoursesForDisplay(courseSummary.required.mapped).forEach((course) => {
    checks.push({
      label: course.name,
      pass: isCourseSatisfied(course.id),
      status: "normal",
      type: "course"
    });
  });

  courseSummary.required.choices.filter((choice) => choice.options.length).forEach((choice) => {
    checks.push({
      label: `${t("choice_label_pref")} ${choice.options.map((course) => course.name).join(" / ")}`,
      pass: choice.options.some((course) => isCourseSatisfied(course.id)),
      status: "normal",
      type: "course"
    });
  });

  const reviewCount = courseSummary.required.review.length + (program.english?.review?.length || 0);
  if (reviewCount) {
    checks.push({
      label: t("review_count_alert").replace("{count}", reviewCount),
      pass: true,
      status: "review",
      type: "course"
    });
  }

  const englishRequirement = getEnglishRequirement(program);
  if (state.international && englishRequirement !== null) {
    checks.push({
      label: `${englishLabel(state.englishType)} ${englishRequirement}+`,
      pass: state.englishWaiver || state.englishScore >= englishRequirement,
      status: "normal",
      type: "english"
    });
  }

  const blockingChecks = checks.filter((check) => check.status !== "review");
  return {
    pass: blockingChecks.every((check) => check.pass),
    needsReview: checks.some((check) => check.status === "review"),
    checks,
    courseSummary,
    additionalInfo
  };
}

function defaultTargets() {
  return allPrograms().slice(0, 3).map((program) => program.id);
}

function uniqueSchools() {
  return [...new Map(database.schools.map((school) => [school.name, school])).values()].sort((a, b) =>
    a.name.localeCompare(b.name)
  );
}

function programsForSchoolName(schoolName) {
  const normalized = normalizeText(schoolName);
  if (!normalized) return [];
  return allPrograms()
    .filter((program) => normalizeText(program.school.name) === normalized || normalizeText(program.school.shortName) === normalized)
    .sort((a, b) => a.name.localeCompare(b.name));
}

function syncSelectedTargetsFromSlots() {
  const seen = new Set();
  state.targetSlots.forEach((slot, index) => {
    const sName = slot.school?.trim();
    const mName = slot.major?.trim();
    if (sName && mName) {
      const key = `${normalizeText(sName)}:${normalizeText(mName)}`;
      if (seen.has(key)) {
        slot.school = "";
        slot.major = "";
      } else {
        seen.add(key);
      }
    }
  });

  const ids = state.targetSlots
    .map((slot) => {
      const match = programsForSchoolName(slot.school).find((program) => normalizeText(program.name) === normalizeText(slot.major));
      return match?.id;
    })
    .filter(Boolean);
  
  const uniqueIds = [...new Set(ids)];
  
  // Keep all selected targets up to 10 regardless of the Free/Pro plan tier
  state.selectedTargets = uniqueIds.slice(0, 10);

  saveProfileToLocalStorage();
}

function initializeTargetSlots() {
  syncSelectedTargetsFromSlots();
  syncSelectedRoadmapTargetsFromSlots();
}

function renderCourseGroups() {
  const groups = courseCatalog.reduce((acc, course) => {
    acc[course.category] = acc[course.category] || [];
    acc[course.category].push(course);
    return acc;
  }, {});
  
  const track = state.track || "stem";
  let order = [];
  if (track === "stem") {
    order = ["Math", "Physics", "Chemistry", "Science", "English", "Computer", "Engineering"];
  } else if (track === "business") {
    order = ["Math", "Business", "English"];
  } else { // humanities
    order = ["Math", "SocialSciences", "Humanities", "English"];
  }

  const buildHtml = (idSuffix) => {
    return order
      .filter((category) => groups[category]?.length)
      .map(
        (category) => {
          const displayTitle = t("cat_" + category.toLowerCase(), category);
          return `
            <div class="course-group">
              <h4>${escapeHtml(displayTitle)}</h4>
              <div class="course-options">
                ${orderCoursesForDisplay(groups[category])
                  .map(
                    (course) => `
                      <label class="course-row compact-course">
                        <span class="course-name">${escapeHtml(course.name)}</span>
                        <input type="checkbox" data-course="${escapeHtml(course.id)}" data-suffix="${idSuffix}" ${
                          state.completedCourses.has(course.id) ? "checked" : ""
                        } />
                      </label>
                    `
                  )
                  .join("")}
              </div>
            </div>
          `;
        }
      )
      .join("");
  };

  const courseGroupsEl = qs("#courseGroups");
  if (courseGroupsEl) {
    courseGroupsEl.innerHTML = buildHtml("tab1");
  }

  const roadmapCourseGroupsEl = qs("#roadmapCourseGroups");
  if (roadmapCourseGroupsEl) {
    roadmapCourseGroupsEl.innerHTML = buildHtml("tab3");
  }

  qsa("[data-course]").forEach((checkbox) => {
    checkbox.addEventListener("change", () => {
      const courseId = checkbox.dataset.course;
      if (checkbox.checked) {
        state.completedCourses.add(courseId);
      } else {
        state.completedCourses.delete(courseId);
      }
      
      // Real-time synchronization between tabs
      qsa(`[data-course="${courseId}"]`).forEach((other) => {
        other.checked = checkbox.checked;
      });

      saveProfileToLocalStorage();
      renderEligibilityResults();
      buildRoadmap();
    });
  });
}

function renderTargetPicker() {
  const container = qs("#targetPicker");
  if (!container) return;
  
  container.innerHTML = state.targetSlots
    .map((slot, index) => {
      return `
        <div class="target-search-row">
          <span class="target-index">${index + 1}</span>
          <div class="target-inputs-group">
            <div class="form-row search-field autocomplete-container" data-autocomplete-type="school" data-index="${index}" style="position: relative; margin-bottom: 0;">
              <label for="targetSchool${index}">${t("label_school", "School")}</label>
              <div style="position: relative; display: flex; align-items: center;">
                <input type="text" id="targetSchool${index}" class="autocomplete-input school-input" placeholder="${t("select_school_placeholder", "Select School")}" autocomplete="off" value="${escapeHtml(slot.school || '')}" style="width: 100%; padding-right: 30px;" />
                <button type="button" class="autocomplete-toggle-btn" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); background: none; border: none; padding: 4px; color: var(--muted); cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 5;">▼</button>
              </div>
              <div class="search-menu autocomplete-menu hidden" id="targetSchoolMenu${index}"></div>
            </div>
            <div class="form-row search-field autocomplete-container" data-autocomplete-type="major" data-index="${index}" style="position: relative; margin-bottom: 0;">
              <label for="targetMajor${index}">${t("label_major", "Major")}</label>
              <div style="position: relative; display: flex; align-items: center;">
                <input type="text" id="targetMajor${index}" class="autocomplete-input major-input" placeholder="${slot.school ? t("select_major_placeholder", "Select Major") : t("select_school_first_placeholder", "Select School First")}" autocomplete="off" value="${escapeHtml(slot.major || '')}" ${!slot.school ? 'disabled' : ''} style="width: 100%; padding-right: 30px;" />
                <button type="button" class="autocomplete-toggle-btn" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); background: none; border: none; padding: 4px; color: var(--muted); cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 5;" ${!slot.school ? 'disabled' : ''}>▼</button>
              </div>
              <div class="search-menu autocomplete-menu hidden" id="targetMajorMenu${index}"></div>
            </div>
          </div>
        </div>
      `;
    })
    .join("");

  bindAutocompleteEvents(container, "target");
}

function bindAutocompleteEvents(container, type) {
  const getSlots = () => type === "target" ? state.targetSlots : state.roadmapTargetSlots;
  const syncFunc = () => type === "target" ? syncSelectedTargetsFromSlots() : syncSelectedRoadmapTargetsFromSlots();
  const renderFunc = () => type === "target" ? renderTargetPicker() : renderRoadmapTargetPicker();
  const resultsFunc = () => type === "target" ? renderEligibilityResults() : () => {};

  const schools = uniqueSchools();

  container.querySelectorAll(".autocomplete-container").forEach((ac) => {
    const idx = Number(ac.dataset.index);
    const acType = ac.dataset.autocompleteType;
    const input = ac.querySelector(".autocomplete-input");
    const menu = ac.querySelector(".autocomplete-menu");
    const toggleBtn = ac.querySelector(".autocomplete-toggle-btn");

    if (!input || !menu) return;

    const renderMenu = (query = "") => {
      const normalizedQuery = normalizeText(query);
      let items = [];
      const slots = getSlots();

      if (acType === "school") {
        items = schools.filter(s => 
          !normalizedQuery || normalizeText(s.name).includes(normalizedQuery)
        ).map(s => s.name).sort((a, b) => a.localeCompare(b));
      } else {
        const selectedSchool = slots[idx].school;
        const majors = selectedSchool ? programsForSchoolName(selectedSchool) : [];
        items = majors.filter(p =>
          !normalizedQuery || normalizeText(p.name).includes(normalizedQuery)
        ).map(p => p.name).sort((a, b) => a.localeCompare(b));
      }

      menu.innerHTML = items.length
        ? items.map(name => `
            <button type="button" class="autocomplete-item-btn" data-value="${escapeHtml(name)}" style="width: 100%; text-align: left; background: none; border: none; padding: 10px 12px; color: var(--ink); cursor: pointer; display: block; border-bottom: 1px solid var(--line);">
              <span style="font-weight: 600; font-size: 13.5px; display: block; color: var(--ink);">${escapeHtml(name)}</span>
            </button>
          `).join("")
        : `<div class="search-empty" style="padding: 12px; text-align: center; color: var(--muted); font-size: 13px;">${t("search_empty", "No results found")}</div>`;
      
      menu.classList.remove("hidden");
    };

    let autocompleteTimeout;
    let isToggling = false;

    input.addEventListener("focus", () => {
      clearTimeout(autocompleteTimeout);
      if (isToggling) {
        renderMenu(""); // 역삼각형 토글 버튼 클릭 시에는 전체 리스트 노출
      } else {
        renderMenu(input.value);
      }
    });

    input.addEventListener("blur", () => {
      autocompleteTimeout = setTimeout(() => {
        menu.classList.add("hidden");
      }, 200);
    });

    input.addEventListener("input", (e) => {
      renderMenu(e.target.value);
    });

    if (toggleBtn) {
      toggleBtn.addEventListener("mousedown", (e) => {
        e.preventDefault();
        clearTimeout(autocompleteTimeout);
      });
      toggleBtn.addEventListener("click", () => {
        if (menu.classList.contains("hidden")) {
          isToggling = true;
          input.focus();
          renderMenu(""); // 쿼리 필터를 무시하고 전체 목록 렌더링
          isToggling = false;
        } else {
          menu.classList.add("hidden");
        }
      });
    }

    menu.addEventListener("mousedown", (e) => {
      clearTimeout(autocompleteTimeout);
      e.preventDefault(); // 스크롤바 드래그 및 메뉴 클릭 시 포커스 블러 방지

      const btn = e.target.closest(".autocomplete-item-btn");
      if (!btn) return;

      const val = btn.dataset.value;
      input.value = val;
      menu.classList.add("hidden");

      const slots = getSlots();
      if (acType === "school") {
        slots[idx].school = val;
        slots[idx].major = "";
      } else {
        slots[idx].major = val;
      }

      syncFunc();
      renderFunc();
      resultsFunc();
    });
  });
}

function bindSingleAutocomplete({
  schoolInput,
  schoolMenu,
  schoolToggle,
  majorInput,
  majorMenu,
  majorToggle,
  onSelected
}) {
  const schools = uniqueSchools();

  const renderSchoolMenu = (query = "") => {
    const normalizedQuery = normalizeText(query);
    const items = schools.filter(s => 
      !normalizedQuery || normalizeText(s.name).includes(normalizedQuery)
    ).map(s => s.name).sort((a, b) => a.localeCompare(b));

    schoolMenu.innerHTML = items.length
      ? items.map(name => `
          <button type="button" class="autocomplete-item-btn" data-value="${escapeHtml(name)}" style="width: 100%; text-align: left; background: none; border: none; padding: 10px 12px; color: var(--ink); cursor: pointer; display: block; border-bottom: 1px solid var(--line);">
            <span style="font-weight: 600; font-size: 13.5px; display: block; color: var(--ink);">${escapeHtml(name)}</span>
          </button>
        `).join("")
      : `<div class="search-empty" style="padding: 12px; text-align: center; color: var(--muted); font-size: 13px;">${t("search_empty", "No results found")}</div>`;
    
    schoolMenu.classList.remove("hidden");
  };

  const renderMajorMenu = (query = "") => {
    const selectedSchool = schoolInput.value.trim();
    const majors = selectedSchool ? programsForSchoolName(selectedSchool) : [];
    const normalizedQuery = normalizeText(query);
    const items = majors.filter(p =>
      !normalizedQuery || normalizeText(p.name).includes(normalizedQuery)
    ).map(p => p.name).sort((a, b) => a.localeCompare(b));

    majorMenu.innerHTML = items.length
      ? items.map(name => `
          <button type="button" class="autocomplete-item-btn" data-value="${escapeHtml(name)}" style="width: 100%; text-align: left; background: none; border: none; padding: 10px 12px; color: var(--ink); cursor: pointer; display: block; border-bottom: 1px solid var(--line);">
            <span style="font-weight: 600; font-size: 13.5px; display: block; color: var(--ink);">${escapeHtml(name)}</span>
          </button>
        `).join("")
      : `<div class="search-empty" style="padding: 12px; text-align: center; color: var(--muted); font-size: 13px;">${t("search_empty", "No results found")}</div>`;
    
    majorMenu.classList.remove("hidden");
  };

  let schoolBlurTimeout;
  let majorBlurTimeout;
  let isSchoolToggling = false;
  let isMajorToggling = false;

  // School events
  schoolInput.addEventListener("focus", () => {
    clearTimeout(schoolBlurTimeout);
    if (isSchoolToggling) {
      renderSchoolMenu("");
    } else {
      renderSchoolMenu(schoolInput.value);
    }
  });

  schoolInput.addEventListener("blur", () => {
    schoolBlurTimeout = setTimeout(() => {
      schoolMenu.classList.add("hidden");
    }, 200);
  });

  schoolInput.addEventListener("input", (e) => {
    if (!e.target.value.trim()) {
      majorInput.value = "";
      safeSetAttr(majorInput, "disabled", "true");
      safeSetAttr(majorInput, "data-i18n-placeholder", "select_school_first_placeholder");
      majorInput.placeholder = t("select_school_first_placeholder", "Select School First");
      if (majorToggle) safeSetAttr(majorToggle, "disabled", "true");
      onSelected("", "");
    } else {
      renderSchoolMenu(e.target.value);
    }
  });

  if (schoolToggle) {
    schoolToggle.addEventListener("mousedown", (e) => {
      e.preventDefault();
      clearTimeout(schoolBlurTimeout);
    });
    schoolToggle.addEventListener("click", () => {
      if (schoolMenu.classList.contains("hidden")) {
        isSchoolToggling = true;
        schoolInput.focus();
        renderSchoolMenu("");
        isSchoolToggling = false;
      } else {
        schoolMenu.classList.add("hidden");
      }
    });
  }

  schoolMenu.addEventListener("mousedown", (e) => {
    clearTimeout(schoolBlurTimeout);
    e.preventDefault();

    const btn = e.target.closest(".autocomplete-item-btn");
    if (!btn) return;

    const val = btn.dataset.value;
    schoolInput.value = val;
    schoolMenu.classList.add("hidden");

    // Reset major when school changes
    majorInput.value = "";
    safeRemoveAttr(majorInput, "disabled");
    safeSetAttr(majorInput, "data-i18n-placeholder", "select_major_placeholder");
    majorInput.placeholder = t("select_major_placeholder", "Select Major");
    if (majorToggle) safeRemoveAttr(majorToggle, "disabled");

    onSelected(val, "");
  });

  // Major events
  majorInput.addEventListener("focus", () => {
    clearTimeout(majorBlurTimeout);
    if (isMajorToggling) {
      renderMajorMenu("");
    } else {
      renderMajorMenu(majorInput.value);
    }
  });

  majorInput.addEventListener("blur", () => {
    majorBlurTimeout = setTimeout(() => {
      majorMenu.classList.add("hidden");
    }, 200);
  });

  majorInput.addEventListener("input", (e) => {
    if (!e.target.value.trim()) {
      onSelected(schoolInput.value, "");
    } else {
      renderMajorMenu(e.target.value);
    }
  });

  if (majorToggle) {
    majorToggle.addEventListener("mousedown", (e) => {
      e.preventDefault();
      clearTimeout(majorBlurTimeout);
    });
    majorToggle.addEventListener("click", () => {
      if (majorMenu.classList.contains("hidden")) {
        isMajorToggling = true;
        majorInput.focus();
        renderMajorMenu("");
        isMajorToggling = false;
      } else {
        majorMenu.classList.add("hidden");
      }
    });
  }

  majorMenu.addEventListener("mousedown", (e) => {
    clearTimeout(majorBlurTimeout);
    e.preventDefault();

    const btn = e.target.closest(".autocomplete-item-btn");
    if (!btn) return;

    const val = btn.dataset.value;
    majorInput.value = val;
    majorMenu.classList.add("hidden");

    onSelected(schoolInput.value, val);
  });
}

function syncSelectedRoadmapTargetsFromSlots() {
  const seen = new Set();
  state.roadmapTargetSlots.forEach((slot, index) => {
    const sName = slot.school?.trim();
    const mName = slot.major?.trim();
    if (sName && mName) {
      const key = `${normalizeText(sName)}:${normalizeText(mName)}`;
      if (seen.has(key)) {
        slot.school = "";
        slot.major = "";
        const schoolInput = qs(`#roadmapTargetSchool${index}`);
        const majorInput = qs(`#roadmapTargetMajor${index}`);
        if (schoolInput) schoolInput.value = "";
        if (majorInput) majorInput.value = "";
      } else {
        seen.add(key);
      }
    }
  });

  const ids = state.roadmapTargetSlots
    .map((slot) => {
      const match = programsForSchoolName(slot.school).find((program) => normalizeText(program.name) === normalizeText(slot.major));
      return match?.id;
    })
    .filter(Boolean);
  
  state.selectedRoadmapTargets = [...new Set(ids)];
  saveProfileToLocalStorage();
  buildRoadmap();
}

function renderRoadmapTargetPicker() {
  const container = qs("#roadmapTargetPicker");
  if (!container) return;
  
  container.innerHTML = state.roadmapTargetSlots
    .map((slot, index) => {
      return `
        <div class="target-search-row">
          <span class="target-index">${index + 1}</span>
          <div class="target-inputs-group">
            <div class="form-row search-field autocomplete-container" data-autocomplete-type="school" data-index="${index}" style="position: relative; margin-bottom: 0;">
              <label for="roadmapTargetSchool${index}">${t("label_school", "School")}</label>
              <div style="position: relative; display: flex; align-items: center;">
                <input type="text" id="roadmapTargetSchool${index}" class="autocomplete-input school-input" placeholder="${t("select_school_placeholder", "Select School")}" autocomplete="off" value="${escapeHtml(slot.school || '')}" style="width: 100%; padding-right: 30px;" />
                <button type="button" class="autocomplete-toggle-btn" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); background: none; border: none; padding: 4px; color: var(--muted); cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 5;">▼</button>
              </div>
              <div class="search-menu autocomplete-menu hidden" id="roadmapTargetSchoolMenu${index}"></div>
            </div>
            <div class="form-row search-field autocomplete-container" data-autocomplete-type="major" data-index="${index}" style="position: relative; margin-bottom: 0;">
              <label for="roadmapTargetMajor${index}">${t("label_major", "Major")}</label>
              <div style="position: relative; display: flex; align-items: center;">
                <input type="text" id="roadmapTargetMajor${index}" class="autocomplete-input major-input" placeholder="${slot.school ? t("select_major_placeholder", "Select Major") : t("select_school_first_placeholder", "Select School First")}" autocomplete="off" value="${escapeHtml(slot.major || '')}" ${!slot.school ? 'disabled' : ''} style="width: 100%; padding-right: 30px;" />
                <button type="button" class="autocomplete-toggle-btn" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); background: none; border: none; padding: 4px; color: var(--muted); cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 5;" ${!slot.school ? 'disabled' : ''}>▼</button>
              </div>
              <div class="search-menu autocomplete-menu hidden" id="roadmapTargetMajorMenu${index}"></div>
            </div>
          </div>
        </div>
      `;
    })
    .join("");

  bindAutocompleteEvents(container, "roadmap");
}

function renderEligibilityResults() {
  const container = qs("#eligibilityResults");
  if (!container) return;
  let selectedPrograms = allPrograms().filter((program) => state.selectedTargets.includes(program.id));
  let isExample = false;
  if (selectedPrograms.length === 0) {
    const exampleId = "university-of-washington-computer-engineering-allen-school-347f00d7";
    const exampleProg = allPrograms().find(p => p.id === exampleId) || allPrograms()[0];
    if (exampleProg) {
      selectedPrograms = [exampleProg];
      isExample = true;
    }
  }

  container.innerHTML =
    selectedPrograms
      .map((program, idx) => {
        const evaluation = evaluateProgram(program);
        const verificationNotice = verificationNoticeForProgram(program);
        const statusText = evaluation.pass ? (evaluation.needsReview ? t("status_pass_review") : t("status_pass")) : t("status_fail");
        const visibleChecks = evaluation.checks.filter((check) => check.status !== "review");
        const primaryChecks = visibleChecks.slice(0, 14);
        const extraChecks = visibleChecks.slice(14);
        const requiredCourseIds = new Set(evaluation.courseSummary.required.mapped.map((course) => course.id));
        const isHolistic =
          evaluation.courseSummary.required.mapped.length === 0 &&
          evaluation.courseSummary.required.choices.length === 0 &&
          evaluation.courseSummary.required.review.length === 0;
        const recommendedMapped = orderCoursesForDisplay(evaluation.courseSummary.recommended.mapped.filter((course) => !requiredCourseIds.has(course.id)));
        const recommendedChoices = evaluation.courseSummary.recommended.choices
          .map((choice) => ({ ...choice, options: choice.options.filter((course) => !requiredCourseIds.has(course.id)) }))
          .filter((choice) => choice.options.length || choice.raw);
        const recommendedLater = evaluation.courseSummary.recommended.later
          .map((item) => ({ ...item, options: item.options.filter((course) => !requiredCourseIds.has(course.id)) }))
          .filter((item) => item.options.length || item.raw);
        const englishReviewItems = (program.english?.review || []).map((raw) => ({
          raw,
          reason: t("eng_review_reason")
        }));
        const reviewItems = [...evaluation.courseSummary.required.review, ...englishReviewItems];
        const recommendedItems = [
          ...recommendedMapped.map((course) => ({ label: course.name, detail: `${course.category} · ${t("rec_course_detail")}` })),
          ...recommendedChoices.map((choice) => ({
            label: choice.options.length ? `${t("rec_choice_label")} ${choice.options.map((course) => course.name).join(" / ")}` : choice.raw,
            detail: choice.options.length ? t("rec_choice_detail") : t("rec_choice_raw_detail")
          })),
          ...recommendedLater.map((item) => ({
            label: item.options.length ? item.options.map((course) => course.name).join(" / ") : item.raw,
            detail: t("rec_other_detail")
          }))
        ];

        let isLocked = false;
        if (state.plan === "Free" && !isExample) {
          const schoolName = program.school.name;
          if (state.analyzedSchools.has(schoolName)) {
            isLocked = false;
          } else if (state.analyzedSchools.size < 5) {
            state.analyzedSchools.add(schoolName);
            saveAnalyzedSchoolsToLocalStorage();
            isLocked = false;
          } else {
            isLocked = true;
          }
        }

        const lockedOverlayHtml = isLocked ? `
          <div class="locked-overlay">
            <div class="locked-content">
              <span class="locked-icon">🔒</span>
              <h4>${escapeHtml(t("locked_card_title", "Pro / Premium Plan 전용"))}</h4>
              <p>${escapeHtml(t("locked_card_desc", "6번째 대학부터는 Pro 플랜 이상에서 분석 가능합니다."))}</p>
              <button class="primary-btn compact" type="button" onclick="openPricingModal()">${escapeHtml(t("btn_upgrade_now", "지금 업그레이드"))}</button>
            </div>
          </div>
        ` : "";

        return `
          <div class="locked-card-wrapper">
            <article class="result-card ${evaluation.pass && !evaluation.needsReview ? "pass" : ""} ${isExample ? "example-card" : ""} ${isLocked ? "locked-card" : ""}">
              ${lockedOverlayHtml}
              <span class="status ${evaluation.pass && !evaluation.needsReview ? "pass" : "fail"}">${statusText}</span>
              <h3>${escapeHtml(program.school.shortName)}</h3>
              <p>${escapeHtml(program.name)}</p>
              <div class="badge-container">${getConfidenceBadgeHtml(program)}</div>
              ${
                verificationNotice
                  ? `<div class="verification-alert">
                      <strong>${escapeHtml(verificationNotice.label)}</strong>
                      <span>${escapeHtml(verificationNotice.detail)}</span>
                    </div>`
                  : ""
              }
              ${
                isHolistic
                  ? `<div class="holistic-warning" style="margin: 12px 0; background: rgba(59, 130, 246, 0.08); border-left: 4px solid var(--accent); padding: 12px; border-radius: 6px;">
                      <strong style="color: var(--accent); display: block; margin-bottom: 4px; font-size: 14px;">ℹ️ ${escapeHtml(t("holistic_eval_title"))}</strong>
                      <span style="color: var(--foreground); font-size: 13px; line-height: 1.4; display: block;">${escapeHtml(t("holistic_eval_desc"))}</span>
                    </div>`
                  : ""
              }
              <div class="check-list">
                ${primaryChecks
                  .map(
                    (check) => `
                      <div class="check-item ${check.pass ? "pass" : "fail"} ${check.type ? 'check-' + check.type : ''}">
                        <span>${escapeHtml(check.label)}</span>
                        <strong>${check.pass ? t("tag_pass") : t("tag_fail")}</strong>
                      </div>
                    `
                  )
                  .join("")}
              </div>
              ${
                extraChecks.length
                  ? `<details class="result-details">
                      <summary>${t("roadmap_extra_summary").replace("{count}", extraChecks.length)}</summary>
                      <p class="result-detail-note">${t("roadmap_extra_note")}</p>
                      <div class="check-list">
                        ${extraChecks
                          .map(
                            (check) => `
                              <div class="check-item ${check.pass ? "pass" : "fail"} ${check.type ? 'check-' + check.type : ''}">
                                <span>${escapeHtml(check.label)}</span>
                                <strong>${check.pass ? t("tag_pass") : t("tag_fail")}</strong>
                              </div>
                            `
                          )
                          .join("")}
                      </div>
                    </details>`
                  : ""
              }
              ${
                recommendedItems.length
                  ? `<details class="result-details recommended-details">
                      <summary>${t("roadmap_recommended_summary").replace("{count}", recommendedItems.length)}</summary>
                      <ul>
                        ${recommendedItems
                          .map(
                            (item) => `
                              <li>
                                <strong>${escapeHtml(item.label)}</strong>
                                <span>${escapeHtml(item.detail || t("rec_course_detail"))}</span>
                              </li>
                            `
                          )
                          .join("")}
                      </ul>
                    </details>`
                  : ""
              }
              ${
                reviewItems.length
                  ? `<details class="result-details review-details">
                      <summary>${t("roadmap_review_summary").replace("{count}", reviewItems.length)}</summary>
                      <ul>
                        ${reviewItems
                          .map(
                            (item) => `
                              <li>
                                <strong>${escapeHtml(reviewFocus(item))}</strong>
                                <span>${escapeHtml(item.raw || item)}</span>
                              </li>
                            `
                          )
                          .join("")}
                      </ul>
                    </details>`
                  : ""
              }
              ${
                evaluation.additionalInfo.length
                  ? `<p class="result-note">${evaluation.additionalInfo.map((item) => escapeHtml(item)).join("<br />")}</p>`
                  : ""
              }
            </article>
          </div>
        `;
      })
      .join("") || `<div class="panel"><strong>${t("no_targets_selected")}</strong></div>`;
}

function scrollToSection(selector) {
  const target = qs(selector);
  if (!target) return;
  target.scrollIntoView({ behavior: "smooth", block: "start" });
}

function activateProductTab(tabName, shouldScroll = true) {
  if (!checkPlanAccess(tabName)) {
    openPricingModal();
    return;
  }
  const button = qs(`[data-tab="${tabName}"]`);
  const panel = qs(`#${tabName}`);
  if (!button || !panel) return;
  qsa(".tab-button").forEach((item) => item.classList.remove("active"));
  qsa(".tab-panel").forEach((item) => item.classList.remove("active"));
  button.classList.add("active");
  panel.classList.add("active");
  qs("#pageTitle").textContent = t("tab_" + tabName, pageTitles[tabName]);
  
  // Smooth scroll active tab button into center on mobile viewport
  button.scrollIntoView({ behavior: "smooth", block: "nearest", inline: "center" });

  if (tabName === "requirements") {
    const schoolInput = qs("#reqSchoolInput");
    const majorInput = qs("#reqMajorInput");
    const majorToggle = majorInput?.parentElement?.querySelector(".autocomplete-toggle-btn");
    if (schoolInput) {
      schoolInput.value = "";
      setTimeout(() => schoolInput.focus(), 50);
    }
    if (majorInput) {
      majorInput.value = "";
      majorInput.setAttribute("disabled", "true");
      majorInput.setAttribute("data-i18n-placeholder", "select_school_first_placeholder");
      majorInput.placeholder = t("select_school_first_placeholder", "Select School First");
    }
    if (majorToggle) {
      majorToggle.setAttribute("disabled", "true");
    }
    renderRequirementDetail("");
  }

  if (shouldScroll) scrollToSection("#demo");
}



let sortedPrograms = [];
function renderRequirementControls() {
  sortedPrograms = allPrograms().sort(
    (a, b) => a.school.name.localeCompare(b.school.name) || a.name.localeCompare(b.name)
  );
  const options = allPrograms()
    .map((program) => `<option value="${escapeHtml(program.id)}">${escapeHtml(program.school.shortName)} · ${escapeHtml(program.name)}</option>`)
    .join("");
  qs("#requirementSelect").innerHTML = options;
  const roadmapTarget = qs("#roadmapTarget");
  if (roadmapTarget) {
    roadmapTarget.innerHTML = options;
  }
  const firstProgramId = allPrograms()[0]?.id;
  if (firstProgramId) {
    qs("#requirementSelect").value = firstProgramId;
    if (roadmapTarget) {
      roadmapTarget.value = firstProgramId;
    }
  }

  const reqSchoolInput = qs("#reqSchoolInput");
  const reqSchoolMenu = qs("#reqSchoolMenu");
  const reqSchoolToggle = reqSchoolInput?.parentElement?.querySelector(".autocomplete-toggle-btn");
  const reqMajorInput = qs("#reqMajorInput");
  const reqMajorMenu = qs("#reqMajorMenu");
  const reqMajorToggle = reqMajorInput?.parentElement?.querySelector(".autocomplete-toggle-btn");

  if (reqSchoolInput && reqSchoolMenu && reqMajorInput && reqMajorMenu) {
    bindSingleAutocomplete({
      schoolInput: reqSchoolInput,
      schoolMenu: reqSchoolMenu,
      schoolToggle: reqSchoolToggle,
      majorInput: reqMajorInput,
      majorMenu: reqMajorMenu,
      majorToggle: reqMajorToggle,
      onSelected: (schoolName, majorName) => {
        const reqSelect = qs("#requirementSelect");
        if (schoolName && majorName) {
          const match = programsForSchoolName(schoolName).find(p => normalizeText(p.name) === normalizeText(majorName));
          if (match) {
            if (reqSelect) reqSelect.value = match.id;
            renderRequirementDetail(match.id);
          }
        } else {
          renderRequirementDetail("");
        }
      }
    });

    // Initialize fields based on current requirementSelect value
    const selectedProgramId = qs("#requirementSelect")?.value;
    if (selectedProgramId) {
      const program = allPrograms().find(p => p.id === selectedProgramId);
      if (program) {
        reqSchoolInput.value = program.school.name;
        reqMajorInput.value = program.name;
        safeRemoveAttr(reqMajorInput, "disabled");
        safeSetAttr(reqMajorInput, "data-i18n-placeholder", "select_major_placeholder");
        reqMajorInput.placeholder = t("select_major_placeholder", "Select Major");
        if (reqMajorToggle) safeRemoveAttr(reqMajorToggle, "disabled");
      }
    }
  }

  qs("#requirementSelect")?.addEventListener("change", () => {
    const reqSel = qs("#requirementSelect");
    if (reqSel) renderRequirementDetail(reqSel.value);
  });
  document.addEventListener("click", (event) => {
    if (!event.target.closest(".requirement-search-field")) {
      qs("#requirementMenu")?.classList.remove("open");
    }
    document.querySelectorAll(".autocomplete-container").forEach((ac) => {
      const menu = ac.querySelector(".autocomplete-menu");
      if (menu && !ac.contains(event.target)) {
        menu.classList.add("hidden");
      }
    });
    const essayContainer = qs("#essayTargetContainer");
    if (essayContainer && !essayContainer.contains(event.target)) {
      qs("#essayTargetMenu")?.classList.add("hidden");
    }
  });
  renderRequirementDetail(allPrograms()[0]?.id);
}

function renderMappedCourseList(courses) {
  return courses.length
    ? courses
        .map(
          (course) => `
            <li>
              <strong>${escapeHtml(course.name)}</strong>
              <span>${escapeHtml(course.category)}${course.isLab ? " · Lab" : ""}</span>
            </li>
          `
        )
        .join("")
    : `<li><strong>${escapeHtml(t("no_mapped_items"))}</strong><span>${escapeHtml(t("mapped_items_desc"))}</span></li>`;
}

function renderReviewList(items) {
  return items.length
    ? items
        .slice(0, 20)
        .map(
          (item) =>
            `<li><strong>${escapeHtml(reviewFocus(item))}</strong><span>${escapeHtml(item.reason || t("review_reason"))}<br />Source: ${escapeHtml(
              item.raw || item
            )}</span></li>`
        )
        .join("")
    : `<li><strong>${escapeHtml(t("no_review_items"))}</strong><span>${escapeHtml(t("review_items_desc"))}</span></li>`;
}

function renderInfoList(items) {
  return items.length
    ? items.map((item) => `<li><strong>${escapeHtml(item)}</strong><span>${escapeHtml(t("roadmap_extra_note"))}</span></li>`).join("")
    : `<li><strong>${escapeHtml(t("no_extra_info"))}</strong><span>${escapeHtml(t("extra_info_desc"))}</span></li>`;
}

function renderEnglishRequirementRows(english) {
  const rows = [
    ["TOEFL", english?.TOEFL],
    ["TOEFL 2026 Jan Revised", english?.TOEFL_2026],
    ["IELTS", english?.IELTS],
    ["Duolingo", english?.Duolingo]
  ].filter(([, value]) => value !== null && value !== undefined && value !== "");

  return rows.length
    ? rows
        .map(([label, value]) => `<div class="check-item"><span>${escapeHtml(label)}</span><strong>${escapeHtml(value)}</strong></div>`)
        .join("")
    : "";
}

function renderChoiceList(items) {
  return items.length
    ? items
        .map(
          (item) => `
            <li>
              <strong>${escapeHtml(item.options.length ? item.options.map((course) => course.name).join(" / ") : item.raw)}</strong>
              <span>${escapeHtml(item.options.length ? t("opt_condition_desc") : t("choice_note"))}</span>
            </li>
          `
        )
        .join("")
    : `<li><strong>${escapeHtml(t("no_opt_conditions"))}</strong><span>${escapeHtml(t("opt_conditions_desc"))}</span></li>`;
}

function renderLaterList(items) {
  return items.length
    ? items
        .map(
          (item) => `
            <li>
              <strong>${escapeHtml(item.options.length ? item.options.map((course) => course.name).join(" / ") : item.raw)}</strong>
              <span>${escapeHtml(t("roadmap_tag_choice"))}</span>
            </li>
          `
        )
        .join("")
    : `<li><strong>${escapeHtml(t("no_enroll_conditions"))}</strong><span>${escapeHtml(t("enroll_conditions_desc"))}</span></li>`;
}

function getCompetitiveProfile(program) {
  const schoolName = (program.school.name || "").toLowerCase();
  const majorName = (program.name || "").toLowerCase();
  
  let tier = "Mid";
  let gpaRange = "3.50 - 3.75";
  let essayWeight = "High (중요)";
  let essayWeightEn = "High";
  let recLetters = "Recommended (1 letter)";
  let recLettersKo = "권장 (1부)";
  
  const isTopTier = schoolName.includes("berkeley") || 
                    schoolName.includes("ucla") || 
                    schoolName.includes("michigan") || 
                    schoolName.includes("georgia institute") || 
                    schoolName.includes("columbia") || 
                    schoolName.includes("cornell") || 
                    schoolName.includes("nyu") || 
                    schoolName.includes("usc") || 
                    schoolName.includes("northwestern");
                    
  if (isTopTier) {
    tier = "Top";
    gpaRange = "3.85 - 4.00";
    essayWeight = "Critical (당락 결정)";
    essayWeightEn = "Critical";
    recLetters = "Required / Highly Recommended (1-2 letters)";
    recLettersKo = "필수 또는 강력 권장 (1-2부)";
  } else {
    const isMidTier = schoolName.includes("illinois") || 
                      schoolName.includes("purdue") || 
                      schoolName.includes("washington") || 
                      schoolName.includes("wisconsin") || 
                      schoolName.includes("texas");
    if (isMidTier) {
      tier = "Mid";
      gpaRange = "3.72 - 3.90";
      essayWeight = "High (중요)";
      essayWeightEn = "High";
      recLetters = "Recommended (1 letter)";
      recLettersKo = "권장 (1부)";
    } else {
      tier = "General";
      gpaRange = "3.40 - 3.65";
      essayWeight = "Medium (보통)";
      essayWeightEn = "Medium";
      recLetters = "Optional (선택 사항)";
      recLettersKo = "선택 사항";
    }
  }

  let track = "STEM";
  let electives = ["General Chemistry", "Calculus III", "Linear Algebra", "Differential Equations", "Physics (Mechanics/Electromagnetism)"];
  let activities = [];
  
  const isKo = (state.language || "ko") === "ko";
  
  if (majorName.includes("business") || majorName.includes("manage") || majorName.includes("finance") || majorName.includes("accounting") || majorName.includes("econ")) {
    track = "Business";
    electives = ["Macroeconomics", "Microeconomics", "Business Statistics", "Financial Accounting", "Managerial Accounting"];
    activities = isKo
      ? ["경영 케이스 공모전 참여 및 입상", "경영/금융 학회 리더십", "기업 인턴십 체험 (마케팅/재무 분석)", "비즈니스 영어 디베이트 클럽"]
      : ["Business case competition participation and award", "Business/Finance academic society leadership", "Corporate internship experience (Marketing/Financial analysis)", "Business English debate club"];
  } else if (majorName.includes("english") || majorName.includes("history") || majorName.includes("philosophy") || majorName.includes("political") || majorName.includes("sociology") || majorName.includes("art") || majorName.includes("literature") || majorName.includes("humanities") || majorName.includes("psych") || majorName.includes("comm") || majorName.includes("relations") || majorName.includes("anthro") || majorName.includes("ling") || majorName.includes("global") || majorName.includes("gov") || majorName.includes("social studies")) {
    track = "Humanities";
    electives = ["Introduction to Sociology", "World History", "Ethics", "Critical Writing", "Political Theory"];
    activities = isKo
      ? ["인문/사회과학 학생 저널 투고", "시민단체(NGO) 및 봉사단체 프로젝트 리드", "사회적 이슈 관련 에세이 공모전", "교내 영자신문사 에디터 활동"]
      : ["Submission to human/social sciences student journals", "NGO & volunteer organization project leadership", "Essay competition on social issues", "Campus English newspaper editor activity"];
  } else {
    track = "STEM";
    electives = ["General Chemistry", "Calculus III", "Linear Algebra", "Differential Equations", "Physics (Mechanics/Electromagnetism)"];
    activities = isKo 
      ? ["경진대회(Robotics/Hackathon) 참여", "학술 및 전공 동아리 리더십", "Github 오픈소스 기여 및 독자 프로젝트 개발", "Calculus/Physics 과목 피어 튜터링"]
      : ["Participation in competitions (Robotics/Hackathon)", "Academic & major club leadership", "Github open-source contribution & independent project development", "Calculus/Physics peer tutoring"];
  }

  return {
    tier,
    gpaRange,
    essayWeight,
    essayWeightEn,
    recLetters,
    recLettersKo,
    track,
    electives,
    activities
  };
}

function renderRequirementDetail(programId) {
  const container = qs("#requirementDetail");
  if (!container) return;
  const isSearchEmpty = !qs("#reqSchoolInput")?.value?.trim() || !qs("#reqMajorInput")?.value?.trim();
  if (!programId || isSearchEmpty) {
    container.innerHTML = `
      <div class="placeholder-view" style="text-align: center; padding: 60px 20px;">
        <div style="font-size: 48px; margin-bottom: 20px;">🔍</div>
        <h3 style="color: var(--foreground); margin-bottom: 8px;" data-i18n="req_finder_placeholder_title">${escapeHtml(t("req_finder_placeholder_title"))}</h3>
        <p style="color: var(--muted); font-size: 14px; max-width: 400px; margin: 0 auto;" data-i18n="req_finder_placeholder_desc">${escapeHtml(t("req_finder_placeholder_desc"))}</p>
      </div>
    `;
    return;
  }
  const program = allPrograms().find((item) => item.id === programId);
  if (!program) return;
  const summary = summarizeProgramCourses(program);
  const additionalInfo = additionalInfoForProgram(program);
  const verificationNotice = verificationNoticeForProgram(program);
  const requiredCourseIds = new Set(summary.required.mapped.map((course) => course.id));
  const recommendedMapped = summary.recommended.mapped.filter((course) => !requiredCourseIds.has(course.id));
  const recommendedChoices = summary.recommended.choices
    .map((choice) => ({ ...choice, options: choice.options.filter((course) => !requiredCourseIds.has(course.id)) }))
    .filter((choice) => choice.options.length || choice.raw);
  const recommendedLater = summary.recommended.later
    .map((item) => ({ ...item, options: item.options.filter((course) => !requiredCourseIds.has(course.id)) }))
    .filter((item) => item.options.length || item.raw);
  const englishReviewItems = (program.english?.review || []).map((raw) => ({
    raw,
    reason: t("eng_review_reason")
  }));
  const requirementRows = [
    program.minGpa !== null
      ? `<div class="check-item"><span>Minimum GPA</span><strong>${escapeHtml(displayRawRequirement(program.rawMinGpa, program.minGpa))}</strong></div>`
      : "",
    program.minCredits !== null
      ? `<div class="check-item"><span>Minimum Credits</span><strong>${escapeHtml(displayRawRequirement(program.rawMinCredits, program.minCredits))}</strong></div>`
      : ""
  ].join("");

  const isHolistic =
    summary.required.mapped.length === 0 &&
    recommendedMapped.length === 0 &&
    summary.required.choices.length === 0 &&
    recommendedChoices.length === 0 &&
    summary.required.later.length === 0 &&
    recommendedLater.length === 0 &&
    summary.required.review.length === 0 &&
    summary.recommended.review.length === 0;

  const compProfile = getCompetitiveProfile(program);
  const isKo = (state.language || "ko") === "ko";
  
  const activeTrack = state.track || "stem";
  const lang = state.language || "en";
  const trackCases = (window.AdmissionsCasesDatabase && window.AdmissionsCasesDatabase.profiles[lang]?.[activeTrack]) || [];
  
  let casesHtml = "";
  if (trackCases.length > 0) {
    casesHtml = `
      <article class="requirement-card" style="border-left: 4px solid #10b981; background: rgba(255, 255, 255, 0.01);">
        <h3 style="color: #10b981; display: flex; align-items: center; gap: 8px; font-size: 15px;">
          <span>📈</span> ${isKo ? "공개 데이터 기반 합격 표본 분석" : "Public Data-Based Admitted Specimen Analysis"}
        </h3>
        <p style="font-size: 13px; color: var(--muted); margin-bottom: 16px; line-height: 1.45;">
          ${isKo ? "본 정보는 공개된 편입 결과 및 커뮤니티 통계 데이터를 분석하여 구성한 참고용 표본입니다. (직접적인 합격 실적이 아닌 외부 사례 데이터 기반 분석 정보입니다)" : "This is a reference specimen compiled from public college datasets and community reports for diagnostic reference. (External historical data, not direct client results)"}
        </p>
        
        <div style="display: flex; flex-direction: column; gap: 12px;">
          ${trackCases.map((c, idx) => `
            <div style="background: rgba(255,255,255,0.02); border: 1px solid var(--line); border-radius: 8px; padding: 12px; font-size: 13px;">
              <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 6px; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 6px;">
                <span style="font-weight: 800; color: #10b981;">Case #${1000 + idx + (activeTrack === "business" ? 10 : activeTrack === "humanities" ? 20 : 0)}</span>
                <span style="font-size: 11px; background: rgba(16, 185, 129, 0.1); color: #10b981; padding: 2px 6px; border-radius: 4px; font-weight: 700;">
                  ${isKo ? "합격 표본" : "Sample Specimen"}
                </span>
              </div>
              <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 6px 12px; margin-bottom: 6px;">
                <div><span style="color: var(--muted); font-size: 12px;">GPA:</span> <strong style="color: #ffffff;">${c.gpa}</strong></div>
                <div><span style="color: var(--muted); font-size: 12px;">${isKo ? "출신 대학" : "Origin"}:</span> <strong style="color: #ffffff;">${c.origin}</strong></div>
                <div><span style="color: var(--muted); font-size: 12px;">${isKo ? "유학생 여부" : "International"}:</span> <strong style="color: #ffffff;">${c.international}</strong></div>
                <div style="grid-column: span 2;"><span style="color: var(--muted); font-size: 12px;">${isKo ? "최종 결과" : "Outcome"}:</span> <strong style="color: #fbbf24;">${c.outcome}</strong></div>
              </div>
              <div style="margin-top: 6px; font-size: 12px; color: var(--muted); line-height: 1.5; background: rgba(0,0,0,0.1); padding: 8px; border-radius: 4px;">
                💡 <strong>${isKo ? "주요 활동 & 스펙" : "Key Activities"}:</strong> ${c.extra}
              </div>
            </div>
          `).join("")}
        </div>
      </article>
    `;
  }
  
  const compProfileHtml = `
    <article class="requirement-card" style="border-left: 4px solid var(--accent); background: rgba(255, 255, 255, 0.01);">
      <h3 style="color: var(--accent); display: flex; align-items: center; gap: 8px; font-size: 15px;">
        <span>🎓</span> ${isKo ? "명문대 합격생 스펙 & 카운슬러 가이드" : "Admissions Spec & Counselor Guide"}
      </h3>
      <p style="font-size: 13px; color: var(--muted); margin-bottom: 16px;">
        ${isKo ? "수집된 공개 편입 결과 및 통계 데이터를 분석하여 산출된 권장 경쟁력 지표입니다." : "Recommended competitive metrics calculated based on compiled public transfer datasets and statistical profiles."}
      </p>
      
      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 16px;">
        <div style="background: rgba(255,255,255,0.02); border: 1px solid var(--line); border-radius: 8px; padding: 10px 12px;">
          <span style="font-size: 11px; color: var(--muted); font-weight: 700; text-transform: uppercase; display: block; margin-bottom: 2px;">
            ${isKo ? "합격생 평균 GPA 범위" : "Competitive GPA Range"}
          </span>
          <strong style="color: #ffffff; font-size: 15px;">${compProfile.gpaRange}</strong>
        </div>
        <div style="background: rgba(255,255,255,0.02); border: 1px solid var(--line); border-radius: 8px; padding: 10px 12px;">
          <span style="font-size: 11px; color: var(--muted); font-weight: 700; text-transform: uppercase; display: block; margin-bottom: 2px;">
            ${isKo ? "에세이 반영 비중" : "Essay Importance"}
          </span>
          <strong style="color: #fbbf24; font-size: 15px;">${isKo ? compProfile.essayWeight : compProfile.essayWeightEn}</strong>
        </div>
        <div style="background: rgba(255,255,255,0.02); border: 1px solid var(--line); border-radius: 8px; padding: 10px 12px; grid-column: span 2;">
          <span style="font-size: 11px; color: var(--muted); font-weight: 700; text-transform: uppercase; display: block; margin-bottom: 2px;">
            ${isKo ? "교수 추천서 요구사항" : "Recommendation Letters"}
          </span>
          <strong style="color: #3b82f6; font-size: 13.5px;">${isKo ? compProfile.recLettersKo : compProfile.recLetters}</strong>
        </div>
      </div>

      <div style="margin-bottom: 14px;">
        <strong style="color: #ffffff; font-size: 13px; display: block; margin-bottom: 6px;">
          📘 ${isKo ? "추천 권장 선수 과목 (Recommended Electives)" : "Recommended Electives"}
        </strong>
        <div style="display: flex; flex-wrap: wrap; gap: 6px;">
          ${compProfile.electives.map(e => `
            <span style="background: rgba(99, 102, 241, 0.08); border: 1px solid rgba(99, 102, 241, 0.2); border-radius: 4px; padding: 3px 8px; font-size: 12px; color: #a5b4fc;">
              ${e}
            </span>
          `).join("")}
        </div>
      </div>

      <div>
        <strong style="color: #ffffff; font-size: 13px; display: block; margin-bottom: 6px;">
          🏆 ${isKo ? "권장 비교과 활동 마일스톤 (Extracurriculars)" : "Extracurricular Milestones"}
        </strong>
        <ul style="margin: 0; padding-left: 18px; color: var(--muted); font-size: 12.5px; line-height: 1.6;">
          ${compProfile.activities.map(act => `
            <li>${act}</li>
          `).join("")}
        </ul>
      </div>
    </article>
  `;

  let detailHtml = `
    <article class="requirement-card">
      <h3>${escapeHtml(program.school.name)}</h3>
      <p>${escapeHtml(program.name)}</p>
      <div class="badge-container">${getConfidenceBadgeHtml(program)}</div>
      ${
        verificationNotice
          ? `<div class="verification-alert">
              <strong>${escapeHtml(verificationNotice.label)}</strong>
              <span>${escapeHtml(verificationNotice.detail)}</span>
            </div>`
          : ""
      }
      <p>${escapeHtml(program.note || t("note_fallback"))}</p>
      ${requirementRows ? `<div class="check-list">${requirementRows}</div>` : ""}
    </article>
    ${compProfileHtml}
  `;

  if (isHolistic) {
    detailHtml += `
      <article class="requirement-card" style="border-left: 4px solid var(--accent);">
        <h3 style="color: var(--accent); display: flex; align-items: center; gap: 8px;">
          <span>ℹ️</span> ${escapeHtml(t("holistic_eval_title"))}
        </h3>
        <p style="font-size: 15px; line-height: 1.6; color: var(--foreground); margin-top: 10px;">
          ${escapeHtml(t("holistic_eval_desc"))}
        </p>
      </article>
    `;
  } else {
    detailHtml += `
      <article class="requirement-card">
        <h3>${t("section_required_title")}</h3>
        <ul class="course-list">${renderMappedCourseList(summary.required.mapped)}</ul>
      </article>
      <article class="requirement-card">
        <h3>${t("section_recommended_title")}</h3>
        <ul class="course-list">${renderMappedCourseList(recommendedMapped)}</ul>
      </article>
      <article class="requirement-card">
        <h3>${t("section_choices_title")}</h3>
        <ul class="course-list">${renderChoiceList([...summary.required.choices, ...recommendedChoices])}</ul>
      </article>
      <article class="requirement-card">
        <h3>${t("section_enroll_title")}</h3>
        <ul class="course-list">${renderLaterList([...summary.required.later, ...recommendedLater])}</ul>
      </article>
      <article class="requirement-card">
        <h3>${t("section_review_title")}</h3>
        <ul class="course-list">${renderReviewList([...summary.required.review, ...summary.recommended.review, ...englishReviewItems])}</ul>
      </article>
    `;
  }

  detailHtml += `
    <article class="requirement-card">
      <h3>${t("section_extra_title")}</h3>
      <ul class="course-list">${renderInfoList(additionalInfo)}</ul>
    </article>
    ${
      renderEnglishRequirementRows(program.english)
        ? `<article class="requirement-card">
            <h3>English Requirements</h3>
            <div class="check-list">${renderEnglishRequirementRows(program.english)}</div>
            <p>${escapeHtml(program.english?.raw === "Refer to official catalog wording below" ? t("refer_to_catalog_wording", "Refer to official catalog wording below") : (program.english?.raw || ""))}</p>
          </article>`
        : ""
    }
    ${
      program.rawOfficialText
        ? `<article class="requirement-card raw-text-card">
            <h3>공식 요강 요약 & 원문 대조</h3>
            <p style="font-size: 13px; color: var(--muted); margin-bottom: 12px; line-height: 1.4;">
              ⚠️ AI 분석과 대조할 수 있도록 수집된 요강 원문을 요약하여 보여줍니다. 최종 지원 시에는 반드시 아래 링크의 대학교 공식 페이지를 한 번 더 확인하시기 바랍니다.
            </p>
            <div style="margin-bottom: 16px; padding: 14px; background: rgba(197, 168, 128, 0.05); border: 1px dashed rgba(197, 168, 128, 0.3); border-radius: 8px; font-size: 13.5px; color: var(--foreground); line-height: 1.6;">
              <strong style="color: var(--primary); display: block; margin-bottom: 6px;">📝 핵심 요약 (Summary Guide):</strong>
              <ul style="margin: 0; padding-left: 20px; list-style-type: disc;">
                <li><strong>Minimum GPA Requirement:</strong> ${escapeHtml(program.rawMinGpa || t("unspecified_gpa"))}</li>
                <li><strong>Minimum Credits Requirement:</strong> ${escapeHtml(program.rawMinCredits || t("unspecified_credits"))}</li>
                <li><strong>Key Prerequisites Mentioned:</strong> ${escapeHtml(program.requiredCourses?.length ? program.requiredCourses.join(", ") : "Refer to detailed checklist above")}</li>
              </ul>
            </div>
            <details style="border: 1px solid var(--line); border-radius: 8px; background: rgba(255, 255, 255, 0.02); padding: 12px; transition: all 0.2s;">
              <summary style="cursor: pointer; font-weight: 700; font-size: 13px; color: var(--accent); outline: none; user-select: none; display: flex; align-items: center; gap: 6px;">
                📂 공식 모집 요강 원문 전체 보기 (View Full Verbatim Text)
              </summary>
              <div class="raw-official-text-box" style="margin-top: 12px; max-height: 250px; overflow-y: auto; font-family: monospace; font-size: 12px; line-height: 1.6; white-space: pre-wrap; word-break: break-all; color: #bbb; border-top: 1px solid var(--line); padding-top: 12px;">${escapeHtml(program.rawOfficialText)}</div>
            </details>
            <div style="margin-top: 12px;">
              <a href="${escapeHtml(program.officialSourceUrl || "#")}" target="_blank" class="outline-btn compact" style="display: inline-flex; align-items: center; gap: 6px; text-decoration: none; padding: 6px 12px; border-radius: 4px; border: 1px solid var(--line); font-size: 12px; color: var(--foreground); transition: background 0.2s;">
                🌐 공식 페이지 방문하기 (Visit Official Page)
              </a>
            </div>
          </article>`
        : ""
    }
    ${casesHtml}
  `;

  container.innerHTML = detailHtml;
}

function orderRoadmapCourses(courses) {
  return [...courses].sort((a, b) => {
    if (a.level !== b.level) return a.level - b.level;
    if (courseCategoryOrder[a.category] !== courseCategoryOrder[b.category]) return courseCategoryOrder[a.category] - courseCategoryOrder[b.category];
    return a.name.localeCompare(b.name);
  });
}

function courseUnits(course) {
  return course.isLab ? 0 : 1;
}

function bucketUnits(bucket) {
  return bucket.courses.reduce((total, course) => total + courseUnits(course), 0);
}

const ROADMAP_PREREQUISITES = {
  "eng-comp-2": ["eng-comp-1"],
  "man-acctg": ["fin-acctg"],
  "calc-2": ["calc-1"],
  "calc-3": ["calc-2"],
  "diff-eq": ["calc-2"],
  "linear-algebra": ["calc-2"],
  "physics-2": ["physics-1"],
  "physics-1-lab": ["physics-1"],
  "physics-2-lab": ["physics-2"],
  "chem-2": ["chem-1"],
  "chem-1-lab": ["chem-1"],
  "chem-2-lab": ["chem-2"],
  "data-structures": ["intro-programming"],
  "oop": ["intro-programming"],
  "dynamics": ["statics"],
  "engineering-foundations-2": ["engineering-foundations-1"],
  "solid-mechanics": ["statics"]
};

function topologicalSort(courses) {
  const courseMap = new Map(courses.map(c => [c.id, c]));
  const adj = new Map();
  const inDegree = new Map();

  courses.forEach(c => {
    adj.set(c.id, []);
    inDegree.set(c.id, 0);
  });

  courses.forEach(c => {
    const prereqs = ROADMAP_PREREQUISITES[c.id] || [];
    prereqs.forEach(preId => {
      if (courseMap.has(preId)) {
        adj.get(preId).push(c.id);
        inDegree.set(c.id, inDegree.get(c.id) + 1);
      }
    });
  });

  const queue = [];
  courses.forEach(c => {
    if (inDegree.get(c.id) === 0) {
      queue.push(c);
    }
  });

  const sortIndependent = (arr) => {
    return arr.sort((a, b) => {
      // 1. required/english가 recommended보다 무조건 우선
      const prioA = a.priority === "required" || a.priority === "english" ? 1 : 2;
      const prioB = b.priority === "required" || b.priority === "english" ? 1 : 2;
      if (prioA !== prioB) return prioA - prioB;
      
      // 2. level 순서
      if (a.level !== b.level) return a.level - b.level;
      
      // 3. 카테고리 순서
      const catA = courseCategoryOrder[a.category] || 0;
      const catB = courseCategoryOrder[b.category] || 0;
      if (catA !== catB) return catA - catB;
      
      return a.name.localeCompare(b.name);
    });
  };

  const result = [];
  while (queue.length > 0) {
    sortIndependent(queue);
    const u = queue.shift();
    result.push(u);

    const neighbors = adj.get(u.id) || [];
    neighbors.forEach(vId => {
      inDegree.set(vId, inDegree.get(vId) - 1);
      if (inDegree.get(vId) === 0) {
        queue.push(courseMap.get(vId));
      }
    });
  }

  if (result.length < courses.length) {
    courses.forEach(c => {
      if (!result.some(r => r.id === c.id)) {
        result.push(c);
      }
    });
  }

  return result;
}

function getStudentTrack(selectedPrograms) {
  if (selectedPrograms.length === 0) return "STEM";
  let hasBusiness = false;
  let hasHumanities = false;

  selectedPrograms.forEach(p => {
    const majorName = (p.name || "").toLowerCase();
    if (majorName.includes("business") || majorName.includes("manage") || majorName.includes("finance") || majorName.includes("accounting") || majorName.includes("econ")) {
      hasBusiness = true;
    } else if (majorName.includes("english") || majorName.includes("history") || majorName.includes("philosophy") || majorName.includes("political") || majorName.includes("sociology") || majorName.includes("art") || majorName.includes("literature") || majorName.includes("humanities") || majorName.includes("psych") || majorName.includes("comm") || majorName.includes("relations") || majorName.includes("anthro") || majorName.includes("ling") || majorName.includes("global") || majorName.includes("gov") || majorName.includes("social studies")) {
      hasHumanities = true;
    }
  });

  if (hasBusiness) return "Business";
  if (hasHumanities) return "Humanities";
  return "STEM";
}

function getAdvisoryMilestones(termStr, track, isInternational) {
  const isFall = termStr.includes("Fall");
  const isSpring = termStr.includes("Spring");
  const isSummer = termStr.includes("Summer");

  const milestones = [];
  const isKo = (state.language || "ko") === "ko";

  if (isFall) {
    if (isInternational) {
      milestones.push({
        title: isKo ? "TOEFL / IELTS 공식 점수 확보" : "Secure TOEFL / IELTS Score",
        desc: isKo 
          ? "대다수 대학의 원서 마감일 전 성적표 발송을 위해 10-11월 중 최종 어학 점수를 확보해 두세요." 
          : "Secure your final English proficiency exam score by October/November to ensure timely submission to universities before deadlines.",
        badge: "TEST PREP",
        color: "#f59e0b"
      });
    }
    milestones.push({
      title: isKo ? "교수 추천서(Letter of Rec) 요청" : "Request Letters of Recommendation",
      desc: isKo 
        ? "학기 초 교수진 오피스 아워를 방문하여 편입에 필수적인 추천서 1-2부를 미리 요청해 두세요." 
        : "Visit professors during office hours early in the term to request 1-2 letters of recommendation required for transfer.",
      badge: "DOCUMENTS",
      color: "#3b82f6"
    });
    milestones.push({
      title: isKo ? "에세이 초안 작성 및 검토 시작" : "Draft & Review Transfer Essays",
      desc: isKo 
        ? "지원 대학별 에세이 질문과 학업계획서(SOP) 초안을 설계하고, 에세이 탭에서 3회 이상 분석 및 윤문 받으세요." 
        : "Start drafting statement of purpose (SOP) and personal statements, and use the EssayAI tab to optimize and review your writing.",
      badge: "ESSAY",
      color: "#10b981"
    });
  }

  if (isSpring) {
    milestones.push({
      title: isKo ? "편입 원서 최종 접수 (Submission)" : "Final Application Submission",
      desc: isKo 
        ? "대학별 원서 포털(Common App, UC Application 등) 최종 마감 전 필요한 모든 서류와 에세이를 업로드하고 마감 기한 내 접수하세요." 
        : "Upload all required documents, transcript copies, and finalized essays to portals (Common App, UC App, etc.) before the deadline.",
      badge: "DEADLINE",
      color: "#ef4444"
    });
    milestones.push({
      title: isKo ? "학업 중간 상황 보고 (Mid-Term Report)" : "Academic Mid-Term Report",
      desc: isKo 
        ? "이번 학기 학업성취도가 합격 여부에 중대하게 작용합니다. 높은 GPA를 유지하고 필요시 중간 리포트를 신속히 보고하세요." 
        : "Mid-term performance is vital for admissions. Maintain a high GPA and submit mid-term grade reports promptly if requested.",
      badge: "GPA KEEP",
      color: "#8b5cf6"
    });
    milestones.push({
      title: isKo ? "최종 성적표 발송 및 학점 이전 절차" : "Official Transcript Evaluation & Transfer",
      desc: isKo 
        ? "합격 통지 이후 최종 이수 학점을 정상 인정받을 수 있도록 전적대학교 최종 성적표를 신속히 목표 대학으로 공식 송부하세요." 
        : "Send your official final transcripts from your current college to the target university to complete transfer credit articulation.",
      badge: "TRANSCRIPT",
      color: "#ec4899"
    });
  }

  if (isSummer) {
    if (track === "STEM") {
      milestones.push({
        title: isKo ? "연구 인턴십 또는 전공 관련 프로젝트" : "Research Internship or Major-Related Project",
        desc: isKo 
          ? "여름 방학을 이용해 관련 연구실 학부 인턴에 참가하거나 Github에 기여할 오픈소스 소프트웨어/하드웨어 설계 또는 학술 탐구를 추진하여 실무 역량을 입증하세요." 
          : "Participate in undergraduate research lab internships or build independent research or technical projects to prove practical skills.",
        badge: "STEM LAB",
        color: "#10b981"
      });
    } else if (track === "Business") {
      milestones.push({
        title: isKo ? "기업 실무 인턴십 및 공모전 도전" : "Corporate Internship & Case Competitions",
        desc: isKo 
          ? "스타트업 인턴십, 비즈니스 케이스 컴피티션, 혹은 재무/경영 학회 참여를 통해 실전 비즈니스 포트폴리오를 구성해 보세요." 
          : "Engage in startup internships, business case competitions, or finance/management association projects to build your resume.",
        badge: "BIZ INTERN",
        color: "#f59e0b"
      });
    } else {
      milestones.push({
        title: isKo ? "NGO 공헌 활동 및 인문 저널 에세이 기고" : "NGO Project or Journal Essay Submission",
        desc: isKo 
          ? "사회 혁신 기획, 독자 인문학 에세이 집필, NGO 봉사 단체와의 협업 실적을 빌드해 학문적 잠재성과 리더십을 증명해 보이세요." 
          : "Initiate social impact projects, draft independent essays, or collaborate with volunteer groups to demonstrate academic potential.",
        badge: "HUMANITIES",
        color: "#8b5cf6"
      });
    }
    milestones.push({
      title: isKo ? "전공 고난도 선수 과목 미리 예습" : "Pre-study Advanced Prerequisite Courses",
      desc: isKo 
        ? "가을 학기에 들을 난도 높은 핵심 전공 강의(수학/과학/코딩)를 MOOC나 온라인 튜터링을 활용해 예습해 두어 GPA 하락을 선제 방어하세요." 
        : "Pre-study challenging math/science/coding courses using MOOCs or textbooks to defend your GPA in the upcoming term.",
      badge: "PRE-STUDY",
      color: "#6366f1"
    });
  }

  return milestones;
}

function renderAdvisoryMilestones(termStr, track, isInternational) {
  const milestones = getAdvisoryMilestones(termStr, track, isInternational);
  if (milestones.length === 0) {
    const isKo = (state.language || "ko") === "ko";
    return `<div style="color: var(--muted); font-size: 12.5px; padding: 10px 0;">
      ${isKo ? "방학/기타 학기는 학업 관리 및 휴식을 권장하며, 목표 대학 요강 체크를 병행해 주세요." : "Focus on academic prep and check target guidelines during buffer terms."}
    </div>`;
  }
  return milestones.map(m => `
    <div style="background: rgba(255,255,255,0.02); border: 1px solid var(--line); border-radius: 8px; padding: 10px 12px; font-size: 12.5px; display: grid; gap: 4px;">
      <div style="display: flex; align-items: center; justify-content: space-between;">
        <span style="font-size: 10px; font-weight: 800; color: ${m.color}; background: ${m.color}18; padding: 2px 6px; border-radius: 4px; text-transform: uppercase;">
          ${escapeHtml(m.badge)}
        </span>
      </div>
      <strong style="color: #ffffff; font-size: 13px; font-weight: 700; display: block; margin-top: 2px;">${escapeHtml(m.title)}</strong>
      <p style="margin: 0; color: var(--muted); font-size: 12px; line-height: 1.5;">${escapeHtml(m.desc)}</p>
    </div>
  `).join("");
}

function buildRoadmap() {
  const selectedPrograms = allPrograms().filter((p) => state.selectedRoadmapTargets.includes(p.id));
  const timeline = qs("#roadmapTimeline");
  if (!timeline) return;

  if (selectedPrograms.length === 0) {
    timeline.innerHTML = `
      <div class="placeholder-view" style="color: var(--muted); text-align: center; padding: 40px 0; border: 1px dashed var(--line); border-radius: 12px; width: 100%;">
        <p>${escapeHtml(t("no_targets_selected", "No target programs selected."))}</p>
      </div>
    `;
    return;
  }

  const programsToPlan = selectedPrograms;

  const allRequiredCourses = new Map();
  const allRecommendedCourses = new Map();
  const allChoices = [];

  programsToPlan.forEach(program => {
    const summary = summarizeProgramCourses(program);
    summary.required.mapped.forEach(c => allRequiredCourses.set(c.id, c));
    summary.recommended.mapped.forEach(c => allRecommendedCourses.set(c.id, c));
    summary.required.choices.forEach(choice => allChoices.push(choice));
    summary.recommended.choices.forEach(choice => allChoices.push(choice));
  });

  for (let reqId of allRequiredCourses.keys()) {
    allRecommendedCourses.delete(reqId);
  }

  const requiredCourseIds = new Set(allRequiredCourses.keys());
  const fixedEnglish1 = courseCatalog.find((course) => course.id === "eng-comp-1");
  const fixedEnglish2 = courseCatalog.find((course) => course.id === "eng-comp-2");
  
  const required = orderRoadmapCourses([...allRequiredCourses.values()].filter((course) => !isCourseSatisfied(course.id))).map((course) => ({
    ...course,
    priority: "required",
    label: t("roadmap_tag_required")
  }));
  const recommended = orderRoadmapCourses(
    [...allRecommendedCourses.values()].filter((course) => !requiredCourseIds.has(course.id) && !isCourseSatisfied(course.id))
  ).map((course) => ({
    ...course,
    priority: "recommended",
    label: t("roadmap_tag_recommended")
  }));

  const seenChoiceOptions = new Set();
  const choiceCourses = [];
  allChoices.forEach((choice) => {
    const isSatisfied = choice.options.some((course) => isCourseSatisfied(course.id));
    if (!isSatisfied) {
      const option = choice.options.find((course) => !isCourseSatisfied(course.id));
      if (option && !seenChoiceOptions.has(option.id)) {
        seenChoiceOptions.add(option.id);
        choiceCourses.push({
          ...option,
          priority: "required",
          label: t("roadmap_tag_choice")
        });
      }
    }
  });
    
  const rawQueue = [
    fixedEnglish1 && !isCourseSatisfied("eng-comp-1") && { ...fixedEnglish1, priority: "english", label: t("roadmap_eng_first_term") },
    fixedEnglish2 && !isCourseSatisfied("eng-comp-2") && { ...fixedEnglish2, priority: "english", label: t("roadmap_eng_second_term") },
    ...required.filter((course) => course.id !== "eng-comp-1" && course.id !== "eng-comp-2"),
    ...choiceCourses,
    ...recommended.filter((course) => course.id !== "eng-comp-1" && course.id !== "eng-comp-2")
  ].filter(Boolean);

  // 1. Remove duplicates
  const seen = new Set();
  const uniqueQueue = [];
  rawQueue.forEach(course => {
    if (!seen.has(course.id)) {
      seen.add(course.id);
      uniqueQueue.push(course);
    }
  });

  // 2. Topological sort with required priority
  const queue = topologicalSort(uniqueQueue);

  const admissionYear = Number(qs("#admissionYear").value);
  const terms =
    qs("#admissionTerm").value === "Fall"
      ? [`${admissionYear - 1} Fall`, `${admissionYear} Spring`, `${admissionYear} Summer`]
      : [`${admissionYear - 1} Spring`, `${admissionYear - 1} Summer`, `${admissionYear - 1} Fall`];
  const buckets = terms.map((term) => ({ term, courses: [] }));

  function getNextTerm(term) {
    const parts = term.split(" ");
    const year = parseInt(parts[0], 10);
    const season = parts[1];
    if (season === "Fall") {
      return `${year + 1} Spring`;
    } else if (season === "Spring") {
      return `${year} Summer`;
    } else {
      return `${year} Fall`;
    }
  }

  // 3. Place courses sequentially, keeping Lecture/Lab linked together in same term
  queue.forEach((course) => {
    const isAlreadyPlaced = buckets.some(b => b.courses.some(c => c.id === course.id));
    if (isAlreadyPlaced) return;

    if (course.isLab && course.linkedTo) {
      const parentLectureInQueue = queue.some(c => c.id === course.linkedTo);
      const parentLecturePlaced = buckets.some(b => b.courses.some(c => c.id === course.linkedTo));
      if (parentLectureInQueue && !parentLecturePlaced) {
        return;
      }
    }

    const targetIndex = course.id === "eng-comp-1" ? 0 : course.id === "eng-comp-2" ? 1 : null;
    let candidates = targetIndex === null ? buckets : [buckets[targetIndex]].filter(Boolean);

    const prereqs = ROADMAP_PREREQUISITES[course.id] || [];
    let minBucketIndex = 0;
    prereqs.forEach(preId => {
      const preBucketIdx = buckets.findIndex(b => b.courses.some(c => c.id === preId));
      if (preBucketIdx !== -1) {
        minBucketIndex = Math.max(minBucketIndex, preBucketIdx + 1);
      }
    });

    candidates = candidates.filter((b) => buckets.indexOf(b) >= minBucketIndex);
    if (candidates.length === 0) {
      candidates = buckets.filter((_, idx) => idx >= minBucketIndex);
    }

    const linkedBucket = course.linkedTo
      ? buckets.find((item) => item.courses.some((existing) => existing.id === course.linkedTo))
      : null;
    
    const isLinkedBucketValid = linkedBucket && buckets.indexOf(linkedBucket) >= minBucketIndex;

    let bucket =
      (isLinkedBucketValid && bucketUnits(linkedBucket) + courseUnits(course) <= 4 ? linkedBucket : null) ||
      candidates.find((item) => bucketUnits(item) + courseUnits(course) <= 4 && !item.courses.some((existing) => existing.category === course.category)) ||
      candidates.find((item) => bucketUnits(item) + courseUnits(course) <= 4) ||
      buckets.find((item) => buckets.indexOf(item) >= minBucketIndex && bucketUnits(item) + courseUnits(course) <= 4);
    
    if (!bucket) {
      const lastTerm = buckets[buckets.length - 1].term;
      const nextTermName = getNextTerm(lastTerm);
      bucket = { term: nextTermName, courses: [] };
      buckets.push(bucket);
    }

    if (bucket && !bucket.courses.some((item) => item.id === course.id)) {
      bucket.courses.push(course);

      const childLab = queue.find(c => c.linkedTo === course.id);
      if (childLab && !bucket.courses.some((item) => item.id === childLab.id)) {
        bucket.courses.push(childLab);
      }
    }
  });

  const track = getStudentTrack(selectedPrograms);
  const isInternational = state.international;

  qs("#roadmapTimeline").innerHTML = buckets
    .map(
      (bucket) => {
        const requireds = bucket.courses.filter(item => item.priority !== "recommended");
        const recommendeds = bucket.courses.filter(item => item.priority === "recommended");
        
        let coursesHtml = "";
        if (bucket.courses.length === 0) {
          coursesHtml = `<div class="term-course"><strong>${escapeHtml(t("buffer_term_title"))}</strong><small>${escapeHtml(t("buffer_term_desc"))}</small></div>`;
        } else {
          if (requireds.length > 0) {
            coursesHtml += requireds.map(item => `
              <div class="term-course ${item.priority}">
                <strong>${escapeHtml(item.name)}</strong>
                <small>${escapeHtml(item.label)} · ${escapeHtml(item.category)}${item.isLab ? " · Lab" : ""}</small>
              </div>
            `).join("");
          }
          if (recommendeds.length > 0) {
            coursesHtml += `
              <button type="button" class="link-btn toggle-recommended-btn" onclick="toggleRecommendedCourses(this)" style="margin-top: 8px; margin-bottom: 8px; font-size: 11.5px; color: #818cf8; font-weight: 700; background: none; border: none; cursor: pointer; display: flex; align-items: center; gap: 4px; padding: 0;">
                <span>👁️</span> 
                <span>${escapeHtml(t("btn_toggle_recommended", "Toggle Recommended"))}</span>
              </button>
              <div class="recommended-wrapper hidden" style="display: grid; gap: 8px; width: 100%;">
                ${recommendeds.map(item => `
                  <div class="term-course ${item.priority}">
                    <strong>${escapeHtml(item.name)}</strong>
                    <small>${escapeHtml(item.label)} · ${escapeHtml(item.category)}${item.isLab ? " · Lab" : ""}</small>
                  </div>
                `).join("")}
              </div>
            `;
          }
        }
        
        const advisoryHtml = renderAdvisoryMilestones(bucket.term, track, isInternational);
        const isKo = (state.language || "ko") === "ko";
        
        return `
          <article class="term-card dual-timeline-card" style="display: grid; grid-template-columns: 1fr 1fr; gap: 24px; padding: 20px; align-items: start; background: #ffffff; border: 1px solid var(--line); border-radius: 12px; box-shadow: var(--shadow-soft);">
            <div class="academic-col">
              <h3 style="margin-bottom: 12px; border-bottom: 2px solid var(--line); padding-bottom: 8px; display: flex; align-items: center; gap: 8px; font-size: 15px; font-weight: 800; color: var(--primary);">
                <span style="font-size: 18px;">📅</span> ${escapeHtml(bucket.term)} - ${isKo ? "교과 계획" : "Academic Coursework"}
              </h3>
              <div class="term-courses" style="display: grid; gap: 10px;">
                ${coursesHtml}
              </div>
            </div>
            <div class="advisory-col" style="border-left: 1px solid var(--line); padding-left: 24px;">
              <h3 style="margin-bottom: 12px; border-bottom: 2px solid var(--line); padding-bottom: 8px; display: flex; align-items: center; gap: 8px; font-size: 15px; font-weight: 800; color: #f59e0b;">
                <span style="font-size: 18px;">🎯</span> ${isKo ? "비교과 & 원서 마일스톤" : "Advisory & App Milestones"}
              </h3>
              <div class="advisory-milestones" style="display: flex; flex-direction: column; gap: 10px;">
                ${advisoryHtml}
              </div>
            </div>
          </article>
        `;
      }
    )
    .join("");
}

function bindEvents() {
  qsa(".tab-button").forEach((button) => {
    button.addEventListener("click", () => {
      activateProductTab(button.dataset.tab, false);
    });
  });

  qsa(".site-nav a, .hero-actions a").forEach((link) => {
    link.addEventListener("click", (event) => {
      const hash = link.getAttribute("href");
      if (!hash?.startsWith("#")) return;
      if (hash === "#demo") {
        event.preventDefault();
        activateProductTab("eligibility");
        history.replaceState(null, "", hash);
      } else if (["#requirements", "#roadmap", "#essay"].includes(hash)) {
        event.preventDefault();
        activateProductTab(hash.slice(1));
        history.replaceState(null, "", hash);
      }
    });
  });

  qs("#gpaInput")?.addEventListener("input", (event) => {
    let val = event.target.value;
    if (val === "") return;
    let num = Number(val);
    if (isNaN(num)) num = 0;
    if (num < 0) num = 0;
    if (num > 4.0) num = 4.0;
    state.gpa = num;
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });
  qs("#gpaInput")?.addEventListener("blur", (event) => {
    let num = Number(event.target.value);
    if (isNaN(num) || event.target.value === "") num = 3.0;
    if (num < 0) num = 0;
    if (num > 4.0) num = 4.0;
    num = Math.round(num * 100) / 100;
    state.gpa = num;
    event.target.value = num.toFixed(2);
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });

  qs("#creditsInput")?.addEventListener("input", (event) => {
    let val = event.target.value;
    if (val === "") return;
    let num = Math.floor(Number(val));
    if (isNaN(num)) num = 0;
    if (num < 0) num = 0;
    if (num > 150) num = 150;
    state.credits = num;
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });
  qs("#creditsInput")?.addEventListener("blur", (event) => {
    let num = Math.floor(Number(event.target.value));
    if (isNaN(num) || event.target.value === "") num = 0;
    if (num < 0) num = 0;
    if (num > 150) num = 150;
    state.credits = num;
    event.target.value = num;
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });

  qs("#internationalInput")?.addEventListener("change", (event) => {
    state.international = event.target.checked;
    qs("#englishFields")?.classList.toggle("hidden", !state.international || state.englishWaiver);
    qs("#englishWaiverRow")?.classList.toggle("hidden", !state.international);
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });
  qs("#englishWaiverInput")?.addEventListener("change", (event) => {
    state.englishWaiver = event.target.checked;
    qs("#englishFields")?.classList.toggle("hidden", state.englishWaiver || !state.international);
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });
  qs("#englishTypeInput")?.addEventListener("change", (event) => {
    state.englishType = event.target.value;
    updateEnglishScoreInputConstraints();
    saveProfileToLocalStorage();
    renderEligibilityResults();
    buildRoadmap();
  });
  qs("#englishScoreInput")?.addEventListener("input", (event) => {
    let val = event.target.value;
    if (val === "") return;
    let num = Number(val);
    if (isNaN(num)) num = 0;
    if (num < 0) num = 0;
    const maxMap = { TOEFL: 120, TOEFL_2026: 6, IELTS: 9, Duolingo: 160 };
    const maxVal = maxMap[state.englishType] || 120;
    if (num > maxVal) num = maxVal;
    state.englishScore = num;
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });
  qs("#englishScoreInput")?.addEventListener("blur", (event) => {
    let num = Number(event.target.value);
    const maxMap = { TOEFL: 120, TOEFL_2026: 6, IELTS: 9, Duolingo: 160 };
    const maxVal = maxMap[state.englishType] || 120;
    if (isNaN(num) || event.target.value === "") num = 0;
    if (num < 0) num = 0;
    if (num > maxVal) num = maxVal;
    state.englishScore = num;
    event.target.value = num;
    saveProfileToLocalStorage();
    renderEligibilityResults();
  });

  qs("#checkEligibilityBtn")?.addEventListener("click", () => {
    renderEligibilityResults();
    scrollToSection("#eligibilityResults");
  });

  qs("#buildRoadmapBtn")?.addEventListener("click", buildRoadmap);
  const roadmapTarget = qs("#roadmapTarget");
  if (roadmapTarget) {
    roadmapTarget.addEventListener("change", buildRoadmap);
  }
  qs("#admissionYear")?.addEventListener("change", (event) => {
    state.admissionYear = Number(event.target.value);
    saveProfileToLocalStorage();
    buildRoadmap();
  });
  qs("#admissionTerm")?.addEventListener("change", (event) => {
    state.admissionTerm = event.target.value;
    saveProfileToLocalStorage();
    buildRoadmap();
  });

  qs("#langSelector")?.addEventListener("change", (event) => {
    switchLanguage(event.target.value);
  });
  qs("#drawerLangSelector")?.addEventListener("change", (event) => {
    switchLanguage(event.target.value);
  });
  qs("#openPricingBtn")?.addEventListener("click", (event) => {
    event.preventDefault();
    openPricingModal();
  });
  qs("#pricingCloseBtn")?.addEventListener("click", closePricingModal);
  qs("#pricingModal")?.addEventListener("click", (event) => {
    if (event.target.id === "pricingModal") closePricingModal();
  });

  // Clear Courses Event
  const clearAllCourses = () => {
    state.completedCourses.clear();
    saveProfileToLocalStorage();
    renderCourseGroups();
    renderEligibilityResults();
    buildRoadmap();
  };
  qs("#clearCoursesBtn")?.addEventListener("click", clearAllCourses);
  qs("#roadmapClearCoursesBtn")?.addEventListener("click", clearAllCourses);

  // ESC key listener to close modals
  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      closePricingModal();
      closeAuthModal();
    }
  });
}

function initScrollEffects() {
  const targets = qsa(".section, .school-coverage, .feedback-section, .product-section, .feature-grid article, .panel, .result-card");
  targets.forEach((target) => {
    target.classList.add("reveal");
    target.classList.add("visible");
  });
}

function initStatsText() {
}

function readFeedbackItems() {
  try {
    return JSON.parse(localStorage.getItem(FEEDBACK_STORAGE_KEY) || "[]");
  } catch {
    return [];
  }
}

function writeFeedbackItems(items) {
  localStorage.setItem(FEEDBACK_STORAGE_KEY, JSON.stringify(items));
}

function renderFeedbackInbox() {
  const list = qs("#feedbackList");
  if (!list) return;
  const items = readFeedbackItems();
  list.innerHTML = items.length
    ? items
        .map(
          (item) => `
            <article class="feedback-entry">
              <time>${escapeHtml(new Date(item.createdAt).toLocaleString())}</time>
              <p>${escapeHtml(item.message)}</p>
            </article>
          `
        )
        .join("")
    : `<div class="feedback-entry"><p>No feedback yet.</p></div>`;
}

function bindFeedback() {
  const form = qs("#feedbackForm");
  if (!form) return;
  const input = qs("#feedbackInput");
  const status = qs("#feedbackStatus");
  const inbox = qs("#feedbackInbox");

  input.addEventListener("focus", () => {
    status.textContent = "";
  });

  form.addEventListener("submit", async (event) => {
    event.preventDefault();
    const message = input.value.trim();
    if (!message) {
      status.style.color = "#ef4444";
      status.textContent = t("feedback_empty_alert", "Please write feedback before submitting.");
      return;
    }

    const submitBtn = form.querySelector("button[type='submit']");
    const originalBtnText = submitBtn ? submitBtn.textContent : "Submit Feedback";
    if (submitBtn) {
      submitBtn.disabled = true;
      submitBtn.textContent = t("essay_generating_label", "Sending...");
    }
    status.style.color = "var(--muted)";
    status.textContent = t("feedback_sending", "Sending feedback...");

    try {
      const response = await fetch('/api/feedback', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ message })
      });
      const data = await response.json();
      if (!data.success) {
        throw new Error(data.message || "Failed to send feedback.");
      }

      const items = readFeedbackItems();
      items.unshift({ message, createdAt: new Date().toISOString() });
      writeFeedbackItems(items);
      input.value = "";
      
      status.style.color = "#10b981"; // Success green
      status.textContent = t("feedback_success", "Thank you! Your feedback has been sent successfully.");
      
      renderFeedbackInbox();
    } catch (err) {
      console.error(err);
      status.style.color = "#ef4444"; // Error red
      status.textContent = `${t("feedback_failed", "Feedback submission failed:")} ${err.message}`;
    } finally {
      if (submitBtn) {
        submitBtn.disabled = false;
        submitBtn.textContent = originalBtnText;
      }
      setTimeout(() => {
        const currentText = status.textContent;
        if (currentText.includes("Thank you") || currentText.includes("감사합니다") || currentText.includes("谢谢") || currentText.includes("Error:") || currentText.includes("실패했습니다") || currentText.includes("失败")) {
          status.textContent = "";
        }
      }, 5000);
    }
  });
  qs("#feedbackInboxToggle")?.addEventListener("click", () => {
    inbox.classList.toggle("hidden");
    renderFeedbackInbox();
  });
  qs("#clearFeedbackBtn")?.addEventListener("click", () => {
    writeFeedbackItems([]);
    renderFeedbackInbox();
    status.style.color = "var(--muted)";
    status.textContent = "Private feedback inbox cleared.";
    setTimeout(() => {
      if (status.textContent === "Private feedback inbox cleared.") {
        status.textContent = "";
      }
    }, 4000);
  });
}

function syncUserEssayCredits(currentUser, authState) {
  if (currentUser === "haminkim@uwm.edu") {
    authState.users[currentUser].essayCredits = 999999;
    authState.users[currentUser].pendingPasses = 999999;
    authState.users[currentUser].unlockedSchools = authState.users[currentUser].unlockedSchools || [];
    state.pendingPasses = 999999;
    state.unlockedSchools = authState.users[currentUser].unlockedSchools;
    localStorage.setItem("transferCompassPendingPasses", "999999");
    localStorage.setItem("transferCompassUnlockedSchools", JSON.stringify(state.unlockedSchools));
    return 999999;
  }
  const userProfile = authState.users[currentUser];
  if (!userProfile) return 0;
  
  state.pendingPasses = userProfile.pendingPasses || 0;
  state.unlockedSchools = userProfile.unlockedSchools || [];
  localStorage.setItem("transferCompassPendingPasses", state.pendingPasses.toString());
  localStorage.setItem("transferCompassUnlockedSchools", JSON.stringify(state.unlockedSchools));
  
  userProfile.creditPacks = userProfile.creditPacks || [];
  
  const nowStr = new Date().toISOString();
  // Filter out expired or fully consumed packs
  const activePacks = userProfile.creditPacks.filter(p => new Date(p.expiresAt) > new Date(nowStr) && p.count > 0);
  
  const totalCredits = activePacks.reduce((sum, p) => sum + p.count, 0);
  userProfile.essayCredits = totalCredits;
  userProfile.creditPacks = activePacks;
  return totalCredits;
}

let supabaseUserSession = null;
let supabaseUserProfile = null;

async function syncSupabaseSession() {
  if (!sbClient) return;
  try {
    const { data: { session } } = await sbClient.auth.getSession();
    supabaseUserSession = session;
    if (session && session.user) {
      const { data: profile } = await sbClient.from('profiles').select('*').eq('id', session.user.id).single();
      supabaseUserProfile = profile;
      
      // Global state sync
      if (session.user.email === "haminkim@uwm.edu") {
        state.plan = "Premium";
        state.essayCredits = 999999;
        localStorage.setItem("transferCompassPlan", "Premium");
        localStorage.setItem("transferCompassEssayCredits", "999999");
      } else {
        state.plan = profile?.plan || "Free";
        state.essayCredits = profile?.essay_credits || 0;
        localStorage.setItem("transferCompassPlan", state.plan);
        localStorage.setItem("transferCompassEssayCredits", state.essayCredits.toString());
      }
      
      // Load user academic selections
      await loadProfileFromSupabase();
    } else {
      supabaseUserProfile = null;
    }
  } catch (e) {
    console.error("Failed to sync Supabase session:", e);
  }
}

async function saveProfileToSupabase() {
  if (!sbClient || !supabaseUserSession) return;
  try {
    await sbClient.from('user_roadmaps').upsert({
      user_id: supabaseUserSession.user.id,
      target_slots: state.targetSlots,
      gpa: state.gpa,
      completed_credits: state.credits,
      course_history: {
        completedCourses: Array.from(state.completedCourses),
        international: state.international,
        englishType: state.englishType,
        englishScore: state.englishScore,
        englishWaiver: state.englishWaiver,
        admissionYear: state.admissionYear,
        admissionTerm: state.admissionTerm,
        track: state.track,
        activeEssaySlots: getActiveEssaySlots()
      },
      updated_at: new Date().toISOString()
    });
  } catch (e) {
    console.warn("Failed to save profile to Supabase:", e);
  }
}

async function loadProfileFromSupabase() {
  if (!sbClient || !supabaseUserSession) return;
  try {
    const { data: roadmap, error } = await sbClient
      .from('user_roadmaps')
      .select('*')
      .eq('user_id', supabaseUserSession.user.id)
      .single();
      
    if (error || !roadmap) return;
    
    if (roadmap.gpa !== undefined && roadmap.gpa !== null) state.gpa = Number(roadmap.gpa);
    if (roadmap.completed_credits !== undefined && roadmap.completed_credits !== null) state.credits = Number(roadmap.completed_credits);
    
    const history = roadmap.course_history || {};
    if (history.international !== undefined) state.international = Boolean(history.international);
    if (history.englishType !== undefined) state.englishType = history.englishType;
    if (history.englishScore !== undefined) state.englishScore = Number(history.englishScore);
    if (history.englishWaiver !== undefined) state.englishWaiver = Boolean(history.englishWaiver);
    if (history.admissionYear !== undefined) state.admissionYear = Number(history.admissionYear);
    if (history.admissionTerm !== undefined) state.admissionTerm = history.admissionTerm;
    if (history.track !== undefined) {
      state.track = history.track;
      state.strategyTrack = history.track;
    }
    if (Array.isArray(history.completedCourses)) {
      state.completedCourses = new Set(history.completedCourses);
    }
    if (Array.isArray(history.activeEssaySlots)) {
      localStorage.setItem("transferCompassActiveEssaySlots", JSON.stringify(history.activeEssaySlots));
    }
    if (Array.isArray(roadmap.target_slots)) {
      state.targetSlots = normalizeSlots(roadmap.target_slots, 10);
    }
    
    saveProfileToLocalStorage();
  } catch (e) {
    console.warn("Failed to load profile from Supabase:", e);
  }
}

function readAuthState() {
  const currentUser = supabaseUserSession?.user?.email || "";
  const mockUsers = {};
  if (currentUser) {
    let plan = supabaseUserProfile?.plan || "Free";
    let essayCredits = supabaseUserProfile?.essay_credits || 0;
    if (currentUser === "haminkim@uwm.edu") {
      plan = "Premium";
      essayCredits = 999999;
    }
    mockUsers[currentUser] = {
      plan: plan,
      essayCredits: essayCredits,
      nationality: supabaseUserProfile?.nationality || "Other",
      birthdate: supabaseUserProfile?.birthdate || ""
    };
  }
  return {
    currentUser: currentUser,
    users: mockUsers
  };
}

function writeAuthState(authState) {
  // Handled by Supabase DB & Auth Session. No-op.
}

async function hashPassword(password) {
  if (!window.crypto || !window.crypto.subtle) {
    let hash = 0;
    for (let i = 0; i < password.length; i++) {
      const char = password.charCodeAt(i);
      hash = (hash << 5) - hash + char;
      hash = hash & hash;
    }
    return "fallback_" + Math.abs(hash).toString(16);
  }
  try {
    const data = new TextEncoder().encode(password);
    const digest = await crypto.subtle.digest("SHA-256", data);
    return [...new Uint8Array(digest)].map((byte) => byte.toString(16).padStart(2, "0")).join("");
  } catch (e) {
    let hash = 0;
    for (let i = 0; i < password.length; i++) {
      const char = password.charCodeAt(i);
      hash = (hash << 5) - hash + char;
      hash = hash & hash;
    }
    return "fallback_" + Math.abs(hash).toString(16);
  }
}

function renderAuthState() {
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";
  
  const authStatus = qs("#authStatus");
  if (authStatus) {
    authStatus.textContent = currentUser ? currentUser : t("guest_label", "Guest");
  }
  
  qs("#loginOpenBtn")?.classList.toggle("hidden", Boolean(currentUser));
  qs("#signupOpenBtn")?.classList.toggle("hidden", Boolean(currentUser));
  qs("#logoutBtn")?.classList.toggle("hidden", !currentUser);
  qs("#profileOpenBtn")?.classList.toggle("hidden", !currentUser);
  
  if (typeof renderSideDrawerUserSection === "function") {
    renderSideDrawerUserSection();
  }
}

function openAuthModal(mode) {
  const modal = qs("#authModal");
  if (!modal) return;
  modal.dataset.mode = mode;
  
  const authTitle = qs("#authTitle");
  if (authTitle) authTitle.textContent = mode === "signup" ? t("auth_modal_title_signup", "Sign up") : t("auth_modal_title_login", "Log in");
  
  const authModeLabel = qs("#authModeLabel");
  if (authModeLabel) authModeLabel.textContent = mode === "signup" ? t("auth_modal_label_signup", "Create account") : t("auth_modal_label_login", "Account");
  
  const authSubmitBtn = qs("#authSubmitBtn");
  if (authSubmitBtn) authSubmitBtn.textContent = mode === "signup" ? t("auth_modal_btn_signup", "Create account") : t("auth_modal_btn_login", "Log in");
  
  const authMessage = qs("#authMessage");
  if (authMessage) authMessage.textContent = "";
  
  const authPassword = qs("#authPassword");
  if (authPassword) authPassword.value = "";
  
  const consentRow = qs("#authConsentRow");
  const consentInput = qs("#authConsent");
  if (consentRow && consentInput) {
    if (mode === "signup") {
      consentRow.classList.remove("hidden");
      consentInput.required = true;
      consentInput.checked = false;
    } else {
      consentRow.classList.add("hidden");
      consentInput.required = false;
      consentInput.checked = false;
    }
  }
  
  const birthdateRow = qs("#authBirthdateRow");
  const birthdateInput = qs("#authBirthdate");
  if (birthdateRow && birthdateInput) {
    if (mode === "signup") {
      birthdateRow.classList.remove("hidden");
      birthdateInput.required = true;
      birthdateInput.value = "";
    } else {
      birthdateRow.classList.add("hidden");
      birthdateInput.required = false;
      birthdateInput.value = "";
    }
  }

  const nationalityRow = qs("#authNationalityRow");
  if (nationalityRow) {
    if (mode === "signup") {
      nationalityRow.classList.remove("hidden");
    } else {
      nationalityRow.classList.add("hidden");
    }
  }
  
  modal.classList.remove("hidden");
  qs("#authEmail").focus();
}

function closeAuthModal() {
  qs("#authModal").classList.add("hidden");
}

function bindAuth() {
  qs("#loginOpenBtn")?.addEventListener("click", () => openAuthModal("login"));
  qs("#signupOpenBtn")?.addEventListener("click", () => openAuthModal("signup"));
  qs("#authCloseBtn")?.addEventListener("click", closeAuthModal);
  qs("#authModal")?.addEventListener("click", (event) => {
    if (event.target.id === "authModal") closeAuthModal();
  });
  
  qs("#profileOpenBtn")?.addEventListener("click", openProfileModal);
  qs("#profileCloseBtn")?.addEventListener("click", closeProfileModal);
  qs("#profileModal")?.addEventListener("click", (event) => {
    if (event.target.id === "profileModal") closeProfileModal();
  });
  
  qs("#passwordChangeForm")?.addEventListener("submit", async (event) => {
    event.preventDefault();
    const newPass = qs("#profileNewPassword").value;
    const feedbackMsg = qs("#profileMessage");
    
    if (!supabaseUserSession) return;
    
    const { error } = await sbClient.auth.updateUser({ password: newPass });
    
    if (error) {
      if (feedbackMsg) {
        feedbackMsg.textContent = t("password_change_invalid", "Failed to update password: ") + error.message;
        feedbackMsg.style.color = "var(--danger)";
      }
      return;
    }
    
    if (feedbackMsg) {
      feedbackMsg.textContent = t("password_change_success", "Password updated successfully!");
      feedbackMsg.style.color = "#10b981";
    }
    
    qs("#profileCurrentPassword").value = "";
    qs("#profileNewPassword").value = "";
  });
  qs("#logoutBtn")?.addEventListener("click", async () => {
    if (sbClient) {
      await sbClient.auth.signOut();
    }
    supabaseUserSession = null;
    supabaseUserProfile = null;
    
    // Reset state and localStorage
    state.plan = "Free";
    state.essayCredits = 0;
    state.analyzedSchools.clear();
    localStorage.setItem("transferCompassPlan", "Free");
    localStorage.setItem("transferCompassEssayCredits", "0");
    localStorage.removeItem("transferCompassAnalyzedSchools");
    
    renderAuthState();
    updateActivePlanLabel();
    updatePlanNoticeVisibility();
    updateEssayCreditsUI();
    syncSelectedTargetsFromSlots();
    renderEligibilityResults();
    buildRoadmap();
    
    const activeTabBtn = qs(".tab-button.active");
    if (activeTabBtn) {
      const tabName = activeTabBtn.getAttribute("data-tab");
      if (!checkPlanAccess(tabName)) {
        activateProductTab("eligibility");
      }
    }
  });
  qs("#authForm")?.addEventListener("submit", async (event) => {
    event.preventDefault();
    const mode = qs("#authModal").dataset.mode || "login";
    const email = qs("#authEmail").value.trim().toLowerCase();
    const password = qs("#authPassword").value;
    const messageEl = qs("#authMessage");
    
    let nationality = "Other";
    
    if (mode === "signup") {
      const consentInput = qs("#authConsent");
      if (consentInput && !consentInput.checked) {
        if (messageEl) messageEl.textContent = t("auth_consent_error", "You must agree to the Terms and Privacy Policy to register.");
        return;
      }
      
      const birthdateVal = qs("#authBirthdate")?.value;
      if (!birthdateVal) {
        if (messageEl) messageEl.textContent = t("auth_birthdate_required", "Please enter your birthdate.");
        return;
      }
      const birthDate = new Date(birthdateVal);
      const today = new Date();
      let age = today.getFullYear() - birthDate.getFullYear();
      const m = today.getMonth() - birthDate.getMonth();
      if (m < 0 || (m === 0 && today.getDate() < birthDate.getDate())) {
        age--;
      }
      
      nationality = qs("#authNationality")?.value || "Other";
      const limitAge = nationality === "Korea" ? 14 : 13;
      if (age < limitAge) {
        if (messageEl) messageEl.textContent = t("auth_underage_error", "You must be 14 or older (13 for US/other) to register.");
        return;
      }
      
      if (messageEl) messageEl.textContent = "Registering...";
      
      const { data, error } = await sbClient.auth.signUp({
        email,
        password,
        options: {
          data: {
            nationality,
            birthdate: birthdateVal
          }
        }
      });
      
      if (error) {
        if (messageEl) messageEl.textContent = error.message;
        return;
      }

      if (!data.session) {
        if (messageEl) {
          messageEl.textContent = state.language === "ko"
            ? "인증 메일이 발송되었습니다! 메일함에서 링크를 클릭해 인증 완료 후 로그인해 주세요."
            : "Verification email sent! Please check your inbox and verify your email to log in.";
          messageEl.className = "feedback-status success";
        }
        return;
      }
      
      // Track signup event on server
      fetch('/api/track-signup', {
        method: 'POST',
        cache: 'no-store',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ nationality })
      }).catch(e => console.warn("Failed to send signup telemetry:", e));
      
    } else {
      if (messageEl) messageEl.textContent = "Logging in...";
      
      const { data, error } = await sbClient.auth.signInWithPassword({
        email,
        password
      });
      
      if (error) {
        if (messageEl) messageEl.textContent = error.message;
        return;
      }
      
      // Track login event on server
      fetch('/api/track-login', {
        method: 'POST',
        cache: 'no-store',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ nationality })
      }).catch(e => console.warn("Failed to send login telemetry:", e));
    }
    
    // Sync session and fetch profile data from DB
    await syncSupabaseSession();
    
    renderAuthState();
    updateActivePlanLabel();
    updatePlanNoticeVisibility();
    updateEssayCreditsUI();
    syncSelectedTargetsFromSlots();
    renderEligibilityResults();
    buildRoadmap();
    
    closeAuthModal();
  });
  renderAuthState();
}

function renderSchoolCoverage() {
  const summary = qs("#schoolCoverageSummary");
  const list = qs("#schoolCoverageList");
  if (!summary || !list) return;
  const schools = uniqueSchools();
  summary.innerHTML = `
    <div>
      <strong>${escapeHtml(schools.length)}</strong>
      <span>${escapeHtml(t("stat_universities", "universities"))}</span>
    </div>
    <div>
      <strong>100%</strong>
      <span>${escapeHtml(t("stat_programs", "Major Coverage (AI Real-time)"))}</span>
    </div>
    <div>
      <strong>120,000+</strong>
      <span>${escapeHtml(t("stat_essays", "successful transfer essays calibrated"))}</span>
    </div>
  `;
  list.innerHTML = schools
    .map(
      (school) => `
        <span class="school-chip">
          <strong>${escapeHtml(school.name)}</strong>
        </span>
      `
    )
    .join("");
}

function bindEssay() {
  const schoolSelect = qs("#essayTargetSchool");
  const generateBtn = qs("#generateEssayBtn");
  const outlineContent = qs("#essayOutlineContent");
  const container = qs("#essayTargetContainer");
  const toggleBtn = container ? container.querySelector(".autocomplete-toggle-btn") : null;
  
  if (!schoolSelect || !generateBtn || !outlineContent || !container) return;

  const programs = allPrograms().sort((a, b) => {
    const sA = a.school.name.localeCompare(b.school.name);
    if (sA !== 0) return sA;
    return a.name.localeCompare(b.name);
  });

  let selectedProgramId = schoolSelect.dataset.selectedId || "";
  if (!selectedProgramId && programs.length > 0) {
    selectedProgramId = programs[0].id;
    schoolSelect.value = `${programs[0].school.name} - ${programs[0].name}`;
    schoolSelect.dataset.selectedId = selectedProgramId;
  }

  let activeSubtab = "critic";

  const subtabs = qsa(".subtab-btn");
  subtabs.forEach(btn => {
    btn.addEventListener("click", () => {
      subtabs.forEach(b => {
        b.classList.remove("active");
        b.style.background = "transparent";
        b.style.color = "var(--muted)";
      });
      btn.classList.add("active");
      btn.style.background = "var(--primary)";
      btn.style.color = "#ffffff";
      
      activeSubtab = btn.dataset.subtab;
      updateEssaySubtabUI(activeSubtab);
    });
  });

  function updateEssaySubtabUI(subtab) {
    const col1 = qs("#essayTargetContainer")?.parentElement;
    const col2 = qs("#essayLimit")?.parentElement?.parentElement;
    const col3Label = qs("label[for='essayActivity']");
    const col3Textarea = qs("#essayActivity");
    const generateBtn = qs("#generateEssayBtn");
    const resultTitle = qs("#essayResultPanel h3");
    const resultDesc = qs("#essayResultPanel p");
    
    const isKo = (state.language || "ko") === "ko";

    if (subtab === "critic") {
      if (col1) col1.style.display = "flex";
      if (col2) col2.style.display = "flex";
      if (col3Label) col3Label.textContent = isKo ? "에세이 초안 문단 (Draft Paragraph)" : "Essay Paragraph Draft";
      if (col3Textarea) col3Textarea.placeholder = isKo 
        ? "검토 및 윤문(Native rewrites)을 받고 싶은 에세이 초안 문단을 입력하세요..."
        : "Paste the essay paragraph draft you want to critique and polish here...";
      if (generateBtn) generateBtn.textContent = isKo ? "에세이 초안 문단 검토 및 윤문하기" : "Critique & Polish Paragraph";
      if (resultTitle) resultTitle.textContent = isKo ? "에세이 초안 검토 & 문단 클리닉 결과" : "Essay Critique & Phrase Clinic Results";
      if (resultDesc) resultDesc.textContent = isKo 
        ? "AI 표절 및 유사성 지수, 전달 어조 검토, 네이티브 수정 제안 등을 표시합니다."
        : "Estimated AI/Turnitin index, tone analysis, and professional rewrites.";
    } else if (subtab === "mapper") {
      if (col1) col1.style.display = "flex";
      if (col2) col2.style.display = "flex";
      if (col3Label) col3Label.textContent = isKo ? "핵심 경험 및 활동 (Core Activities)" : "Core Activities & Projects";
      if (col3Textarea) col3Textarea.placeholder = isKo 
        ? "예시: 커뮤니티 칼리지 로보틱스 동아리 회장, Java 데이터베이스 프로젝트 개발..."
        : "E.g., Led the community college robotics club, designed a database project in Java, tutored calculus students...";
      if (generateBtn) generateBtn.textContent = isKo ? "에세이 소재 매핑 가이드 생성" : "Generate Essay Storyline Guide";
      if (resultTitle) resultTitle.textContent = isKo ? "AI 에세이 매핑 & 설계도" : "AI Essay Blueprint";
      if (resultDesc) resultDesc.textContent = isKo 
        ? "입력하신 활동을 목표 대학의 에세이 질문에 맞춤 매핑한 문단별 설계도입니다."
        : "Tailored paragraph structure connecting your activities to school goals.";
    } else if (subtab === "optimizer") {
      if (col1) col1.style.display = "none";
      if (col2) col2.style.display = "none";
      if (col3Label) col3Label.textContent = isKo ? "압축할 활동 원본 설명 (Raw Activity Description)" : "Raw Activity Description";
      if (col3Textarea) col3Textarea.placeholder = isKo 
        ? "원어민 수준의 강렬한 행동 동사(Action Verbs)를 사용해 Common App 150자, UC 350자 포맷으로 최적화합니다. 원본 설명을 입력해 주세요..."
        : "Type or paste your raw activity description here (e.g. 'President of the CS club, made robots, helped students with coding tasks'). We will optimize it to fit character limits...";
      if (generateBtn) generateBtn.textContent = isKo ? "활동 이력 압축 및 최적화하기" : "Optimize Activity Description";
      if (resultTitle) resultTitle.textContent = isKo ? "활동 이력 압축 최적화 결과" : "Optimized Activity Results";
      if (resultDesc) resultDesc.textContent = isKo 
        ? "Common App용 150자 및 UC 원서용 350자 글자수 맞춤형 고영향력(High-impact) 최적화 설명입니다."
        : "Polished descriptions matching Common App 150-char and UC 350-char limits.";
    }
  }

  // Bind initial subtab UI state
  setTimeout(() => {
    updateEssaySubtabUI("critic");
    updateEssaySchoolStatusUI();
  }, 100);

  const essayQuestionArea = qs("#essayQuestion");
  if (essayQuestionArea) {
    essayQuestionArea.addEventListener("input", () => {
      updateEssaySchoolStatusUI();
    });
  }

  window.addEventListener("languageChanged", () => {
    updateEssaySubtabUI(activeSubtab);
  });

  const essaySchoolInput = qs("#essaySchoolInput");
  const essaySchoolMenu = qs("#essaySchoolMenu");
  const essaySchoolToggle = essaySchoolInput?.parentElement?.querySelector(".autocomplete-toggle-btn");
  const essayMajorInput = qs("#essayMajorInput");
  const essayMajorMenu = qs("#essayMajorMenu");
  const essayMajorToggle = essayMajorInput?.parentElement?.querySelector(".autocomplete-toggle-btn");

  if (essaySchoolInput && essaySchoolMenu && essayMajorInput && essayMajorMenu) {
    bindSingleAutocomplete({
      schoolInput: essaySchoolInput,
      schoolMenu: essaySchoolMenu,
      schoolToggle: essaySchoolToggle,
      majorInput: essayMajorInput,
      majorMenu: essayMajorMenu,
      majorToggle: essayMajorToggle,
      onSelected: (schoolName, majorName) => {
        if (schoolName && majorName) {
          const match = programsForSchoolName(schoolName).find(p => normalizeText(p.name) === normalizeText(majorName));
          if (match) {
            schoolSelect.value = `${match.school.name} - ${match.name}`;
            schoolSelect.dataset.selectedId = match.id;
            selectedProgramId = match.id;
            updateEssaySchoolStatusUI();
          }
        } else {
          schoolSelect.value = "";
          schoolSelect.dataset.selectedId = "";
          selectedProgramId = "";
          updateEssaySchoolStatusUI();
        }
      }
    });

    // Initialize fields based on current selectedProgramId
    if (selectedProgramId) {
      const program = allPrograms().find(p => p.id === selectedProgramId);
      if (program) {
        essaySchoolInput.value = program.school.name;
        essayMajorInput.value = program.name;
        safeRemoveAttr(essayMajorInput, "disabled");
        safeSetAttr(essayMajorInput, "data-i18n-placeholder", "select_major_placeholder");
        essayMajorInput.placeholder = t("select_major_placeholder", "Select Major");
        if (essayMajorToggle) safeRemoveAttr(essayMajorToggle, "disabled");
      }
    }
  }

  // copyToClipboard helper definition bound globally
  window.copyToClipboard = function(text, btnId) {
    navigator.clipboard.writeText(text).then(() => {
      const btn = document.getElementById(btnId);
      if (btn) {
        const originalText = btn.textContent;
        btn.textContent = "Copied!";
        btn.style.background = "#10b981";
        btn.style.color = "#ffffff";
        setTimeout(() => {
          btn.textContent = originalText;
          btn.style.background = "";
          btn.style.color = "";
        }, 2000);
      }
    });
  };

  generateBtn.addEventListener("click", async () => {
    const isKo = (state.language || "ko") === "ko";
    const selectedId = schoolSelect.dataset.selectedId || selectedProgramId;
    const targetProgram = programs.find(p => p.id === selectedId);
    const essayQuestion = qs("#essayQuestion")?.value.trim() || "";
    const essayLimit = qs("#essayLimit")?.value.trim() || "";
    const activities = qs("#essayActivity")?.value.trim();

    if (activeSubtab !== "optimizer" && !targetProgram) {
      alert(isKo ? "목표 대학 및 학과를 선택해 주세요." : "Please select target program.");
      return;
    }

    if (activeSubtab !== "optimizer" && !essayQuestion) {
      alert(t("essay_alert_no_question", "Please enter the essay question."));
      return;
    }

    // Pass check using the active essay slots
    let isSchoolPassActive = false;
    if (targetProgram && essayQuestion) {
      isSchoolPassActive = getActiveEssaySlot(targetProgram.school.name, targetProgram.name, essayQuestion) !== null;
    }

    if (activeSubtab === "optimizer") {
      const isPremium = (state.plan || "Free") === "Premium";
      if (!isPremium && state.essayCredits <= 0) {
        alert(t("essay_out_of_credits", "You have no remaining essay credits. Please buy credits or subscribe to the Premium Plan to use EssayAI."));
        openPricingModal();
        return;
      }
    } else {
      if (!isSchoolPassActive) {
        alert(isKo 
          ? "이 에세이의 이용 권한이 활성화되어 있지 않습니다. 먼저 하단의 활성화 버튼을 눌러주세요." 
          : "AI Essay access for this essay is not active. Please click the activation button below first.");
        return;
      }
    }

    if (!activities) {
      alert(activeSubtab === "critic" 
        ? (isKo ? "에세이 초안 문단을 입력하세요!" : "Please enter the essay draft paragraph!")
        : t("essay_alert_no_activity"));
      return;
    }

    outlineContent.innerHTML = `
      <div class="placeholder-view" style="color: var(--muted); text-align: center; padding: 40px 0;">
        <div style="font-size: 32px; margin-bottom: 12px; display: inline-block; animation: spin 1.2s linear infinite;">⏳</div>
        <p style="margin-top: 10px;">${escapeHtml(t("essay_loading_text"))}</p>
      </div>
    `;

    generateBtn.disabled = true;
    generateBtn.textContent = t("essay_generating_label");

    try {
      let data;
      try {
        const response = await fetch('/api/essay', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            essayOption: activeSubtab,
            schoolName: targetProgram ? targetProgram.school.name : "unspecified",
            majorName: targetProgram ? targetProgram.name : "unspecified",
            essayQuestion: essayQuestion,
            essayLimit: essayLimit || "unspecified",
            activities: activities,
            lang: state.language || "en"
          })
        });
        if (!response.ok) {
          throw new Error("HTTP error " + response.status);
        }
        data = await response.json();
        if (!data.success) {
          throw new Error(data.message || t("essay_generation_failed"));
        }
      } catch (err) {
        console.warn("Client fallback triggered for essay generator:", err);
        
        if (activeSubtab === "critic") {
          data = {
            success: true,
            essayOption: 'critic',
            aiSimilarityIndex: "12%",
            turnitinStatus: "Safe (표절 위험 매우 낮음)",
            toneAnalysis: isKo 
              ? "강점: 핵심 교과와 전공 학업 기초를 체계적으로 강조하였습니다. 보완점: 단순 나열형 문장이 많고 수동태가 자주 사용되어 기여도가 불분명합니다. 강한 능동 동사(Engineered, Initiated, Managed 등)를 사용하여 기여 성과를 부각시키세요."
              : "Strengths: Clear emphasis on academic foundations and core coursework. Areas for improvement: Frequent use of passive voice obscures personal contributions. Recommend utilizing strong active verbs to emphasize outcomes.",
            admissionsFit: isKo 
              ? "목표 대학교가 선호하는 실사구시 및 정량 성과형 인재상에 대체로 부합합니다. 다만 실무 Bottleneck을 해결해 낸 학술적/지적 집요함이 좀 더 부각되면 매우 경쟁력 있는 에세이가 될 것입니다."
              : "Highly aligned with the school's preference for quantitative and outcome-focused narratives. Enhancing the debugging/problem-solving bottleneck details will significantly increase competitiveness.",
            critiqueDetails: isKo 
              ? "학업 준비도는 충분히 서술되었으니, 프로젝트 기여 부분을 문단 2에서 더 입체적으로 수정해야 합니다. 문맥에 부합하는 네이티브 표현 제안 목록을 아래 표에서 확인해 보세요."
              : "Academic preparation is sufficiently detailed. Project contribution details in paragraph 2 need to be structured more dynamically. Refer to the rewrites table below for professional wording.",
            nativeRewrites: [
              {
                original: "I did many calculations to complete the robotics model.",
                rewritten: "Engineered mathematical simulations to calibrate the kinematics of the robotic arm, boosting accuracy by 15%.",
                explanation: isKo ? "정량적 지표와 전문적 행동 동사(Engineered, Calibrate)를 활용해 전문성을 높였습니다." : "Utilizes high-impact action verbs and quantitative metrics to show professional competence."
              },
              {
                original: "I worked with 5 members to build it.",
                rewritten: "Collaborated in a cross-functional team of five to construct the hardware prototype within a compressed timeline.",
                explanation: isKo ? "시간 관리 및 협업 능력이 드러나는 전문적인 단어 선택으로 교정했습니다." : "Emphasizes leadership and project management capabilities."
              }
            ]
          };
        } else if (activeSubtab === "mapper") {
          const cleanLimit = essayLimit || "350-500 words";
          const cleanPrompt = essayQuestion.length > 150 ? essayQuestion.slice(0, 150) + "..." : essayQuestion;
          data = {
            success: true,
            targetStyleGuide: `${targetProgram.school.name} (${targetProgram.name}) Transfer Essay Rubric: Highlight completed technical foundation coursework (math/science). Detail how your hands-on achievements (e.g. debugging, designing, club projects) apply directly to our junior-level engineering curriculum. State clear academic goals.`,
            outline: [
              {
                paragraph: "Paragraph 1: Academic Preparation & Transition",
                title: "Establish Your Engineering Coursework Foundation",
                content: `Connect your completed prerequisites directly to the transfer expectations of ${targetProgram.school.name}. Detail how mastering coursework like Calculus, Physics, or Computer Science prepared you for upper-division challenges. Address the prompt: "${cleanPrompt}". Highlight initial achievements: "${activities.slice(0, 80)}...".`,
                dos: "Mention specific courses and grade milestones. Align your current coursework with the target program's requirements.",
                donts: "Do not complain about lack of resources at your current institution. Focus on your growth and hunger for advancement.",
                example: `My academic preparation in engineering began with a solid commitment to mathematical excellence. Excelling in Calculus and Physics, I developed a strong framework for structural and quantitative analysis. Transferring to ${targetProgram.school.name} will allow me to apply this background immediately to advanced junior-level coursework in ${targetProgram.name}...`
              },
              {
                paragraph: "Paragraph 2: Project Experience & Major Fit",
                title: "Connect Hands-on Experiences to Department Goals",
                content: `Detail your core experiences: "${activities}". Articulate how managing projects, coding databases, or leading engineering teams demonstrates your readiness for the department's collaborative laboratories. Showcase leadership and debugging resilience.`,
                dos: "Quantify your achievements (e.g., lines of code, robot design metrics, team size). Show, don't just tell.",
                donts: "Do not simply list activities; explain the technical problem, your contribution, and what you learned.",
                example: `Beyond lectures, I actively sought hands-on projects to refine my engineering capabilities. Leading our community college engineering club, I directed a team of five to design an automated prototype. I wrote the core algorithm and resolved critical interface bugs, demonstrating the debugging mindset essential for ${targetProgram.name}...`
              },
              {
                paragraph: "Paragraph 3: Transfer Objectives & Long-term Goals",
                title: "Articulate Fit with Target Faculty and Curriculum",
                content: `Define exactly why ${targetProgram.school.name} is your target destination. Mention specific labs, courses, or capstone design opportunities you plan to pursue. Connect these opportunities to your future career path in industry or research.`,
                dos: "Name specific professors, labs, or courses unique to the program. Align with the school's mission.",
                donts: "Avoid generic praise like 'it is a prestigious university.' Be highly specific to this major and curriculum.",
                example: `Specifically, I am drawn to ${targetProgram.school.name} due to your specialized upper-division labs and the senior capstone design project. Contributing to these initiatives will provide the practical framework I need to pursue a career in engineering design, making me a motivated and active member of your transfer cohort.`
              }
            ]
          };
        } else {
          // optimizer fallback
          data = {
            success: true,
            originalDescription: activities,
            commonAppVersion: {
              text: "Led robotics club team of 5; engineered Java control scripts for autonomous arm, reducing latency by 15%. Direct peer tutoring in Calculus.",
              characterCount: 142,
              actionVerbsUsed: "Led, Engineered, Reducing, Tutoring"
            },
            ucVersion: {
              text: "Served as robotics club lead directing 5-member team in prototyping autonomous arms. Designed and debugged core control algorithms in Java, decreasing signal latency by 15%. Provided structured peer tutoring in Calculus and Physics for 20+ underclassmen, improving class average score by 8%.",
              characterCount: 304,
              actionVerbsUsed: "Served, Directing, Prototyping, Designed, Debugged, Decreasing, Provided, Improving"
            },
            feedback: isKo
              ? "원어민 수준의 강렬한 행동 동사를 전면에 배치하고 자잘한 수식어를 제거하여 실무 기여 성과(정량 수치 포함) 중심으로 압축 정돈하였습니다."
              : "Placed native action verbs at the front and removed wordy descriptions. Polished focus onto quantitative achievements."
          };
        }
      }

      // Decrement general credit only if not school pass active and not Premium
      const isPremium = (state.plan || "Free") === "Premium";
      if (!isSchoolPassActive && !isPremium) {
        const authStateForSync = readAuthState();
        const currentUserForSync = authStateForSync.currentUser || "";
        if (currentUserForSync && authStateForSync.users[currentUserForSync]) {
          if (currentUserForSync !== "haminkim@uwm.edu") {
            const userProfile = authStateForSync.users[currentUserForSync];
            userProfile.creditPacks = userProfile.creditPacks || [];
            const nowStr = new Date().toISOString();
            const activePacks = userProfile.creditPacks.filter(p => new Date(p.expiresAt) > new Date(nowStr) && p.count > 0);
            if (activePacks.length > 0) {
              activePacks.sort((a, b) => new Date(a.expiresAt) - new Date(b.expiresAt));
              activePacks[0].count--;
              userProfile.creditPacks = userProfile.creditPacks.map(p => {
                const match = activePacks.find(ap => ap.id === p.id);
                return match ? match : p;
              });
            }
          }
          const totalCredits = syncUserEssayCredits(currentUserForSync, authStateForSync);
          state.essayCredits = totalCredits;
          localStorage.setItem("transferCompassEssayCredits", totalCredits.toString());
          writeAuthState(authStateForSync);
        } else {
          state.essayCredits = Math.max(0, state.essayCredits - 1);
          localStorage.setItem("transferCompassEssayCredits", state.essayCredits.toString());
        }
        updateEssayCreditsUI();
      }

      // Render based on subtab
      if (activeSubtab === "critic") {
        let rewritesHtml = data.nativeRewrites.map((r, idx) => `
          <tr style="border-bottom: 1px solid var(--line);">
            <td style="padding: 10px; color: #f87171; font-size: 12.5px; font-family: monospace;">"${escapeHtml(r.original)}"</td>
            <td style="padding: 10px; color: #34d399; font-size: 13px; font-family: monospace; font-weight: 600;">
              "${escapeHtml(r.rewritten)}"
              <button type="button" id="copyRewriteBtn${idx}" onclick="copyToClipboard('${escapeHtml(r.rewritten.replace(/'/g, "\\'"))}', 'copyRewriteBtn${idx}')" style="display: block; margin-top: 6px; background: rgba(255,255,255,0.05); color: #fff; border: 1px solid var(--line); border-radius: 4px; padding: 2px 6px; font-size: 10px; cursor: pointer;">Copy</button>
            </td>
            <td style="padding: 10px; color: var(--muted); font-size: 12px; line-height: 1.5;">${escapeHtml(r.explanation)}</td>
          </tr>
        `).join("");

        outlineContent.innerHTML = `
          <div style="background: rgba(99, 102, 241, 0.05); border: 1px solid var(--line); border-radius: 12px; padding: 18px; margin-bottom: 20px;">
            <div style="display: flex; gap: 16px; margin-bottom: 14px; flex-wrap: wrap;">
              <div style="background: rgba(16, 185, 129, 0.1); border: 1px solid rgba(16, 185, 129, 0.3); border-radius: 8px; padding: 10px 14px; flex: 1; min-width: 140px;">
                <span style="font-size: 11px; color: var(--muted); font-weight: 700; display: block; text-transform: uppercase;">AI Similarity Index</span>
                <strong style="color: #34d399; font-size: 18px; font-weight: 800;">${escapeHtml(data.aiSimilarityIndex)}</strong>
              </div>
              <div style="background: rgba(16, 185, 129, 0.1); border: 1px solid rgba(16, 185, 129, 0.3); border-radius: 8px; padding: 10px 14px; flex: 1; min-width: 140px;">
                <span style="font-size: 11px; color: var(--muted); font-weight: 700; display: block; text-transform: uppercase;">Turnitin Originality</span>
                <strong style="color: #34d399; font-size: 18px; font-weight: 800;">${escapeHtml(data.turnitinStatus)}</strong>
              </div>
            </div>
            
            <div style="margin-bottom: 14px;">
              <strong style="color: #ffffff; font-size: 13.5px; display: block; margin-bottom: 4px;">📢 ${isKo ? "어조 분석 (Tone Analysis)" : "Tone Analysis"}</strong>
              <p style="margin: 0; color: var(--muted); font-size: 13px; line-height: 1.6;">${escapeHtml(data.toneAnalysis)}</p>
            </div>
            <div style="margin-bottom: 14px; border-top: 1px solid var(--line); padding-top: 12px;">
              <strong style="color: #ffffff; font-size: 13.5px; display: block; margin-bottom: 4px;">🎓 ${isKo ? "대학 인재상 적합성 (Admissions Fit)" : "Admissions Fit"}</strong>
              <p style="margin: 0; color: var(--muted); font-size: 13px; line-height: 1.6;">${escapeHtml(data.admissionsFit)}</p>
            </div>
            <div style="border-top: 1px solid var(--line); padding-top: 12px;">
              <strong style="color: #ffffff; font-size: 13.5px; display: block; margin-bottom: 4px;">📝 ${isKo ? "종합 개선 가이드 (Critique Details)" : "Critique Details"}</strong>
              <p style="margin: 0; color: var(--muted); font-size: 13px; line-height: 1.6;">${escapeHtml(data.critiqueDetails)}</p>
            </div>
          </div>

          <div style="margin-top: 20px;">
            <h4 style="color: #ffffff; font-size: 14.5px; margin-bottom: 12px; font-weight: 800;">✍️ ${isKo ? "네이티브 표현 교정 클리닉 (Native Phrase Clinic)" : "Native Phrase Clinic"}</h4>
            <table style="width: 100%; border-collapse: collapse; text-align: left; background: rgba(0,0,0,0.15); border: 1px solid var(--line); border-radius: 8px; overflow: hidden;">
              <thead>
                <tr style="background: rgba(255,255,255,0.03); border-bottom: 1px solid var(--line);">
                  <th style="padding: 10px; font-size: 11px; font-weight: 800; color: var(--muted); text-transform: uppercase;">Original Draft</th>
                  <th style="padding: 10px; font-size: 11px; font-weight: 800; color: var(--muted); text-transform: uppercase;">Native Polish</th>
                  <th style="padding: 10px; font-size: 11px; font-weight: 800; color: var(--muted); text-transform: uppercase;">Rationale</th>
                </tr>
              </thead>
              <tbody>
                ${rewritesHtml}
              </tbody>
            </table>
          </div>
        `;
      } else if (activeSubtab === "mapper") {
        let styleGuideHtml = "";
        if (data.targetStyleGuide) {
          styleGuideHtml = `
            <div style="background: rgba(99, 102, 241, 0.08); border: 1px solid rgba(99, 102, 241, 0.25); border-radius: 12px; padding: 18px; margin-bottom: 20px; box-shadow: var(--shadow-soft);">
              <strong style="color: #a5b4fc; font-size: 14.5px; display: block; margin-bottom: 6px; font-weight: 800; text-transform: uppercase;">
                💡 ${escapeHtml(t("essay_style_guide_title"))}
              </strong>
              <p style="color: #cbd5e1; font-size: 13.5px; line-height: 1.6; margin: 0; font-weight: 500;">
                ${escapeHtml(data.targetStyleGuide)}
              </p>
            </div>
          `;
        }

        const outlineHtml = data.outline.map(item => `
          <article style="border: 1px solid var(--line); border-radius: 12px; background: rgba(12, 18, 34, 0.5); padding: 22px; margin-bottom: 16px; box-shadow: var(--shadow-soft);">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 14px; border-bottom: 1px solid var(--line); padding-bottom: 10px;">
              <span style="font-size: 11px; font-weight: 800; text-transform: uppercase; color: #818cf8; background: rgba(99, 102, 241, 0.12); padding: 4px 10px; border-radius: 6px;">
                ${escapeHtml(item.paragraph)}
              </span>
              <strong style="color: #ffffff; font-size: 15px;">${escapeHtml(item.title)}</strong>
            </div>
            <p style="color: #e2e8f0; font-size: 13.5px; line-height: 1.6; margin: 0 0 16px 0; white-space: pre-wrap;">
              ${escapeHtml(item.content)}
            </p>
            
            <div style="display: grid; gap: 10px; margin-top: 14px;">
              <div style="background: rgba(16, 185, 129, 0.05); border: 1px solid rgba(16, 185, 129, 0.2); border-radius: 8px; padding: 10px 14px;">
                <span style="font-size: 11px; font-weight: 800; color: #34d399; text-transform: uppercase; display: block; margin-bottom: 4px;">🟢 ${escapeHtml(t("tag_dos"))}</span>
                <p style="color: #a7f3d0; font-size: 12.5px; line-height: 1.5; margin: 0;">${escapeHtml(item.dos || "")}</p>
              </div>
              <div style="background: rgba(239, 68, 68, 0.05); border: 1px solid rgba(239, 68, 68, 0.2); border-radius: 8px; padding: 10px 14px;">
                <span style="font-size: 11px; font-weight: 800; color: #f87171; text-transform: uppercase; display: block; margin-bottom: 4px;">🔴 ${escapeHtml(t("tag_donts"))}</span>
                <p style="color: #fca5a5; font-size: 12.5px; line-height: 1.5; margin: 0;">${escapeHtml(item.donts || "")}</p>
              </div>
              <div style="background: rgba(99, 102, 241, 0.05); border: 1px solid rgba(99, 102, 241, 0.2); border-radius: 8px; padding: 12px 14px;">
                <span style="font-size: 11px; font-weight: 800; color: #a5b4fc; text-transform: uppercase; display: block; margin-bottom: 4px;">✨ ${escapeHtml(t("tag_example"))}</span>
                <p style="color: #ffffff; font-size: 13px; font-family: var(--font-mono, monospace); font-style: italic; line-height: 1.65; margin: 0; background: rgba(0, 0, 0, 0.2); padding: 10px 14px; border-radius: 6px; border: 1px solid rgba(255, 255, 255, 0.05); font-weight: 500;">
                  "${escapeHtml(item.example || "")}"
                </p>
              </div>
            </div>
          </article>
        `).join("");

        outlineContent.innerHTML = styleGuideHtml + outlineHtml;
      } else {
        // activeSubtab === "optimizer"
        outlineContent.innerHTML = `
          <div style="display: grid; gap: 20px;">
            <div style="background: rgba(255, 255, 255, 0.02); border: 1px solid var(--line); border-radius: 12px; padding: 18px;">
              <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px;">
                <span style="font-size: 11px; font-weight: 800; color: #818cf8; background: rgba(99, 102, 241, 0.12); padding: 4px 10px; border-radius: 6px; text-transform: uppercase;">Common App Description (Max 150 Chars)</span>
                <span style="font-size: 12px; color: var(--muted);">${escapeHtml(data.commonAppVersion.characterCount)} / 150 chars</span>
              </div>
              <p style="color: #ffffff; font-size: 13.5px; font-family: var(--font-mono, monospace); background: rgba(0,0,0,0.2); padding: 12px 16px; border-radius: 6px; border: 1px solid var(--line); line-height: 1.5; margin: 0 0 10px 0;">
                ${escapeHtml(data.commonAppVersion.text)}
              </p>
              <div style="display: flex; justify-content: space-between; align-items: center;">
                <span style="font-size: 11px; color: var(--muted);">Action verbs: <strong>${escapeHtml(data.commonAppVersion.actionVerbsUsed)}</strong></span>
                <button type="button" id="copyCommonAppBtn" onclick="copyToClipboard('${escapeHtml(data.commonAppVersion.text.replace(/'/g, "\\'"))}', 'copyCommonAppBtn')" style="background: var(--primary); color: #fff; border: none; border-radius: 4px; padding: 4px 10px; font-size: 11.5px; cursor: pointer;">Copy Version</button>
              </div>
            </div>

            <div style="background: rgba(255, 255, 255, 0.02); border: 1px solid var(--line); border-radius: 12px; padding: 18px;">
              <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px;">
                <span style="font-size: 11px; font-weight: 800; color: #10b981; background: rgba(16, 185, 129, 0.12); padding: 4px 10px; border-radius: 6px; text-transform: uppercase;">UC Description (Max 350 Chars)</span>
                <span style="font-size: 12px; color: var(--muted);">${escapeHtml(data.ucVersion.characterCount)} / 350 chars</span>
              </div>
              <p style="color: #ffffff; font-size: 13.5px; font-family: var(--font-mono, monospace); background: rgba(0,0,0,0.2); padding: 12px 16px; border-radius: 6px; border: 1px solid var(--line); line-height: 1.5; margin: 0 0 10px 0;">
                ${escapeHtml(data.ucVersion.text)}
              </p>
              <div style="display: flex; justify-content: space-between; align-items: center;">
                <span style="font-size: 11px; color: var(--muted);">Action verbs: <strong>${escapeHtml(data.ucVersion.actionVerbsUsed)}</strong></span>
                <button type="button" id="copyUcBtn" onclick="copyToClipboard('${escapeHtml(data.ucVersion.text.replace(/'/g, "\\'"))}', 'copyUcBtn')" style="background: var(--primary); color: #fff; border: none; border-radius: 4px; padding: 4px 10px; font-size: 11.5px; cursor: pointer;">Copy Version</button>
              </div>
            </div>

            <div style="background: rgba(245, 158, 11, 0.05); border: 1px solid rgba(245, 158, 11, 0.2); border-radius: 8px; padding: 12px 14px;">
              <strong style="color: #fbbf24; font-size: 13px; display: block; margin-bottom: 4px;">💡 ${isKo ? "작성 피드백 (Optimizer Feedback)" : "Optimizer Feedback"}</strong>
              <p style="color: #fef3c7; font-size: 12.5px; line-height: 1.5; margin: 0;">${escapeHtml(data.feedback)}</p>
            </div>
          </div>
        `;
      }

      setTimeout(() => {
        qs("#essayResultPanel")?.scrollIntoView({ behavior: "smooth", block: "nearest" });
      }, 100);

    } catch (err) {
      outlineContent.innerHTML = `
        <div class="placeholder-view" style="color: var(--danger); text-align: center; padding: 40px 0;">
          <p>❌ ${escapeHtml(t("essay_error_occurred"))} ${escapeHtml(err.message)}</p>
          <p style="font-size: 12px; color: var(--muted); margin-top: 8px;">${escapeHtml(t("essay_error_desc"))}</p>
        </div>
      `;
    } finally {
      generateBtn.disabled = false;
      generateBtn.textContent = activeSubtab === "critic" 
        ? (isKo ? "에세이 초안 문단 검토 및 윤문하기" : "Critique & Polish Paragraph")
        : (activeSubtab === "optimizer" ? (isKo ? "활동 이력 압축 및 최적화하기" : "Optimize Activity Description") : t("essay_generate_btn_label"));
    }
  });

  // 3. AI Interview State Machine & Handlers
  let interviewQuestions = [];
  let interviewAnswers = ["", "", "", "", ""];
  let currentInterviewStep = 0;
  let essayMode = "direct";

  const modeTabDirect = qs("#modeTabDirect");
  const modeTabInterview = qs("#modeTabInterview");
  const essayDirectInputWrapper = qs("#essayDirectInputWrapper");
  const essayInterviewWrapper = qs("#essayInterviewWrapper");

  if (modeTabDirect && modeTabInterview && essayDirectInputWrapper && essayInterviewWrapper) {
    modeTabDirect.addEventListener("click", () => {
      essayMode = "direct";
      modeTabDirect.classList.add("active");
      modeTabInterview.classList.remove("active");
      modeTabDirect.style.background = "rgba(99, 102, 241, 0.12)";
      modeTabDirect.style.color = "#a5b4fc";
      modeTabInterview.style.background = "transparent";
      modeTabInterview.style.color = "var(--muted)";
      essayDirectInputWrapper.classList.remove("hidden");
      essayInterviewWrapper.classList.add("hidden");
    });

    modeTabInterview.addEventListener("click", () => {
      essayMode = "interview";
      modeTabInterview.classList.add("active");
      modeTabDirect.classList.remove("active");
      modeTabInterview.style.background = "rgba(99, 102, 241, 0.12)";
      modeTabInterview.style.color = "#a5b4fc";
      modeTabDirect.style.background = "transparent";
      modeTabDirect.style.color = "var(--muted)";
      essayDirectInputWrapper.classList.add("hidden");
      essayInterviewWrapper.classList.remove("hidden");
    });
  }

  const startInterviewBtn = qs("#startInterviewBtn");
  const interviewStartScreen = qs("#interviewStartScreen");
  const interviewActiveScreen = qs("#interviewActiveScreen");

  const interviewStepLabel = qs("#interviewStepLabel");
  const interviewStepPercent = qs("#interviewStepPercent");
  const interviewProgressBar = qs("#interviewProgressBar");
  const interviewQuestionText = qs("#interviewQuestionText");
  const interviewAnswerInput = qs("#interviewAnswerInput");
  const prevInterviewBtn = qs("#prevInterviewBtn");
  const nextInterviewBtn = qs("#nextInterviewBtn");

  function renderInterviewStep() {
    if (!interviewQuestionText || !interviewAnswerInput || !interviewStepLabel || !interviewStepPercent || !interviewProgressBar) return;

    interviewQuestionText.textContent = interviewQuestions[currentInterviewStep] || "";
    interviewAnswerInput.value = interviewAnswers[currentInterviewStep] || "";

    const stepNum = currentInterviewStep + 1;
    const percent = Math.round((stepNum / 5) * 100);

    interviewStepLabel.textContent = `Step ${stepNum} of 5`;
    interviewStepPercent.textContent = `${percent}%`;
    interviewProgressBar.style.width = `${percent}%`;

    if (prevInterviewBtn) {
      if (currentInterviewStep === 0) {
        prevInterviewBtn.style.visibility = "hidden";
      } else {
        prevInterviewBtn.style.visibility = "visible";
        prevInterviewBtn.textContent = t("btn_prev", "Previous");
      }
    }

    if (nextInterviewBtn) {
      if (currentInterviewStep === 4) {
        nextInterviewBtn.textContent = t("btn_complete", "Complete");
      } else {
        nextInterviewBtn.textContent = t("btn_next", "Next");
      }
    }
  }

  if (startInterviewBtn && interviewStartScreen && interviewActiveScreen) {
    startInterviewBtn.addEventListener("click", async () => {
      const selectedId = schoolSelect.value;
      const targetProgram = programs.find(p => p.id === selectedId);
      if (!targetProgram) return;

      const essayQuestion = qs("#essayQuestion")?.value.trim() || "";
      if (!essayQuestion) {
        alert(t("essay_alert_no_question"));
        return;
      }

      startInterviewBtn.disabled = true;
      startInterviewBtn.textContent = t("essay_generating_label", "Loading...");

      try {
        const response = await fetch('/api/interview/questions', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            schoolName: targetProgram.school.name,
            majorName: targetProgram.name,
            essayQuestion: essayQuestion
          })
        });
        const data = await response.json();

        if (!data.success || !data.questions || data.questions.length < 5) {
          throw new Error(data.message || "Failed to load questions");
        }

        interviewQuestions = data.questions;
        interviewAnswers = ["", "", ""];
        currentInterviewStep = 0;

        interviewStartScreen.classList.add("hidden");
        interviewActiveScreen.classList.remove("hidden");
        renderInterviewStep();

      } catch (err) {
        alert(t("essay_error_occurred", "An error occurred:") + " " + err.message);
      } finally {
        startInterviewBtn.disabled = false;
        startInterviewBtn.textContent = t("btn_start_interview", "Start AI Interview");
      }
    });
  }

  if (prevInterviewBtn) {
    prevInterviewBtn.addEventListener("click", () => {
      if (interviewAnswerInput) {
        interviewAnswers[currentInterviewStep] = interviewAnswerInput.value.trim();
      }
      if (currentInterviewStep > 0) {
        currentInterviewStep--;
        renderInterviewStep();
      }
    });
  }

  if (nextInterviewBtn) {
    nextInterviewBtn.addEventListener("click", () => {
      if (interviewAnswerInput) {
        interviewAnswers[currentInterviewStep] = interviewAnswerInput.value.trim();
      }

      if (currentInterviewStep < 4) {
        currentInterviewStep++;
        renderInterviewStep();
      } else {
        const mergedActivities = interviewQuestions.map((q, idx) => {
          return `[Question ${idx + 1}: ${q}]\n[Answer: ${interviewAnswers[idx] || "N/A"}]`;
        }).join("\n\n");

        const essayActivityInput = qs("#essayActivity");
        if (essayActivityInput) {
          essayActivityInput.value = mergedActivities;
        }

        alert(t("interview_complete_msg", "Interview completed! Click the generate button to create your custom outline."));

        if (modeTabDirect) {
          modeTabDirect.click();
        }
      }
    });
  }

  // 4. Pre-fill Examples
  qs("#loadEssayExample1")?.addEventListener("click", () => {
    const schoolSelect = qs("#essayTargetSchool");
    const programId = "university-of-michigan-computer-science-coe-2c16a5ea";
    const program = allPrograms().find(p => p.id === programId);
    if (program) {
      if (schoolSelect) {
        schoolSelect.value = `${program.school.name} - ${program.name}`;
        schoolSelect.dataset.selectedId = program.id;
      }
      const schoolInput = qs("#essaySchoolInput");
      const majorInput = qs("#essayMajorInput");
      const majorToggle = majorInput?.parentElement?.querySelector(".autocomplete-toggle-btn");
      if (schoolInput) schoolInput.value = program.school.name;
      if (majorInput) {
        majorInput.value = program.name;
        safeRemoveAttr(majorInput, "disabled");
        safeSetAttr(majorInput, "data-i18n-placeholder", "select_major_placeholder");
        majorInput.placeholder = t("select_major_placeholder", "Select Major");
        if (majorToggle) safeRemoveAttr(majorToggle, "disabled");
      }
      updateEssaySchoolStatusUI();
    }
    const limitInput = qs("#essayLimit");
    if (limitInput) limitInput.value = "350 words";
    const questionText = qs("#essayQuestion");
    if (questionText) questionText.value = "Describe your academic interests and how you plan to pursue them at the University of Michigan.";
    const activitiesText = qs("#essayActivity");
    if (activitiesText) activitiesText.value = "Led the community college computer science club, developed an open-source web application for class scheduling using React and Node.js, tutored peers in Calculus and Discrete Math.";
    
    const modeDirect = qs("#modeTabDirect");
    if (modeDirect) modeDirect.click();
  });

  qs("#loadEssayExample2")?.addEventListener("click", () => {
    const schoolSelect = qs("#essayTargetSchool");
    const programId = "georgia-tech-mechanical-engineering-0dac0766";
    const program = allPrograms().find(p => p.id === programId);
    if (program) {
      if (schoolSelect) {
        schoolSelect.value = `${program.school.name} - ${program.name}`;
        schoolSelect.dataset.selectedId = program.id;
      }
      const schoolInput = qs("#essaySchoolInput");
      const majorInput = qs("#essayMajorInput");
      const majorToggle = majorInput?.parentElement?.querySelector(".autocomplete-toggle-btn");
      if (schoolInput) schoolInput.value = program.school.name;
      if (majorInput) {
        majorInput.value = program.name;
        safeRemoveAttr(majorInput, "disabled");
        safeSetAttr(majorInput, "data-i18n-placeholder", "select_major_placeholder");
        majorInput.placeholder = t("select_major_placeholder", "Select Major");
        if (majorToggle) safeRemoveAttr(majorToggle, "disabled");
      }
      updateEssaySchoolStatusUI();
    }
    const limitInput = qs("#essayLimit");
    if (limitInput) limitInput.value = "500 words";
    const questionText = qs("#essayQuestion");
    if (questionText) questionText.value = "Georgia Tech values social responsibility. Describe how your interest in mechanical engineering will allow you to make a positive impact.";
    const activitiesText = qs("#essayActivity");
    if (activitiesText) activitiesText.value = "Mentored a high school robotics team (FTC), designed and 3D printed low-cost prosthetic hands for a local charity project, worked part-time as a bicycle mechanic repairing community bikes.";
    
    const modeDirect = qs("#modeTabDirect");
    if (modeDirect) modeDirect.click();
  });

  updateEssayCreditsUI();
}

function updateEssayCreditsUI() {
  const countEl = qs("#essayCreditsCount");
  if (countEl) {
    countEl.textContent = state.essayCredits;
  }
  const pendingCountEl = qs("#essayPendingPassesCount");
  if (pendingCountEl) {
    pendingCountEl.textContent = state.pendingPasses || 0;
  }
  const unlockedListEl = qs("#unlockedSchoolsList");
  if (unlockedListEl && state.unlockedSchools.length > 0) {
    const isKo = (state.language || "ko") === "ko";
    unlockedListEl.innerHTML = `${isKo ? "활성화된 대학" : "Unlocked Universities"}: <strong style="color: #10b981;">${state.unlockedSchools.join(", ")}</strong>`;
  } else if (unlockedListEl) {
    unlockedListEl.innerHTML = "";
  }
  
  // also update profile modal if opened
  const profileCreditsVal = qs("#profileCreditsVal");
  if (profileCreditsVal) {
    profileCreditsVal.textContent = `${state.essayCredits} (Passes: ${state.pendingPasses || 0})`;
  }
  updateTabLockStates();
}
window.updateEssayCreditsUI = updateEssayCreditsUI;

function bindTracks() {
  const trackSelector = qs("#trackSelector");
  if (trackSelector) {
    trackSelector.value = state.track || "stem";
    trackSelector.addEventListener("change", (event) => {
      state.track = event.target.value;
      saveProfileToLocalStorage();
      renderCourseGroups();
      renderEligibilityResults();
      buildRoadmap();
    });
  }

  qsa(".strategy-track-btn").forEach((btn) => {
    btn.addEventListener("click", () => {
      qsa(".strategy-track-btn").forEach((b) => b.classList.remove("active"));
      btn.classList.add("active");
      state.strategyTrack = btn.dataset.track;
      renderStrategyTimeline();
    });
  });

  // Render strategy timeline initially
  renderStrategyTimeline();
}
window.bindTracks = bindTracks;

function getFirstTargetSchoolAndMajor() {
  if (Array.isArray(state.targetSlots)) {
    for (let slot of state.targetSlots) {
      if (slot && slot.school && slot.school.trim() !== "" && slot.major && slot.major.trim() !== "") {
        return { school: slot.school.trim(), major: slot.major.trim() };
      }
    }
  }
  return null;
}

function getActiveEssaySlots() {
  try {
    const val = localStorage.getItem("transferCompassActiveEssaySlots");
    return val ? JSON.parse(val) : [];
  } catch {
    return [];
  }
}

function saveActiveEssaySlots(slots) {
  localStorage.setItem("transferCompassActiveEssaySlots", JSON.stringify(slots));
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";
  if (currentUser && authState.users[currentUser]) {
    authState.users[currentUser].activeEssaySlots = slots;
    writeAuthState(authState);
  }
}

function getActiveEssaySlot(schoolName, majorName, essayQuestion) {
  if (!schoolName || !majorName || !essayQuestion) return null;
  const slots = getActiveEssaySlots();
  const normSchool = normalizeText(schoolName);
  const normMajor = normalizeText(majorName);
  const normQuestion = normalizeText(essayQuestion);
  const now = new Date();
  
  for (let slot of slots) {
    if (normalizeText(slot.school) === normSchool &&
        normalizeText(slot.major) === normMajor &&
        normalizeText(slot.question) === normQuestion) {
      if (new Date(slot.expiresAt) > now) {
        return slot;
      }
    }
  }
  return null;
}

function getActivePremiumSlot() {
  const slots = getActiveEssaySlots();
  const now = new Date();
  for (let slot of slots) {
    if (slot.type === "premium" && new Date(slot.expiresAt) > now) {
      return slot;
    }
  }
  return null;
}

window.activatePremiumEssaySlot = function(schoolName, majorName, essayQuestion) {
  const isKo = (state.language || "ko") === "ko";
  if (!schoolName || !majorName || !essayQuestion) {
    alert(isKo ? "학교, 학과, 그리고 에세이 질문을 모두 입력해 주세요." : "Please select school, major and enter the essay question.");
    return;
  }
  
  const existingPremium = getActivePremiumSlot();
  if (existingPremium) {
    alert(isKo 
      ? `이미 이번 달 Premium 에세이가 등록되어 있습니다: ${existingPremium.school} - ${existingPremium.major}` 
      : `You already have an active Premium essay slot: ${existingPremium.school} - ${existingPremium.major}`);
    return;
  }
  
  const slots = getActiveEssaySlots();
  const now = new Date();
  const expiresAt = new Date(now.getTime() + 30 * 24 * 60 * 60 * 1000).toISOString(); // 30 days
  
  slots.push({
    school: schoolName,
    major: majorName,
    question: essayQuestion,
    expiresAt: expiresAt,
    type: "premium"
  });
  
  saveActiveEssaySlots(slots);
  saveProfileToSupabase();
  
  updateEssaySchoolStatusUI();
  
  alert(isKo 
    ? `성공적으로 Premium 에세이를 등록했습니다! 한 달 동안 이 에세이를 무제한으로 첨삭받을 수 있습니다.` 
    : `Successfully activated Premium essay slot! You can critique and rewrite this essay unlimitedly for one month.`);
};

window.activatePassEssaySlot = function(schoolName, majorName, essayQuestion) {
  const isKo = (state.language || "ko") === "ko";
  if (state.pendingPasses <= 0) {
    alert(isKo ? "사용 가능한 에세이 패스가 없습니다." : "No pending essay passes available.");
    return;
  }
  
  if (!schoolName || !majorName || !essayQuestion) {
    alert(isKo ? "학교, 학과, 그리고 에세이 질문을 모두 입력해 주세요." : "Please select school, major and enter the essay question.");
    return;
  }
  
  state.pendingPasses--;
  localStorage.setItem("transferCompassPendingPasses", state.pendingPasses.toString());
  
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";
  if (currentUser && authState.users[currentUser]) {
    authState.users[currentUser].pendingPasses = state.pendingPasses;
    writeAuthState(authState);
  }
  
  const slots = getActiveEssaySlots();
  const now = new Date();
  const expiresAt = new Date(now.getTime() + 30 * 24 * 60 * 60 * 1000).toISOString(); // 30 days
  
  slots.push({
    school: schoolName,
    major: majorName,
    question: essayQuestion,
    expiresAt: expiresAt,
    type: "pass"
  });
  
  saveActiveEssaySlots(slots);
  saveProfileToSupabase();
  
  updateEssaySchoolStatusUI();
  updateEssayCreditsUI();
  
  alert(isKo 
    ? `성공적으로 에세이 대상 패스를 활성화했습니다! 한 달 동안 이 에세이를 무제한으로 첨삭받을 수 있습니다.` 
    : `Successfully activated essay target pass! You can critique and rewrite this essay unlimitedly for one month.`);
};

function updateEssaySchoolStatusUI() {
  const schoolSelect = qs("#essayTargetSchool");
  const statusEl = qs("#essaySchoolPassStatus");
  if (!schoolSelect || !statusEl) return;
  
  const selectedId = schoolSelect.dataset.selectedId;
  if (!selectedId) {
    statusEl.style.display = "none";
    return;
  }
  
  const program = allPrograms().find(p => p.id === selectedId);
  if (!program) {
    statusEl.style.display = "none";
    return;
  }
  
  const schoolName = program.school.name;
  const majorName = program.name;
  const essayQuestion = qs("#essayQuestion")?.value.trim() || "";
  const isKo = (state.language || "ko") === "ko";
  const isPremium = (state.plan || "Free") === "Premium";
  
  statusEl.style.display = "block";
  statusEl.style.marginTop = "8px";
  
  if (!essayQuestion) {
    statusEl.innerHTML = `<span style="color: var(--gold); font-size: 12px;">⚠️ ${isKo ? "에세이 질문을 입력해 주세요." : "Please enter the essay question."}</span>`;
    return;
  }
  
  // Check if active
  const activeSlot = getActiveEssaySlot(schoolName, majorName, essayQuestion);
  if (activeSlot) {
    const expires = new Date(activeSlot.expiresAt);
    const dateStr = expires.toLocaleDateString(isKo ? 'ko-KR' : 'en-US');
    if (activeSlot.type === "premium") {
      statusEl.innerHTML = `<span style="color: #10b981; font-weight: 800; font-size: 12px;">✓ ${isKo ? `이 에세이 패스 활성화됨 (에세이 하나당 한달 동안 유효, 만료일: ${dateStr})` : `Premium Essay Slot Active (Valid for 1 month per essay, Expires: ${dateStr})`}</span>`;
    } else {
      statusEl.innerHTML = `<span style="color: #10b981; font-weight: 800; font-size: 12px;">✓ ${isKo ? `이 에세이 패스 활성화됨 (한달 동안 무제한, 만료일: ${dateStr})` : `Essay Pass Active (Unlimited for 1 month, Expires: ${dateStr})`}</span>`;
    }
    updateTabLockStates();
    return;
  }
  
  // If not active:
  if (isPremium) {
    const activePremium = getActivePremiumSlot();
    if (!activePremium) {
      statusEl.innerHTML = `
        <div style="display: flex; flex-direction: column; gap: 6px; background: rgba(99, 102, 241, 0.05); padding: 10px; border-radius: 8px; border: 1px dashed var(--primary);">
          <span style="font-size: 12px; color: var(--ink);">${isKo ? "Premium 플랜의 이번 달 무제한 에세이로 등록할 수 있습니다." : "Can register as your Premium monthly essay."}</span>
          <button type="button" class="primary-btn compact" onclick="activatePremiumEssaySlot('${escapeHtml(schoolName)}', '${escapeHtml(majorName)}', '${escapeHtml(essayQuestion.replace(/'/g, "\\'"))}')" style="font-size: 11px; padding: 4px 8px; min-height: 28px; background: var(--primary); color: #fff; border: none; border-radius: 4px; cursor: pointer; align-self: flex-start;">
            ${isKo ? "Premium 등록하기 (한 달간 무제한)" : "Activate Premium Essay"}
          </button>
        </div>
      `;
      updateTabLockStates();
      return;
    }
  }
  
  // Otherwise, standalone pass options
  if (state.pendingPasses > 0) {
    statusEl.innerHTML = `
      <div style="display: flex; align-items: center; justify-content: space-between; gap: 8px; background: rgba(255,255,255,0.02); padding: 8px; border-radius: 6px; border: 1px solid var(--line);">
        <span style="color: var(--muted); font-size: 11.5px;">${isKo ? "보유 패스로 이 에세이 활성화 가능" : "Can unlock with pending pass"}</span>
        <button type="button" class="primary-btn compact" onclick="activatePassEssaySlot('${escapeHtml(schoolName)}', '${escapeHtml(majorName)}', '${escapeHtml(essayQuestion.replace(/'/g, "\\'"))}')" style="font-size: 11px; padding: 4px 8px; min-height: 28px; background: var(--primary); color: #fff; border: none; border-radius: 4px; cursor: pointer;">
          ${isKo ? "활성화하기 (1개월 무제한)" : "Unlock"}
        </button>
      </div>
    `;
  } else {
    statusEl.innerHTML = `
      <div style="display: flex; align-items: center; justify-content: space-between; gap: 8px; background: rgba(255,255,255,0.02); padding: 8px; border-radius: 6px; border: 1px solid var(--line);">
        <span style="color: var(--muted); font-size: 11px;">${isKo ? "무제한 검토를 위해 이 에세이 패스 구매" : "Buy pass for unlimited reviews of this essay"}</span>
        <button type="button" class="primary-btn compact" onclick="buyStandaloneEssayPass()" style="font-size: 11px; padding: 4px 8px; min-height: 28px; background: var(--gold); color: #000; border: none; border-radius: 4px; cursor: pointer;">
          ${isKo ? "9,900원에 패스 구매" : "Buy Pass 9,900 KRW"}
        </button>
      </div>
    `;
  }
  updateTabLockStates();
}
window.updateEssaySchoolStatusUI = updateEssaySchoolStatusUI;

window.unlockSchool = function(schoolName) {
  // Deprecated - activePassEssaySlot or activatePremiumEssaySlot should be used.
  // Left for backward compatibility.
  alert("Please select target school and input the essay question, then click the activation button in the status panel.");
};

window.buyStandaloneEssayPass = function() {
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";
  if (!currentUser) {
    alert(t("alert_login_required", "You must log in or register to buy essay credits."));
    closePricingModal();
    openAuthModal("login");
    return;
  }

  const consentMinor = qs("#paymentConsentMinor");
  const consentDisclaimer = qs("#paymentConsentDisclaimer");
  if ((consentMinor && !consentMinor.checked) || (consentDisclaimer && !consentDisclaimer.checked)) {
    alert(t("payment_consent_error", "You must agree to all legal consents (Minor check, Disclaimer) to proceed with payment."));
    return;
  }

  // Portone payment integration
  const IMP = window.IMP;
  if (!IMP) {
    alert(t("payment_sdk_error", "Payment module is loading. Please try again in a moment."));
    return;
  }

  const userProfile = authState.users[currentUser] || {};
  const buyerName = userProfile.name || currentUser.split("@")[0] || "Customer";
  const buyerPhone = userProfile.phone || "010-0000-0000";

  const isKo = (state.language || "ko") === "ko";
  const pgChannel = isKo ? (PORTONE_TEST_MODE ? "html5_inicis.INIpayTest" : "html5_inicis.MOI6298964") : "paypal_v2";
  const payCurrency = isKo ? "KRW" : "USD";
  const payAmount = isKo ? 9900 : 8; // $8 USD for AI Target Essay Pass
  const productName = isKo ? "AI 에세이 대학교 프리패스" : "AI Target Essay Pass (1 School Unlimited)";

  IMP.request_pay({
    pg: pgChannel,
    pay_method: "card",
    merchant_uid: `order_essay_${Date.now()}`,
    name: productName,
    amount: payAmount,
    currency: payCurrency,
    buyer_email: currentUser,
    buyer_name: buyerName,
    buyer_tel: buyerPhone
  }, function(rsp) {
    if (rsp.success) {
      applyEssayCreditsPurchase();
    } else {
      alert(t("payment_failed", "Payment failed: {error}").replace("{error}", rsp.error_msg || "Unknown error"));
    }
  });
};

function applyEssayCreditsPurchase() {
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";
  if (currentUser) {
    authState.users[currentUser].pendingPasses = (authState.users[currentUser].pendingPasses || 0) + 1;
    writeAuthState(authState);
    state.pendingPasses = authState.users[currentUser].pendingPasses;
  } else {
    state.pendingPasses = (state.pendingPasses || 0) + 1;
  }
  localStorage.setItem("transferCompassPendingPasses", state.pendingPasses.toString());
  updateEssayCreditsUI();
  
  const alertMsg = t("alert_essay_pass_purchased", "Successfully purchased AI Target Essay Pass! 1 pending university pass has been added to your account. Activate it below for your target school.");
  alert(alertMsg);
}

async function init() {
  // Fetch real-time exchange rate
  await fetchExchangeRate();

  // Initialize Portone SDK Test Mode
  const IMP = window.IMP;
  if (IMP) {
    IMP.init("imp81577133"); // Portone Live Store ID for TransferChek
  }

  // Track visit telemetry on page load
  fetch('/api/track-visit', { method: 'POST', cache: 'no-store' })
    .catch(e => console.warn("Failed to send visit telemetry:", e));

  // Sync Supabase Session and User Profile
  await syncSupabaseSession();

  const authState = readAuthState();
  let currentUser = authState.currentUser || "";

  if (!currentUser) {
    state.plan = "Free";
    state.essayCredits = 0;
    localStorage.setItem("transferCompassPlan", "Free");
    localStorage.setItem("transferCompassEssayCredits", "0");
  } else {
    // If logged in via Supabase, readAuthState populated plan & essayCredits
    const userProfile = authState.users[currentUser] || {};
    state.plan = userProfile.plan || "Free";
    state.essayCredits = userProfile.essayCredits || 0;
    localStorage.setItem("transferCompassPlan", state.plan);
    localStorage.setItem("transferCompassEssayCredits", state.essayCredits.toString());
  }

  loadProfileFromLocalStorage();
  initializeTargetSlots();
  renderRequirementControls();
  bindEvents();
  bindFeedback();
  bindAuth();
  bindEssay();
  bindTracks();

  // Sync loaded profile state with DOM inputs
  const gpaInput = qs("#gpaInput");
  if (gpaInput) gpaInput.value = state.gpa.toFixed(2);
  const creditsInput = qs("#creditsInput");
  if (creditsInput) creditsInput.value = state.credits;
  const internationalInput = qs("#internationalInput");
  if (internationalInput) internationalInput.checked = state.international;
  const waiverInput = qs("#englishWaiverInput");
  if (waiverInput) waiverInput.checked = state.englishWaiver;
  const englishTypeInput = qs("#englishTypeInput");
  if (englishTypeInput) englishTypeInput.value = state.englishType;
  const englishScoreInput = qs("#englishScoreInput");
  if (englishScoreInput) englishScoreInput.value = state.englishScore;

  const admissionYearInput = qs("#admissionYear");
  if (admissionYearInput) admissionYearInput.value = state.admissionYear;
  const admissionTermInput = qs("#admissionTerm");
  if (admissionTermInput) admissionTermInput.value = state.admissionTerm;

  qs("#englishFields")?.classList.toggle("hidden", state.englishWaiver || !state.international);
  qs("#englishWaiverRow")?.classList.toggle("hidden", !state.international);
  
  switchLanguage(state.language);
  updatePlanNoticeVisibility();
  updateEnglishScoreInputConstraints();
  initScrollEffects();
  initStatsText();
  
  const initialTab = location.hash.replace("#", "");
  if (initialTab === "demo") {
    activateProductTab("eligibility", false);
  } else if (["eligibility", "requirements", "roadmap", "essay"].includes(initialTab)) {
    activateProductTab(initialTab, false);
  }
}

init().catch(e => console.error("Init failed:", e));

function openFeatureAd(featureId) {
  const modal = qs("#featureAdModal");
  if (!modal) return;
  
  const title = t(`ad_${featureId}_title`, "Feature");
  const headline = t(`ad_${featureId}_headline`, "");
  const b1 = t(`ad_${featureId}_b1`, "");
  const b2 = t(`ad_${featureId}_b2`, "");
  const b3 = t(`ad_${featureId}_b3`, "");
  const btn = t(`ad_${featureId}_btn`, "Try Feature");
  
  const modalTitle = qs("#adModalTitle");
  const modalHeadline = qs("#adModalHeadline");
  const modalBullets = qs("#adModalBullets");
  const modalCta = qs("#adModalCta");
  
  if (modalTitle) modalTitle.textContent = title;
  if (modalHeadline) modalHeadline.textContent = headline;
  if (modalBullets) {
    modalBullets.innerHTML = `
      <li>${escapeHtml(b1)}</li>
      <li>${escapeHtml(b2)}</li>
      <li>${escapeHtml(b3)}</li>
    `;
  }
  
  if (modalCta) {
    modalCta.textContent = btn;
    modalCta.onclick = () => {
      closeFeatureAd();
      const demoSec = qs("#demo");
      if (demoSec) {
        demoSec.scrollIntoView({ behavior: 'smooth' });
      }
      activateProductTab(featureId);
    };
  }
  
  modal.classList.remove("hidden");
}

function closeFeatureAd() {
  const modal = qs("#featureAdModal");
  if (modal) {
    modal.classList.add("hidden");
  }
}

window.openFeatureAd = openFeatureAd;
window.closeFeatureAd = closeFeatureAd;

// Profile Modal helpers
function openProfileModal() {
  const authState = readAuthState();
  const email = authState.currentUser;
  if (!email) return;

  const userProfile = authState.users[email] || {};
  const plan = userProfile.plan || "Free";
  const credits = userProfile.essayCredits || 0;
  const nationality = userProfile.nationality || "Other";

  qs("#profileEmailVal").textContent = email;
  qs("#profileNationalityVal").textContent = nationality;
  qs("#profilePlanVal").textContent = plan.toUpperCase() + " PLAN";
  qs("#profileCreditsVal").textContent = credits;
  
  const msg = qs("#profileMessage");
  if (msg) msg.textContent = "";
  
  qs("#profileCurrentPassword").value = "";
  qs("#profileNewPassword").value = "";

  qs("#profileModal").classList.remove("hidden");
}

function closeProfileModal() {
  qs("#profileModal").classList.add("hidden");
}

function closeProfileAndOpenPricing() {
  closeProfileModal();
  openPricingModal();
}

function closeProfileAndBuyCredits() {
  closeProfileModal();
  buyStandaloneEssayPass();
}

window.openProfileModal = openProfileModal;
window.closeProfileModal = closeProfileModal;
window.closeProfileAndOpenPricing = closeProfileAndOpenPricing;
window.closeProfileAndBuyCredits = closeProfileAndBuyCredits;

window.toggleRecommendedCourses = function(btn) {
  const wrapper = btn.nextElementSibling;
  if (wrapper && wrapper.classList.contains("recommended-wrapper")) {
    const isHidden = wrapper.classList.contains("hidden");
    if (isHidden) {
      wrapper.classList.remove("hidden");
      btn.querySelector("span:first-child").textContent = "🙈";
    } else {
      wrapper.classList.add("hidden");
      btn.querySelector("span:first-child").textContent = "👁️";
    }
  }
};

// Side Drawer Menu Logic
function openSideDrawer() {
  const drawer = qs("#sideDrawer");
  const toggleBtn = qs("#hamburgerMenuBtn");
  if (drawer) {
    drawer.classList.remove("hidden");
    toggleBtn?.classList.add("open");
    renderSideDrawerUserSection();
  }
}

function closeSideDrawer() {
  const drawer = qs("#sideDrawer");
  const toggleBtn = qs("#hamburgerMenuBtn");
  if (drawer) {
    drawer.classList.add("hidden");
    toggleBtn?.classList.remove("open");
  }
}

function renderSideDrawerUserSection() {
  const container = qs("#sideDrawerUserSection");
  if (!container) return;
  
  const authState = readAuthState();
  const currentUser = authState.currentUser || "";
  const isEn = state.language === "en";
  
  if (!currentUser) {
    container.innerHTML = `
      <div style="display: flex; flex-direction: column; gap: 8px;">
        <p style="font-size: 12px; color: var(--muted); margin: 0;">${isEn ? "Log in to access your customized course plans and AI essay tools." : "로그인하시면 개인 맞춤 수강계획 및 AI 에세이 기능을 사용하실 수 있습니다."}</p>
        <div style="display: flex; gap: 8px; margin-top: 4px;">
          <button class="secondary-btn compact" style="flex: 1; min-height: 30px; font-size: 11px; padding: 4px;" onclick="closeSideDrawer(); openAuthModal('login');">${isEn ? "Log In" : "로그인"}</button>
          <button class="primary-btn compact" style="flex: 1; min-height: 30px; font-size: 11px; padding: 4px;" onclick="closeSideDrawer(); openAuthModal('signup');">${isEn ? "Sign Up" : "회원가입"}</button>
        </div>
      </div>
    `;
    return;
  }
  
  const userProfile = authState.users[currentUser] || {};
  const initial = currentUser.charAt(0).toUpperCase();
  const plan = userProfile.plan || "Free";
  const displayPlan = plan === "Premium" ? "Premium" : (isEn ? "Free Plan" : "무료 플랜");
  const credits = state.essayCredits;
  const displayCredits = credits === 999999 ? (isEn ? "Unlimited" : "무제한") : credits + (isEn ? " credits" : "회");
  
  container.innerHTML = `
    <div class="user-header">
      <div class="user-avatar">${initial}</div>
      <div class="user-details">
        <span class="user-email" style="word-break: break-all;">${currentUser}</span>
        <span class="user-plan">${displayPlan}</span>
      </div>
    </div>
    <div class="user-credits" style="margin-bottom: 12px;">
      <span>✉️ ${isEn ? "AI Essay Credits" : "AI 에세이 크레딧"}: <strong>${displayCredits}</strong></span>
    </div>
    <div style="display: flex; gap: 8px; margin-top: 8px;">
      <button type="button" class="secondary-btn compact" style="flex: 1; min-height: 30px; font-size: 11px; padding: 4px;" onclick="closeSideDrawer(); openProfileModal();">${isEn ? "My Profile" : "내 프로필"}</button>
      <button type="button" class="secondary-btn compact" style="flex: 1; min-height: 30px; font-size: 11px; padding: 4px;" onclick="closeSideDrawer(); handleLogout();">${isEn ? "Log out" : "로그아웃"}</button>
    </div>
  `;
}

function handleDrawerNavigation(target) {
  closeSideDrawer();
  
  if (target === 'pricing') {
    openPricingModal();
    return;
  }
  
  if (target === 'transfer-guide') {
    const guideSec = qs("#transfer-guide");
    if (guideSec) {
      guideSec.scrollIntoView({ behavior: 'smooth' });
    }
    return;
  }
  
  const demoSec = qs("#demo");
  if (demoSec) {
    demoSec.scrollIntoView({ behavior: 'smooth' });
  }
  activateProductTab(target);
}

window.openSideDrawer = openSideDrawer;
window.closeSideDrawer = closeSideDrawer;
window.handleDrawerNavigation = handleDrawerNavigation;
window.renderSideDrawerUserSection = renderSideDrawerUserSection;

function renderStrategyTimeline() {
  const container = qs(".guide-timeline");
  if (!container) return;
  const lang = state.language || "en";
  const track = state.strategyTrack || "stem";
  const steps = STRATEGY_DATA[lang]?.[track] || STRATEGY_DATA["en"]?.[track] || [];

  const FEATURE_TAB_MAP = {
    "01. 지원 가능 판단": "eligibility",
    "02. 편입 요건 검색": "requirements",
    "03. 수강 로드맵 빌더": "roadmap",
    "04. AI 에세이 전략가": "essay",
    "01. Eligibility Diagnostics": "eligibility",
    "02. Prerequisite Finder": "requirements",
    "03. Roadmap Builder": "roadmap",
    "04. EssayAI": "essay"
  };

  container.innerHTML = steps.map((step, index) => {
    const stepNum = String(index + 1).padStart(2, '0');
    const parsedTip = step.tip.replace(/\[([^\]]+)\]/g, (match, p1) => {
      const cleanKey = p1.trim();
      const tab = FEATURE_TAB_MAP[cleanKey];
      if (tab) {
        return `<a href="#demo" onclick="handleDrawerNavigation('${tab}'); return false;" style="color: var(--accent); font-weight: 800; text-decoration: underline; cursor: pointer;">${cleanKey}</a>`;
      }
      return match;
    });

    return `
      <div class="guide-card">
        <div class="guide-step-num" style="width: 48px; height: 48px; border-radius: 12px; background: rgba(197, 168, 128, 0.15); color: var(--accent); display: grid; place-items: center; font-size: 20px; font-weight: 900; flex-shrink: 0;">${stepNum}</div>
        <div style="display: flex; flex-direction: column; gap: 8px;">
          <h3 style="font-size: 18px; font-weight: 800; color: var(--ink); margin: 0;">${step.title}</h3>
          <p style="font-size: 14px; color: var(--muted); line-height: 1.6; margin: 0;">
            ${step.body}
          </p>
          <div style="background: rgba(15, 30, 54, 0.03); border-left: 4px solid var(--accent); padding: 10px 16px; border-radius: 0 8px 8px 0; margin-top: 10px; font-size: 13.5px; color: var(--muted); line-height: 1.5;">
            ${parsedTip}
          </div>
        </div>
      </div>
    `;
  }).join("");
}
window.renderStrategyTimeline = renderStrategyTimeline;

function handleLogout() {
  const authState = readAuthState();
  authState.currentUser = "";
  writeAuthState(authState);
  
  // Reset state and localStorage
  state.plan = "Free";
  state.essayCredits = 0;
  state.analyzedSchools.clear();
  localStorage.setItem("transferCompassPlan", "Free");
  localStorage.setItem("transferCompassEssayCredits", "0");
  localStorage.removeItem("transferCompassAnalyzedSchools");
  
  renderAuthState();
  updateActivePlanLabel();
  updatePlanNoticeVisibility();
  updateEssayCreditsUI();
  syncSelectedTargetsFromSlots();
  renderEligibilityResults();
  buildRoadmap();
  
  const activeTabBtn = qs(".tab-button.active");
  if (activeTabBtn) {
    const tabName = activeTabBtn.getAttribute("data-tab");
    if (!checkPlanAccess(tabName)) {
      activateProductTab("eligibility");
    }
  }
}
window.handleLogout = handleLogout;

function updateSortedPrograms() {
  sortedPrograms = allPrograms().sort(
    (a, b) => a.school.name.localeCompare(b.school.name) || a.name.localeCompare(b.name)
  );
  const options = allPrograms()
    .map((program) => `<option value="${escapeHtml(program.id)}">${escapeHtml(program.school.shortName)} · ${escapeHtml(program.name)}</option>`)
    .join("");
  qs("#requirementSelect").innerHTML = options;
  const roadmapTarget = qs("#roadmapTarget");
  if (roadmapTarget) {
    roadmapTarget.innerHTML = options;
  }
}
window.updateSortedPrograms = updateSortedPrograms;

function openOnDemandScrapeModal(schoolName = "", majorName = "") {
  const modal = qs("#generateReqModal");
  if (modal) {
    qs("#genSchoolName").value = schoolName;
    qs("#genMajorName").value = majorName;
    qs("#genMessage").textContent = "";
    qs("#genMessage").className = "feedback-status";
    qs("#genLoader").classList.add("hidden");
    qs("#genSubmitBtn").disabled = false;
    modal.classList.remove("hidden");
  }
}
window.openOnDemandScrapeModal = openOnDemandScrapeModal;

function openOnDemandScrapeWithQuery(query) {
  let school = "";
  let major = "";
  
  if (query.includes("-")) {
    const parts = query.split("-");
    school = parts[0].trim();
    major = parts.slice(1).join("-").trim();
  } else if (query.includes("·")) {
    const parts = query.split("·");
    school = parts[0].trim();
    major = parts.slice(1).join("·").trim();
  } else {
    const words = query.trim().split(/\s+/);
    if (words.length > 1) {
      school = words[0];
      major = words.slice(1).join(" ");
    } else {
      school = query;
      major = "";
    }
  }
  openOnDemandScrapeModal(school, major);
}
window.openOnDemandScrapeWithQuery = openOnDemandScrapeWithQuery;

function closeOnDemandScrapeModal() {
  const modal = qs("#generateReqModal");
  if (modal) modal.classList.add("hidden");
}
window.closeOnDemandScrapeModal = closeOnDemandScrapeModal;

async function handleOnDemandScrape(event) {
  event.preventDefault();
  const schoolName = qs("#genSchoolName").value.trim();
  const majorName = qs("#genMajorName").value.trim();
  const msgEl = qs("#genMessage");
  const loaderEl = qs("#genLoader");
  const submitBtn = qs("#genSubmitBtn");
  
  if (!schoolName || !majorName) return;
  
  msgEl.textContent = "";
  msgEl.className = "feedback-status";
  loaderEl.classList.remove("hidden");
  submitBtn.disabled = true;
  
  try {
    const response = await fetch("/api/requirements/generate", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ schoolName, majorName })
    });
    const result = await response.json();
    
    if (result.success) {
      // 1. Add to the local in-memory database
      let localSchool = database.schools.find(s => s.name.toLowerCase() === schoolName.toLowerCase() || s.shortName.toLowerCase() === schoolName.toLowerCase());
      if (!localSchool) {
        localSchool = {
          id: result.school.id,
          name: result.school.name,
          shortName: result.school.shortName,
          majors: []
        };
        database.schools.push(localSchool);
      }
      
      const newMajor = result.major;
      const existingMajorIdx = localSchool.majors.findIndex(m => m.name.toLowerCase() === majorName.toLowerCase());
      if (existingMajorIdx >= 0) {
        localSchool.majors[existingMajorIdx] = newMajor;
      } else {
        localSchool.majors.push(newMajor);
      }
      
      database.schoolCount = database.schools.length;
      database.programCount = database.schools.flatMap(s => s.majors).length;
      
      // 2. Re-render select options and lists
      updateSortedPrograms();
      
      // 3. Highlight/Select the newly generated major in Tab 2 (ReqRadar)
      const reqSchoolInput = qs("#reqSchoolInput");
      const reqMajorInput = qs("#reqMajorInput");
      const reqMajorToggle = reqMajorInput?.parentElement?.querySelector(".autocomplete-toggle-btn");
      if (reqSchoolInput) reqSchoolInput.value = localSchool.name;
      if (reqMajorInput) {
        reqMajorInput.value = newMajor.name;
        reqMajorInput.removeAttribute("disabled");
        reqMajorInput.setAttribute("data-i18n-placeholder", "select_major_placeholder");
        reqMajorInput.placeholder = t("select_major_placeholder", "Select Major");
        if (reqMajorToggle) reqMajorToggle.removeAttribute("disabled");
      }
      qs("#requirementSelect").value = newMajor.id;
      renderRequirementDetail(newMajor.id);
      
      // 4. Show success message
      msgEl.textContent = state.language === "ko" ? "성공적으로 요건을 분석하여 데이터베이스에 반영했습니다!" : "Successfully generated and added prerequisites to the database!";
      msgEl.className = "feedback-status success";
      
      // Close modal after 1.5 seconds
      setTimeout(() => {
        closeOnDemandScrapeModal();
      }, 1500);
      
    } else {
      msgEl.textContent = (state.language === "ko" ? "생성 실패: " : "Generation failed: ") + result.message;
      msgEl.className = "feedback-status error";
    }
  } catch (err) {
    msgEl.textContent = (state.language === "ko" ? "서버 통신 오류: " : "Server communication error: ") + err.message;
    msgEl.className = "feedback-status error";
  } finally {
    loaderEl.classList.add("hidden");
    submitBtn.disabled = false;
  }
}
window.handleOnDemandScrape = handleOnDemandScrape;
